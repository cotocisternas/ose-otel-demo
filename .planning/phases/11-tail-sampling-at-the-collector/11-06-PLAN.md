---
phase: 11-tail-sampling-at-the-collector
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04, 05]
files_modified:
  - README.md
  - docs/screenshots/step-11-tail-sampling-ON.png
  - grafana/dashboards/ose-otel-demo.json
autonomous: false
requirements: [TSAMP-02, TSAMP-03]
must_haves:
  truths:
    - "README.md has a new ## Step 11: Tail Sampling at the Collector section between current ## Step 10 and ## Concepts & FAQ"
    - "The §11 narrative has the five sub-section shape (What you'll learn / Checkpoint / Run / What to look for / Why it matters) plus the F2-3 head-vs-tail double-filter blockquote at the end (D-T11 + D-T12)"
    - "The OFF/ON paired screenshot HTML <table> renders side-by-side (D-T9 + D-T10) — references step-11-tail-sampling-OFF.png (committed in plan 11-01) and step-11-tail-sampling-ON.png (committed in this plan)"
    - "The §11 narrative includes the OR-semantics priority chain explanation (paraphrased from research/PITFALLS.md F2-1 in workshop voice — NOT verbatim from the YAML comment), the WIDGET-SLOW interaction lesson, the composite-vs-flat trade-off + recordpolicy alpha-gate caveat (per DISCUSSION-LOG.md plan-time amendment), and the mise run invocations (mise run load SLOW_RPS=2 + mise run verify:tail-sampling)"
    - "The §11 'What you'll learn' bullet that summarises the priority chain acknowledges the FOUR-tier reality (drop > inverted_not_sample > sample > inverted_sample, with inverted_* tiers deprecated) — does NOT reduce the chain to a 3-tier mental model — so a skim-reader cannot diverge from the YAML's verbatim TSAMP-02 four-tier quote (per WARNING-2 fix; CONTEXT.md <specifics> bullet 3: YAML / README §11 / F2-3 callout MUST agree on OR-semantics)"
    - "docs/screenshots/step-11-tail-sampling-ON.png exists, is a valid PNG ≥10KB, captured from the same Tempo Search query as the OFF screenshot but post-Phase-11 (D-T10)"
    - "The histogram-suffix verification micro-task ran and confirmed (or corrected) the `_milliseconds` / `_seconds` unit suffix presence on the tail_sampling histogram metrics per RESEARCH §3.2.1 — AND, when invoked outside the operator-driven E2E flow, the gate degrades gracefully (skip, exit 0) rather than reporting a false-FAIL (per WARNING-4 fix)"
    - "If the suffix verification revealed missing suffixes, the dashboard PromQL in plan 11-05 was corrected in this plan with a 1-line edit"
  artifacts:
    - path: "README.md"
      provides: "Workshop step-11 narrative + paired-screenshot table + F2-3 double-filter callout (with four-tier-aware priority-chain bullet per WARNING-2 fix)"
      contains: "Step 11: Tail Sampling"
    - path: "docs/screenshots/step-11-tail-sampling-ON.png"
      provides: "Post-Phase-11 Tempo Search trace count screenshot for OFF/ON paired demo"
  key_links:
    - from: "README.md ## Step 11 paired-screenshot table"
      to: "docs/screenshots/step-11-tail-sampling-OFF.png + docs/screenshots/step-11-tail-sampling-ON.png"
      via: "HTML <table> with two columns (OFF / ON)"
      pattern: "step-11-tail-sampling-(OFF|ON).png"
    - from: "README.md ## Step 11 F2-3 blockquote"
      to: "ROADMAP.md Phase 16 callout"
      via: "head-vs-tail double-filter trap warning — Phase 16 will back-link"
      pattern: "double-filter trap|F2-3"
---

<objective>
Three deliverables in this final plan:

1. **README.md ## Step 11 section (D-T11):** ~100-150 lines mirroring Step 10's depth — five sub-sections (What you'll learn / Checkpoint / Run / What to look for / Why it matters) + the F2-3 head-vs-tail double-filter callout blockquote at the end (D-T12). Includes the OR-semantics priority chain narrative (paraphrased from research/PITFALLS.md F2-1, but the "What you'll learn" summary bullet acknowledges the four-tier reality so it does not diverge from the YAML's verbatim TSAMP-02 quote per WARNING-2 fix), the WIDGET-SLOW interaction lesson, the composite-vs-flat trade-off + recordpolicy alpha-gate caveat (per DISCUSSION-LOG.md plan-time amendment), the OFF/ON paired-screenshot HTML <table>, and the mise run invocations.

2. **docs/screenshots/step-11-tail-sampling-ON.png (D-T9 / D-T10):** Operator captures the ON screenshot from the same Tempo Search query as the OFF screenshot from plan 11-01 — Service Name = order-producer, Last 5 minutes — but now post-Phase-11 with all plans landed and live load running.

3. **Histogram-suffix verification micro-task (RESEARCH §3.2.1):** Run a one-shot bash check that asserts the `_milliseconds` / `_seconds` unit suffixes ARE present on the tail_sampling histogram metric series at this collector-contrib version. If they ARE NOT present (MEDIUM-confidence per RESEARCH §3.2.1), apply a 1-line correction to the Panel 4 PromQL in `grafana/dashboards/ose-otel-demo.json` (drop `_milliseconds` from the bucket name). This is cheap insurance, not a blocker. The check now degrades gracefully when the live stack is not running (per WARNING-4 fix), so a CI lint pass invoking the gate outside the operator-driven E2E flow does not report a false-FAIL.

Purpose: This is the human-facing teaching artifact (README §11) + the visual proof (ON screenshot) + the workshop-grade fragility-detection insurance (histogram-suffix check). Once this lands, Phase 11 ships.

Output: README §11 added; ON screenshot captured + committed; dashboard panel queries corrected if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tail-sampling-at-the-collector/11-CONTEXT.md
@.planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md
@.planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md
@.planning/phases/11-tail-sampling-at-the-collector/11-DISCUSSION-LOG.md
@README.md
@grafana/dashboards/ose-otel-demo.json

<interfaces>
<!-- README.md existing anchors: -->
<!-- Line 457: ## Step 10: Stack Decomposition — from one container to five (5-section template starts here) -->
<!-- Line 548: end of Step 10 section -->
<!-- Line 550: ## Concepts & FAQ -->
<!-- Lines 143-152 (Step 2/3 area): HTML <table> paired-screenshot pattern (Phase 7 D-04 precedent) -->
<!-- Lines 469: phase-tag callout pattern: "Workshop is at `step-NN-...` — the orchestrator applies this annotated tag atomically..." -->

<!-- INSERTION SITE: new ## Step 11 section between line 548 (end of Step 10) and line 550 (## Concepts & FAQ) -->

<!-- screenshot file (NEW): docs/screenshots/step-11-tail-sampling-ON.png -->
<!-- Naming convention per PATTERNS.md row 9: step-NN-<short-name>.png -->
<!-- Existing in docs/screenshots/: step-01-empty-tempo.png, step-02-disconnected-traces.png, step-03-joined-trace.png, step-03-waterfall.png, step-05-logs-trace-jump.png, step-06-test-output.png -->
<!-- Plus from plan 11-01: step-11-tail-sampling-OFF.png -->

<!-- ose-otel-demo.json potential touch (only if histogram suffix is missing): -->
<!-- Panel 4 (id 13) PromQL line in 3 places — drop the `_milliseconds` infix from `sampling_decision_timer_latency_milliseconds_bucket` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run histogram-suffix verification micro-task; correct dashboard PromQL if needed</name>
  <files>grafana/dashboards/ose-otel-demo.json</files>
  <read_first>
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §3.2 lines 485-507 — Prometheus exposition format expectations + MEDIUM-confidence note on unit-suffix exposition
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §3.2.1 lines 494-507 — verbatim verification step
    - grafana/dashboards/ose-otel-demo.json — Panel 4 (id 13) PromQL queries that assume `_milliseconds` suffix is present (in 3 places — p50/p95/p99 quantile queries)
  </read_first>
  <action>
**Step A — Confirm prerequisites.** Plans 11-02..11-05 must already be applied. Run:
- `mise run infra:up` (idempotent)
- `mise run dev` (in another terminal, if not already running)
- `mise run load SLOW_RPS=2` (in another terminal — let it run for ~60 seconds so the Collector decision_wait window has populated histograms)

**Step B — Capture the live :8888/metrics output.** Run the verification check verbatim from RESEARCH §3.2.1:

```bash
curl -s http://localhost:8888/metrics | grep -E '^otelcol_processor_tail_sampling_' | sort -u > /tmp/phase11-tailsampling-metrics.txt
wc -l /tmp/phase11-tailsampling-metrics.txt
cat /tmp/phase11-tailsampling-metrics.txt
```

The output is the source of truth. Look for lines matching the histogram metric names referenced by Panel 4 in `grafana/dashboards/ose-otel-demo.json`:
- `otelcol_processor_tail_sampling_sampling_decision_timer_latency_milliseconds_bucket{...}`  ← what Panel 4 ASSUMES
- OR `otelcol_processor_tail_sampling_sampling_decision_timer_latency_bucket{...}`  ← what Panel 4 needs to be corrected to, IF the `_milliseconds` infix is absent

**Step C — Decide and act.**

- **Case 1: `_milliseconds_bucket` IS present in the live output.** No dashboard correction needed. Echo "histogram suffix verification: PASS — _milliseconds suffix present" and proceed to Task 2. Panel 3 also assumes a `_seconds` suffix on the `sampling_late_span_age_bucket` series — verify the `_age_bucket` series exists; in v0.151.0 the late_span_age histogram unit is `s` so the bucket name SHOULD be `sampling_late_span_age_bucket` (no infix — `_age_bucket` is the canonical form because the metric name already contains `age`). The Panel 3 PromQL in plan 11-05 used `sampling_late_span_age_bucket` (NO `_seconds` infix), which is the CORRECT form per the metric name in RESEARCH §3.1 line 465. Confirm both panels match the live output.

- **Case 2: `_milliseconds_bucket` is ABSENT** (the metric appears as just `..._sampling_decision_timer_latency_bucket{...}` with no unit infix). Apply the 1-line correction to Panel 4 in `grafana/dashboards/ose-otel-demo.json`: drop `_milliseconds` from the bucket name in all three quantile queries. Use sed for the surgical edit:

  ```bash
  sed -i 's/sampling_decision_timer_latency_milliseconds_bucket/sampling_decision_timer_latency_bucket/g' grafana/dashboards/ose-otel-demo.json
  ```

  Then re-validate the JSON: `python3 -c "import json; json.load(open('grafana/dashboards/ose-otel-demo.json'))"` (must exit 0).

  Then `docker compose restart grafana` and reload the dashboard browser tab — Panel 4 should now show data.

**Step D — Document the outcome in the plan summary.** The 11-06-SUMMARY.md will record which case applied and whether a correction was made. This is the workshop-grade insurance trail per RESEARCH §3.2.1.

**Step E — Graceful-skip behaviour (per WARNING-4 fix).** This task's <automated> verify block now front-loads a precondition probe so that, when invoked outside the operator-driven E2E flow (e.g., by a CI lint pass with no live stack), the gate exits 0 with a `SKIP:` log line instead of reporting a false-FAIL. The trailing log line distinguishes the two valid outcomes — `SKIP: ...` (stack not live) vs `VERIFIED: ...` (stack live and suffix verified). Both are valid exit-0 paths. The operator-driven E2E flow always produces `VERIFIED:`; CI lint passes always produce `SKIP:`.
  </action>
  <verify>
    <automated>bash -c 'set -e; LOG=/tmp/11-06-task1.log; : > "$LOG"; if ! docker compose ps otel-collector 2>/dev/null | grep -qE "running|Up"; then echo "SKIP: otel-collector is not running; suffix verification requires live stack (mise run infra:up && mise run dev && mise run load)." | tee -a "$LOG"; exit 0; fi; if ! curl -sf http://localhost:8888/metrics > /dev/null; then echo "SKIP: :8888/metrics is not responding; suffix verification requires Collector to be emitting telemetry." | tee -a "$LOG"; exit 0; fi; curl -fsS http://localhost:8888/metrics | grep -E "^otelcol_processor_tail_sampling_" > /tmp/phase11-metrics-check.txt; [ -s /tmp/phase11-metrics-check.txt ]; SUFFIX_PRESENT=$(grep -c "sampling_decision_timer_latency_milliseconds_bucket\\|sampling_decision_timer_latency_bucket" /tmp/phase11-metrics-check.txt); [ "$SUFFIX_PRESENT" -gt 0 ]; DASH_QUERY=$(grep -c "sampling_decision_timer_latency_milliseconds_bucket\\|sampling_decision_timer_latency_bucket" grafana/dashboards/ose-otel-demo.json); [ "$DASH_QUERY" -ge 3 ]; python3 -c "import json; json.load(open(\"grafana/dashboards/ose-otel-demo.json\"))"; LIVE_HAS_MS=$(grep -c "sampling_decision_timer_latency_milliseconds_bucket" /tmp/phase11-metrics-check.txt); DASH_HAS_MS=$(grep -c "sampling_decision_timer_latency_milliseconds_bucket" grafana/dashboards/ose-otel-demo.json); if [ "$LIVE_HAS_MS" -gt 0 ]; then [ "$DASH_HAS_MS" -ge 3 ] || (echo "DRIFT: live has _milliseconds but dashboard does not" | tee -a "$LOG"; exit 1); else [ "$DASH_HAS_MS" -eq 0 ] || (echo "DRIFT: live has no _milliseconds but dashboard still uses it" | tee -a "$LOG"; exit 1); fi; echo "VERIFIED: histogram suffix verification PASS — dashboard agrees with live :8888 exposition" | tee -a "$LOG"'</automated>
  </verify>
  <acceptance_criteria>
    - The bash check above exits 0 in BOTH valid outcomes (per WARNING-4 fix): (a) `VERIFIED: ...` — stack live and suffix verified, OR (b) `SKIP: ...` — stack not live, gate degraded gracefully.
    - `grep -E "SKIP|VERIFIED" /tmp/11-06-task1.log` exits 0 (the log file always contains exactly one of the two outcome lines, distinguishing live-verified from gracefully-skipped runs)
    - When invoked from the operator-driven E2E flow (post `mise run infra:up && mise run dev && mise run load SLOW_RPS=2`, after ~60s for histogram population), the log line MUST be `VERIFIED: ...` — the SKIP path is reserved for non-E2E invocations like CI lint passes
    - `python3 -c "import json; json.load(open('grafana/dashboards/ose-otel-demo.json'))"` exits 0 (if a sed correction was applied, JSON still parses) — runs only on the VERIFIED path
    - On the VERIFIED path: file `/tmp/phase11-metrics-check.txt` exists and is non-empty (proves the live curl succeeded); the dashboard's Panel 4 PromQL form AGREES with the live exposition form (both have `_milliseconds` OR both lack it — no drift)
    - On the SKIP path: no dashboard mutation occurs; the operator must re-run after starting the stack to actually verify the suffix
  </acceptance_criteria>
  <done>The dashboard's Panel 4 PromQL agrees with the actual `:8888/metrics` exposition format at this collector-contrib version. Either (Case 1) the `_milliseconds` suffix is present in both — no edit needed, OR (Case 2) the suffix was absent and a sed correction was applied to drop `_milliseconds` from all three Panel 4 quantile queries. The dashboard JSON parses cleanly. After dashboard reload, Panel 4 shows live data. WHEN INVOKED OUTSIDE THE OPERATOR-DRIVEN E2E FLOW (per WARNING-4 fix), the gate exits 0 with a SKIP log line — no false-FAIL — and the operator must re-run after starting the stack to actually verify the suffix.</done>
</task>

<task type="auto">
  <name>Task 2: Add ## Step 11: Tail Sampling at the Collector section to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md — current state (lines 457-548 Step 10 as the structural template per PATTERNS.md row 7; lines 143-152 paired-screenshot HTML <table> precedent from Phase 7 D-04)
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §7 lines 936-960 — verbatim paired-screenshot HTML <table> for OFF/ON pair
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §2.4 lines 389-408 — OR-semantics priority chain quote (paraphrase in README; YAML quotes verbatim)
    - .planning/phases/11-tail-sampling-at-the-collector/11-CONTEXT.md `<decisions>` D-T11 (Phase-10-equivalent depth), D-T12 (F2-3 inline blockquote at end of §11), and `<specifics>` bullet 3 (YAML / README §11 / F2-3 callout MUST agree on OR-semantics — three artifacts must NOT diverge per WARNING-2 fix)
    - .planning/phases/11-tail-sampling-at-the-collector/11-DISCUSSION-LOG.md lines 235-256 — Route A confirmation; README §11 narrative MUST add a paragraph explaining the composite-vs-flat trade-off + recordpolicy alpha-gate caveat
    - .planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md row 7 + Shared Pattern F — README Step-N section shape
    - .planning/research/PITFALLS.md F2-3 — verbatim source for the head-vs-tail double-filter callout (paraphrased in workshop voice)
    - ROADMAP.md Phase 16 section "CRITICAL: Double-filter trap" callout — F2-3 verbatim source for the back-link contract (Phase 16 will back-link to this §11 blockquote anchor)
  </read_first>
  <action>
**Step A — Locate insertion site.** Find the end of `## Step 10: Stack Decomposition` (around current line 548) and the start of `## Concepts & FAQ` (around current line 550). Insert the new `## Step 11` section between them. There should be a blank line above and below.

**Step B — Author the §11 section.** Use the structure below verbatim. Phase-10-equivalent depth means ~100-150 lines, five sub-sections, plus the F2-3 blockquote at the end. PromQL/YAML excerpts are quoted from the actual files (otelcol-config.yaml, mise.toml, ose-otel-demo.json) so the README stays in sync if those files change.

**WARNING-2 FIX:** The "What you'll learn" priority-chain bullet (third bullet below) was previously `drop > sample > not_sample` — a three-tier paraphrase that diverged from the YAML's verbatim four-tier reality (`drop > inverted_not_sample > sample > inverted_sample`). The replacement bullet now acknowledges the four-tier chain explicitly, names the deprecated `inverted_*` tiers, and notes that the workshop config uses neither — so a skim-reader builds the SAME mental model as the YAML and the F2-3 callout (per CONTEXT.md `<specifics>` bullet 3).

```markdown
## Step 11: Tail Sampling at the Collector — what survives the Tempo write path

### What you'll learn

- A Collector-side `tail_sampling` processor sees the **complete assembled trace** before deciding whether to ship it to Tempo — a decision the SDK can never make because the SDK only sees one process at a time.
- The TSAMP-01 three-policy chain — **status_code ERROR keep-100%**, **latency >1s keep-100%**, **probabilistic 20% fallback** — is **OR-semantics**, not first-match: every policy votes on every trace, and the four-tier priority chain `drop > inverted_not_sample > sample > inverted_sample` decides the outcome (the `inverted_*` tiers are deprecated as of collector-contrib v0.150+ and our workshop config uses neither, so in practice the chain reduces to "ANY sub-policy votes sample → trace kept" — but the four-tier ordering is the reality, and the verbatim YAML comment in `infra/observability/otelcol-config.yaml` quotes it in full).
- The `composite` envelope adds a **per-branch rate-limit** lesson on top of OR-semantics: each sub-policy has its own `spans/s` budget that gates its vote BEFORE composite returns. Bursts visibly clamp.
- A WIDGET-SLOW SKU branch in `OrderService.place()` plus a fourth oha stream in `scripts/load.sh` (`SLOW_RPS=2` default) reliably trip the latency policy under workshop load — without these, the latency branch would be inert.
- A new "Tail Sampling diagnostics" Grafana row surfaces which policy fired how often, late-span behavior, and decision-loop latency so attendees can SEE the processor's internals — backed at runtime by `mise run verify:tail-sampling`.

### Checkpoint

Workshop is at `step-11-tail-sampling` — the orchestrator applies this annotated tag atomically with the phase-completion merge per WORK-01 / D-21. `git checkout step-11-tail-sampling` jumps to this point. The previous tag is `step-10-collector-decompose`; `git diff step-10-collector-decompose..step-11-tail-sampling` shows the focused diff: ~70 YAML lines, ~5 Java lines, ~25 bash lines, ~150 dashboard JSON lines, one docker-compose flag, and this README section.

### Run

```bash
mise run preflight
mise run infra:up
mise run dev
# In another terminal — the SLOW_RPS=2 stream drives the latency policy:
mise run load SLOW_RPS=2
# Wait ~60s for the Collector to populate the decision_wait window:
mise run verify:tail-sampling
# Open Grafana and expand the Tail Sampling diagnostics row:
mise run ui:grafana
```

`verify:tail-sampling` is a two-tier check (Phase 11 D-T14): Tier 1 curls `:8888/metrics` and asserts the `composite-policy` outer name is registered; Tier 2 curls Tempo `:3200/api/search` and asserts each of the three sub-policy names (`keep-errors`, `keep-slow`, `probabilistic-fallback`) appears as a `tailsampling.composite_policy` span attribute. The second tier depends on the alpha feature gate `processor.tailsamplingprocessor.recordpolicy` being enabled on the otel-collector container — see "Why composite + alpha gate?" below.

### What to look for

> **A note on three artifacts saying the same thing (per CONTEXT.md `<specifics>` bullet 3 / WARNING-2 fix).** The OR-semantics priority chain is documented in three places: (1) the verbatim YAML comment above the `composite:` block in `infra/observability/otelcol-config.yaml` quotes the full TSAMP-02 four-tier chain exactly as it appears in the upstream collector-contrib docs; (2) this README §11 paraphrases the chain in workshop voice but acknowledges the four-tier reality (see the third "What you'll learn" bullet — `drop > inverted_not_sample > sample > inverted_sample`); (3) the F2-3 double-filter-trap callout at the end of this section names the priority-chain tiers consistently. All three artifacts agree on the four-tier reality. If you see a divergence — e.g., a paraphrase that reduces the chain to three tiers — it is a documentation bug; please open an issue.

**Trace count delta in Tempo.** Before and after Phase 11, the same `Service Name = order-producer / Last 5 minutes` Tempo Search returns very different counts:

<table>
<tr>
<td><b>Pre-Phase-11 (OFF) — all traces reach Tempo</b></td>
<td><b>Post-Phase-11 (ON) — composite policy in effect</b></td>
</tr>
<tr>
<td><img src="docs/screenshots/step-11-tail-sampling-OFF.png" alt="Tempo Search showing N traces in last 5 min, no tail sampling"></td>
<td><img src="docs/screenshots/step-11-tail-sampling-ON.png"  alt="Tempo Search showing M traces in last 5 min — every error trace, every slow trace, ~20% of the rest"></td>
</tr>
</table>

The exact ratio depends on load mix: at default `mise run load SLOW_RPS=2` settings (~200 rps steady, ~10% errors, ~2 slow rps), the kept-trace count drops to roughly **30%** of the unsampled baseline (100% errors + 100% slow + 20% of the rest).

**The OR-semantics priority chain in action.** The TSAMP-01 policies are wrapped in a `composite` envelope — they all vote on every trace, and composite returns SAMPLED on the first sub-policy that votes sample within its rate cap. Concretely:

- A trace with `status=ERROR` → `keep-errors` votes sample → trace kept.
- A trace with duration > 1s but no error → `keep-slow` votes sample → trace kept.
- A trace with neither → `probabilistic-fallback` rolls a 20% die → trace kept ~20% of the time.
- A trace that is BOTH error AND slow (every ~10th `WIDGET-SLOW` order under the deterministic 10% failure path) → `keep-errors` votes sample first, `keep-slow` never gets a vote, trace kept exactly ONCE. **This is the visible OR-semantics demo: the trace appears once, not twice.**

The four-tier priority chain documented above the `composite:` block in `infra/observability/otelcol-config.yaml` is the canonical TSAMP-02 reference: `drop > inverted_not_sample > sample > inverted_sample`. With our config we use no `drop` policies and no `inverted_*` policies, so the chain reduces in practice to "ANY sub-policy votes sample → trace kept" — but the four-tier ordering is the upstream reality and matters if you ever add a `drop` policy (which would override every `sample` vote).

**The Tail Sampling diagnostics dashboard row.** Open `mise run ui:grafana`, navigate to the `ose-otel-demo` dashboard, and expand the "Tail Sampling diagnostics (Phase 11)" collapsed row. Five panels:

1. **Sampling decisions (composite envelope)** — under Route A composite collapses sub-policy attribution at the metric layer, so this panel shows ONE series per `policy=composite-policy / decision=...`. For per-sub-policy attribution, click through to a kept trace in Tempo and inspect the `tailsampling.composite_policy` span attribute (this is exactly what `verify:tail-sampling` Tier 2 asserts).
2. **Per-policy not-sample votes** — surfaces the drop-vote rate; should show probabilistic-fallback's not_sampled rate ≈ 4× its sample rate (20% sampling → 80% not-sampled).
3. **Late-arriving spans** — F2-2 mitigation surface. Low and stable means `decision_wait: 10s` is right-sized.
4. **Sampling decision-loop latency (p50/p95/p99)** — should stay well under 10ms at workshop scale.
5. **Traces in memory (sanity gauge)** — should oscillate around `decision_wait × incoming_rps` ≈ 2000.

### Why it matters

Tail sampling is the **single biggest cost lever** in production observability. Head sampling (Phase 16) saves bandwidth at the SDK boundary but the SDK can't see the trace it's about to drop. Tail sampling buffers each trace at the Collector for `decision_wait` seconds, then makes a decision based on the **assembled** trace's content — error status, total latency, length. Production teams routinely run head sampling at 100% (no SDK-side dropping) AND tail sampling at 1-5% (Collector keeps the interesting ones) — exactly the layering Phase 11 sets up the demonstration for.

**Why composite + alpha gate?** TSAMP-01's three policies could have been written as a flat top-level list; we wrapped them in a `composite` envelope to demonstrate per-branch rate-limits (the production-realism payoff). The trade-off: at collector-contrib v0.151.0, composite collapses sub-policy attribution at the metric layer — `count_traces_sampled` only carries `policy="composite-policy"`. To recover per-sub-policy attribution we enable the alpha feature gate `processor.tailsamplingprocessor.recordpolicy` on the otel-collector container (see `docker-compose.yml`), which causes the Collector to stamp each sampled span with a `tailsampling.composite_policy` attribute carrying the sub-policy name. This is alpha at v0.151.0 — could rename or default-on in v0.152+, hence `verify:tail-sampling` Tier 2 fast-fails any drift. The full trade-off discussion is in `.planning/phases/11-tail-sampling-at-the-collector/11-DISCUSSION-LOG.md` plan-time amendment.

**The `decision_wait` / `num_traces` sizing tradeoff.** Lower `decision_wait` = faster workshop iteration but more late-span fragments (Scenario 2 of the v0.151.0 README's three late-span scenarios). Higher = lower late-span risk but slower workshop feedback. We picked `10s` and `10000` traces — at ~200 rps steady that's ~2000 in-flight traces vs 10000 buffer = 5× headroom; F2-2 mitigation. The "Traces in memory" panel is the sanity check.

**The WIDGET-SLOW interaction.** A pure-Collector phase would leave the latency policy inert — without the producer-side `Thread.sleep(1500)` for `sku=WIDGET-SLOW` (D-T5 / D-T6) and the `SLOW_RPS=2` load.sh stream (D-T7), no trace would exceed 1s under default load. The 5-line Java branch + 25-line bash addition is a small "bend the pure-Collector framing" cost paid for a high-payoff teaching moment: attendees see all three policies fire concurrently, and the deterministic 10% counter on the consumer side means ~10% of slow traces are ALSO error traces — perfect OR-semantics demonstration.

> **CRITICAL: Double-filter trap (F2-3).** Phase 16 will introduce **SDK-side head sampling** at 50% via `Sampler.parentBased(Sampler.traceIdRatioBased(0.5))`. Running Phase 16's head sampling (50%) simultaneously with Phase 11's tail sampling (20% probabilistic fallback) produces an **effective rate of approximately 10%** (50% head × 20% tail) — visibly under-sampled. Workshop attendees who have Phase 11's `step-11-tail-sampling` checkpoint active while activating Phase 16 MUST either (a) set `OTEL_TRACES_SAMPLER=parentbased_always_on` to disable head sampling during tail-sampling demos, or (b) explicitly accept the ~10% effective rate. Phase 16's README §16 callout will back-link to this blockquote anchor. (The four-tier OR-semantics priority chain — `drop > inverted_not_sample > sample > inverted_sample` — described in "What you'll learn" above governs which trace survives this double filter.)
```

**Step C — Verify markdown structure.** Run a quick grep to confirm the section landed correctly:
- `grep -n "^## Step 11:" README.md` — should return exactly one line, and its line number must be between Step 10 and Concepts & FAQ.
- `grep -n "^## " README.md` — verify the section ordering: Step 10 → Step 11 → Concepts & FAQ.

**Step D — Verify three-artifact agreement (per WARNING-2 fix).** Confirm the three places that document the priority chain all reference the four-tier reality:
- `grep -n "drop > inverted_not_sample > sample > inverted_sample" README.md` — the README §11 must mention the four-tier chain at least once (in the "What you'll learn" bullet AND in the "What to look for" canonical-reference paragraph).
- `grep -n "drop > inverted_not_sample > sample > inverted_sample" infra/observability/otelcol-config.yaml` — the YAML verbatim quote must be present (landed in plan 11-02).
- The F2-3 callout text in this README §11 must reference the four-tier chain (in the parenthetical at the end of the blockquote).
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "^## Step 11: Tail Sampling at the Collector" README.md && grep -q "step-11-tail-sampling-OFF.png" README.md && grep -q "step-11-tail-sampling-ON.png" README.md && grep -q "Workshop is at .step-11-tail-sampling." README.md && grep -q "mise run load SLOW_RPS=2" README.md && grep -q "mise run verify:tail-sampling" README.md && grep -q "WIDGET-SLOW" README.md && grep -q "composite" README.md && grep -q "OR-semantics" README.md && grep -q "double-filter trap" README.md && grep -q "F2-3" README.md && grep -q "OTEL_TRACES_SAMPLER=parentbased_always_on" README.md && grep -q "recordpolicy" README.md && grep -q "tailsampling.composite_policy" README.md && grep -q "### What you.ll learn" README.md && grep -q "### Checkpoint" README.md && grep -q "### Run" README.md && grep -q "### What to look for" README.md && grep -q "### Why it matters" README.md && grep -q "drop > inverted_not_sample > sample > inverted_sample" README.md && [ "$(grep -c "drop > inverted_not_sample > sample > inverted_sample" README.md)" -ge 2 ] && grep -q "inverted_\\* tiers are deprecated" README.md && awk "/^## Step 10:/{s10=NR} /^## Step 11:/{s11=NR} /^## Concepts/{cf=NR} END{exit !(s10<s11 && s11<cf)}" README.md && echo "task2 ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## Step 11: Tail Sampling at the Collector" README.md` exits 0 (section header present at H2 level)
    - `awk "/^## Step 10:/{s10=NR} /^## Step 11:/{s11=NR} /^## Concepts/{cf=NR} END{exit !(s10<s11 && s11<cf)}" README.md` exits 0 (Step 11 sits between Step 10 and Concepts & FAQ — correct ordering per RESEARCH §7 insertion site)
    - `grep -q "### What you.ll learn" README.md` exits 0 (sub-section 1 of 5 — D-T11 five-section shape)
    - `grep -q "### Checkpoint" README.md` exits 0 (sub-section 2)
    - `grep -q "### Run" README.md` exits 0 (sub-section 3)
    - `grep -q "### What to look for" README.md` exits 0 (sub-section 4)
    - `grep -q "### Why it matters" README.md` exits 0 (sub-section 5)
    - `grep -q "step-11-tail-sampling-OFF.png" README.md` exits 0 (paired-screenshot table references OFF screenshot from plan 11-01)
    - `grep -q "step-11-tail-sampling-ON.png" README.md` exits 0 (paired-screenshot table references ON screenshot from this plan)
    - `grep -q "Workshop is at .step-11-tail-sampling." README.md` exits 0 (phase-tag callout per Phase 10 D-21 / WORK-01 pattern)
    - `grep -q "mise run load SLOW_RPS=2" README.md` exits 0 (D-T7 invocation)
    - `grep -q "mise run verify:tail-sampling" README.md` exits 0 (D-T14 invocation)
    - `grep -q "WIDGET-SLOW" README.md` exits 0 (WIDGET-SLOW interaction lesson present)
    - `grep -q "composite" README.md` exits 0 (composite envelope mentioned)
    - `grep -q "OR-semantics" README.md` exits 0 (OR-semantics priority chain narrative present per D-T11 sub-section)
    - `grep -q "recordpolicy" README.md` exits 0 (alpha-gate caveat present per DISCUSSION-LOG plan-time amendment)
    - `grep -q "tailsampling.composite_policy" README.md` exits 0 (Tempo span attribute referenced in Tier 2 narrative)
    - `grep -q "double-filter trap" README.md` exits 0 (F2-3 callout title present per D-T12)
    - `grep -q "F2-3" README.md` exits 0 (F2-3 ID present for traceability)
    - `grep -q "OTEL_TRACES_SAMPLER=parentbased_always_on" README.md` exits 0 (escape hatch documented per ROADMAP Phase 16 callout verbatim)
    - `grep -q "Phase 16" README.md` exits 0 (forward-link to Phase 16 in F2-3 callout)
    - `grep -q "<table>" README.md && grep -q "</table>" README.md` exits 0 (HTML <table> for paired screenshots present)
    - `grep -A2 "<tr>" README.md | grep -q "Pre-Phase-11 (OFF)"` exits 0 (table caption for OFF column)
    - `grep -A2 "<tr>" README.md | grep -q "Post-Phase-11 (ON)"` exits 0 (table caption for ON column)
    - `grep -q "drop > inverted_not_sample > sample > inverted_sample" README.md` exits 0 (WARNING-2 fix: README acknowledges the YAML's verbatim four-tier priority chain so a skim-reader cannot diverge from the YAML)
    - `[ "$(grep -c "drop > inverted_not_sample > sample > inverted_sample" README.md)" -ge 2 ]` exits 0 (WARNING-2 fix: four-tier chain mentioned in BOTH the "What you'll learn" bullet AND the "What to look for" canonical-reference paragraph — so skim-readers and deep-readers both see it)
    - `grep -q "inverted_\\* tiers are deprecated" README.md` exits 0 (WARNING-2 fix: explicitly notes WHY the workshop config doesn't use the inverted_* tiers, preserving the four-tier mental model without confusing readers about the workshop's actual config)
    - `grep -q "If you see a divergence" README.md || grep -q "three artifacts" README.md` exits 0 (WARNING-2 fix: the "What to look for" canonical-reference note explicitly contrasts the three artifacts — YAML verbatim / README §11 paraphrase / F2-3 callout — and tells readers to file an issue if they diverge)
    - The §11 section is at least 80 lines long: `awk "/^## Step 11:/{p=1;next} /^## /{if(p){exit}} p{c++} END{exit !(c>=80)}" README.md` exits 0 (D-T11 ~100-150 lines target; 80 is the reasonable lower bound that confirms Phase-10-equivalent depth was achieved, not a one-paragraph stub)
  </acceptance_criteria>
  <done>README.md has a complete ## Step 11 section between Step 10 and Concepts & FAQ, with all five sub-sections (What you'll learn / Checkpoint / Run / What to look for / Why it matters), the OFF/ON paired-screenshot HTML <table>, the WIDGET-SLOW interaction lesson, the OR-semantics narrative including the four-tier priority chain reality (per WARNING-2 fix — the "What you'll learn" bullet acknowledges `drop > inverted_not_sample > sample > inverted_sample` and the "What to look for" canonical-reference note contrasts the three artifacts that must agree), the composite + recordpolicy alpha-gate caveat, the mise run invocations, and the F2-3 double-filter-trap blockquote at the end with the OTEL_TRACES_SAMPLER escape hatch.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator captures ON Tempo Search screenshot post-Phase-11</name>
  <read_first>
    - .planning/phases/11-tail-sampling-at-the-collector/11-CONTEXT.md `<decisions>` D-T9 / D-T10 — capture mechanism + subject
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §7 lines 936-960 — exact URL, query, and time range (must match the OFF capture for an apples-to-apples ratio)
    - .planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md row 9 — naming convention; analog (existing OFF screenshot from plan 11-01)
    - docs/screenshots/step-11-tail-sampling-OFF.png — to confirm the OFF screenshot exists (committed in plan 11-01); this task is the ON counterpart
  </read_first>
  <what-built>
    Plans 11-02..11-05 are landed. The dashboard row + verify:tail-sampling task confirm the processor is live. The README §11 section in Task 2 above references both screenshots in the paired <table>. Nothing automated for the screenshot capture itself — operator-driven per D-T9 / D-13 lineage.
  </what-built>
  <how-to-verify>
    Operator captures the ON screenshot manually using these EXACT steps (mirror the OFF capture for an apples-to-apples comparison):

    1. Confirm prerequisites:
       - Run: `mise run infra:up` (idempotent)
       - Run: `mise run dev` in another terminal (if not already up)
       - Run: `mise run load SLOW_RPS=2` in another terminal — let it run for at least 5 minutes so Tempo has populated trace data UNDER tail-sampling. The decision_wait=10s window means the first ~10s of load aren't sampled yet; after ~60s the sampling rate should be steady-state.

    2. Open browser to: `http://localhost:3000/explore`

    3. In Grafana Explore (use the EXACT same query as the OFF capture from plan 11-01):
       - Select the **Tempo** datasource (top-left datasource picker)
       - Select the **Search** tab (not "TraceQL")
       - Set **Service Name** = `order-producer`
       - Set the time range to **Last 5 minutes** (top-right time picker)
       - Click **Run query** (top-right blue button)

    4. Wait for the trace results table to populate. The trace count should be visibly LOWER than the OFF capture (rough target: ~30-40% of the OFF count, since 100% errors + 100% slow + 20% rest ≈ 30-40% of total at default load mix).

    5. Capture a screenshot of the **full Tempo Search results panel** (the same panel framing as the OFF capture). The capture should clearly show:
       - The Service Name = order-producer query at the top
       - The time range "Last 5 minutes"
       - The result count or visible long list of traces

    6. Save the file as `docs/screenshots/step-11-tail-sampling-ON.png` (PNG format, no compression artifacts that obscure the trace count).

    7. Verify the file:
       - Run: `file docs/screenshots/step-11-tail-sampling-ON.png` — should report `PNG image data, ...`
       - Run: `ls -la docs/screenshots/step-11-tail-sampling-ON.png` — should show non-zero file size (typically 50-500 KB)

    8. Open the README in a markdown previewer (or just render at GitHub) and confirm the OFF/ON <table> renders side-by-side with both images visible.

    9. Commit BOTH the screenshot AND any README changes from Task 2 (and any dashboard JSON changes from Task 1 if a correction was applied) in a single commit:
       - Run: `git add docs/screenshots/step-11-tail-sampling-ON.png README.md grafana/dashboards/ose-otel-demo.json` (only the dashboard if Task 1 modified it; check `git status` first)
       - Run: `git commit -m "docs(11): add §11 tail-sampling narrative + ON screenshot + histogram-suffix verification"`

    10. **Phase 11 ship gate:** with all six plans landed, run the end-to-end smoke one more time:
        - Run: `mise run infra:up && sleep 5`
        - Run: `mise run dev` in another terminal
        - Run: `mise run load SLOW_RPS=2` in another terminal
        - Wait 60 seconds
        - Run: `mise run verify:tail-sampling` — must exit 0 with GREEN message
        - Open `mise run ui:grafana` and confirm the Tail Sampling diagnostics row shows live data
        - Open Tempo Search (Service Name = order-producer, Last 5 min) and confirm the trace count matches the ON screenshot
  </how-to-verify>
  <resume-signal>Type "approved" once the ON screenshot is captured + committed, the README §11 paired-screenshot table renders both images, and the end-to-end smoke (verify:tail-sampling GREEN + dashboard live data + Tempo trace count matches ON capture) passes. Phase 11 is then ready for the orchestrator's atomic merge + tag-apply per WORK-01 (the orchestrator applies `step-11-tail-sampling` annotated tag — NOT this plan).</resume-signal>
  <acceptance_criteria>
    - `test -f docs/screenshots/step-11-tail-sampling-ON.png` exits 0
    - `file docs/screenshots/step-11-tail-sampling-ON.png | grep -q 'PNG image data'` exits 0
    - `[ "$(stat -c%s docs/screenshots/step-11-tail-sampling-ON.png)" -gt 10000 ]` exits 0 (PNG is at least 10KB)
    - `git log --oneline -- docs/screenshots/step-11-tail-sampling-ON.png | wc -l` is at least 1 (file is committed)
    - `test -f docs/screenshots/step-11-tail-sampling-OFF.png` exits 0 (OFF screenshot from plan 11-01 still present — paired)
    - End-to-end smoke: `mise run verify:tail-sampling` exits 0 with GREEN message after stack runs ~60s with `mise run load SLOW_RPS=2`
    - Grafana dashboard's Tail Sampling diagnostics row shows live data on at least Panel 1 (composite-policy/sampled series visible)
  </acceptance_criteria>
  <files>docs/screenshots/step-11-tail-sampling-ON.png</files>
  <action>
    See <how-to-verify> above — this is a checkpoint:human-action task. The executor presents the <what-built> + <how-to-verify> blocks to the operator, pauses for the <resume-signal>, and after "approved" verifies the PNG exists, is committed, the README §11 paired-screenshot table renders both images, and the end-to-end smoke (verify:tail-sampling GREEN + Grafana dashboard live data + Tempo trace count matches ON capture) passes. The 10 steps inside <how-to-verify> ARE the action.
  </action>
  <verify>
    <automated>test -f docs/screenshots/step-11-tail-sampling-ON.png && file docs/screenshots/step-11-tail-sampling-ON.png | grep -q 'PNG image data' && [ "$(stat -c%s docs/screenshots/step-11-tail-sampling-ON.png)" -gt 10000 ] && [ "$(git log --oneline -- docs/screenshots/step-11-tail-sampling-ON.png | wc -l)" -ge 1 ] && test -f docs/screenshots/step-11-tail-sampling-OFF.png</automated>
  </verify>
  <done>ON screenshot captured + committed; README §11 paired-screenshot table renders both OFF and ON images side-by-side; end-to-end smoke (verify:tail-sampling GREEN + dashboard live data + Tempo trace count matches ON capture) passes. Phase 11 is ship-ready; orchestrator applies step-11-tail-sampling annotated tag in the merge commit per WORK-01 / D-21 / Phase 2-06 / 6-06 / 10 precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → repo | Operator commits a binary PNG + README markdown changes; no untrusted external input crosses any application boundary |
| README workshop attendee → repo | Attendees clone the repo and follow §11 instructions; the README content is plain prose + commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | I (Information disclosure) | docs/screenshots/step-11-tail-sampling-ON.png | accept | Same rationale as plan 11-01 — workshop scope, synthetic data, public-readable repo per PROJECT.md. |
| T-11-06-02 | T (Tampering: a workshop attendee blindly copies the tail_sampling YAML to production without sizing for prod scale) | future production deployment | mitigate | README §11 "Why it matters" paragraph explicitly contrasts workshop-vs-production sizing. The decision_wait/num_traces tradeoff section names the workshop responsiveness vs production cost tradeoff. The composite+alpha-gate paragraph names the alpha-gate fragility surface explicitly. |
| T-11-06-03 | T (Tampering: F2-3 callout is misread or skipped) | workshop attendees enabling Phase 16 head sampling on the same stack | mitigate | The F2-3 callout is a `> **CRITICAL:**` blockquote at the END of §11 (where attendees finish reading), names the exact arithmetic (50% × 20% = ~10%), and provides TWO escape hatches. Phase 16's README will back-link here. ROADMAP Phase 16 also has the same callout. Triple-redundant warning. |
| T-11-06-04 | D (Denial of service: a typo'd histogram-suffix correction breaks Panel 4) | grafana/dashboards/ose-otel-demo.json | mitigate | Task 1 acceptance criterion validates JSON parses AFTER any sed correction; the dashboard reload smoke confirms Panel 4 either (Case 1) was already correct OR (Case 2) shows live data after correction. The new precondition probe (per WARNING-4 fix) ensures the gate degrades gracefully when invoked outside the operator-driven E2E flow — a CI lint pass logs `SKIP:` and exits 0 instead of mutating anything; only the operator-driven E2E flow with a live stack reaches the sed-correction path. |
| T-11-06-05 | I (Information disclosure: skim-reader builds a 3-tier mental model of the priority chain that diverges from the YAML's verbatim 4-tier reality, then mis-configures a production tail_sampling policy) | README §11 + workshop attendees taking the patterns to production | mitigate | Per WARNING-2 fix, the README §11 "What you'll learn" bullet now acknowledges the four-tier reality (`drop > inverted_not_sample > sample > inverted_sample`), names the deprecated `inverted_*` tiers, AND the "What to look for" section adds a canonical-reference note explicitly contrasting the three artifacts (YAML verbatim / README paraphrase / F2-3 callout) that must agree. The F2-3 callout's parenthetical also references the four-tier chain. Triple-redundant — a skim-reader, a deep-reader, and a callout-reader all see the same four-tier mental model. |
</threat_model>

<verification>
- README §11 section is present, complete, follows Phase-10 structural template, contains all D-T11 + D-T12 elements
- README §11's three priority-chain references (the "What you'll learn" bullet, the "What to look for" canonical-reference note, the F2-3 callout's parenthetical) all acknowledge the four-tier reality per WARNING-2 fix — no skim-reader diverges from the YAML's verbatim TSAMP-02 quote
- ON screenshot exists, valid PNG, committed
- Dashboard Panel 4 PromQL agrees with live :8888 exposition format (either Case 1 — no edit; or Case 2 — sed correction applied + JSON re-validated). When invoked outside the operator-driven E2E flow, Task 1 degrades gracefully (SKIP, exit 0) per WARNING-4 fix — no false-FAIL.
- End-to-end smoke shows verify:tail-sampling GREEN + Grafana dashboard live data + Tempo trace count matches ON capture
- Phase 11 is ship-ready; orchestrator applies `step-11-tail-sampling` annotated tag in the merge commit per WORK-01 / D-21 / Phase 2-06 / 6-06 / 10 precedent
</verification>

<success_criteria>
1. D-T11 satisfied — Phase-10-equivalent README §11 with all five sub-sections + composite caveat + mise run invocations
2. D-T9 + D-T10 satisfied — paired OFF/ON screenshots in HTML <table> render the trace-count delta
3. D-T12 satisfied — F2-3 double-filter-trap blockquote at end of §11 with escape hatches and Phase 16 forward-link
4. RESEARCH §3.2.1 insurance — histogram suffix verified live; dashboard corrected if needed
5. WARNING-2 fix — three priority-chain artifacts (YAML verbatim / README §11 paraphrase / F2-3 callout) all reference the four-tier `drop > inverted_not_sample > sample > inverted_sample` reality; no artifact reduces the chain to a 3-tier mental model
6. WARNING-4 fix — Task 1 histogram-suffix gate degrades gracefully when invoked outside the operator-driven E2E flow (logs SKIP, exits 0) instead of reporting a false-FAIL
7. Phase 11 SC #1 (TSAMP-01 in YAML), SC #2 (TSAMP-02 OR-semantics comment), SC #3 (TSAMP-03 ON/OFF screenshot pair + ~30% rate observation) — all three Phase 11 ROADMAP success criteria met after this plan ships
</success_criteria>

<output>
After completion, create `.planning/phases/11-tail-sampling-at-the-collector/11-06-SUMMARY.md`
</output>
