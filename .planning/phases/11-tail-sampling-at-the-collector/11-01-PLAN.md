---
phase: 11-tail-sampling-at-the-collector
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/screenshots/step-11-tail-sampling-OFF.png
autonomous: false
requirements: [TSAMP-03]
must_haves:
  truths:
    - "A docs/screenshots/step-11-tail-sampling-OFF.png file exists in the repo"
    - "The PNG shows a Tempo Search panel with Service Name = order-producer, time range Last 5 min, and a visible trace count from the pre-Phase-11 (no tail_sampling) state"
    - "The screenshot is captured BEFORE any of plans 11-02..11-06 land on main (sequencing constraint per CONTEXT.md <specifics> bullet 4)"
  artifacts:
    - path: "docs/screenshots/step-11-tail-sampling-OFF.png"
      provides: "Pre-Phase-11 baseline trace count visualization for the README §11 paired-screenshot table"
  key_links:
    - from: "docs/screenshots/step-11-tail-sampling-OFF.png"
      to: "README.md (will be added in plan 11-06)"
      via: "HTML <table> paired-screenshot block"
      pattern: "step-11-tail-sampling-OFF.png"
---

<objective>
Capture the OFF (pre-Phase-11) Tempo Search trace-count screenshot and commit it to the repo BEFORE any other Phase 11 plan modifies infra/observability/otelcol-config.yaml. This is the "before" state of `main` — once plan 11-02 lands the tail_sampling block, the OFF state is unreachable on main without git checkout.

Purpose: Provide the "before" half of the OFF/ON paired-screenshot teaching artifact (D-T9 / D-T10 / TSAMP-03). Mirrors Phase 10 D-13 manual-one-shot precedent.

Output: `docs/screenshots/step-11-tail-sampling-OFF.png` committed to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tail-sampling-at-the-collector/11-CONTEXT.md
@.planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator captures OFF Tempo Search screenshot</name>
  <read_first>
    - .planning/phases/11-tail-sampling-at-the-collector/11-CONTEXT.md (`<specifics>` bullet 4 — sequencing constraint; <decisions> D-T9 / D-T10 — capture mechanism)
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §7 "Screenshot mechanism (D-T9 / D-T10)" lines 936-960 — exact URL, query, and time range
    - .planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md row 8 "docs/screenshots/step-11-tail-sampling-OFF.png" — naming convention and analog screenshots
    - docs/screenshots/ directory listing (existing files: step-01-empty-tempo.png, step-02-disconnected-traces.png, step-03-joined-trace.png, step-03-waterfall.png, step-05-logs-trace-jump.png, step-06-test-output.png) — to confirm naming convention
  </read_first>
  <what-built>
    Nothing automated. This task is purely operator-driven. Claude has prepared the workspace by confirming:
    - `main` is at HEAD (no Phase 11 plans landed yet)
    - infra is up via `mise run infra:up` (docker compose ps shows otel-collector, tempo, mimir, loki, grafana healthy)
    - dev services are up via `mise run dev` (producer-service on :8080, consumer-service on :8081)
    - load is running via `mise run load` for at least 5 minutes (so Tempo has populated trace data)
  </what-built>
  <how-to-verify>
    Operator captures the OFF screenshot manually using these EXACT steps:

    1. Confirm prerequisites:
       - Run: `git status` — should show clean tree (no Phase 11 plans landed yet)
       - Run: `git log --oneline -1` — record this commit SHA; it is the OFF baseline
       - Run: `mise run infra:up` (if not already up)
       - Run: `mise run dev` in another terminal (if not already up)
       - Run: `mise run load` in another terminal — let it run for at least 5 minutes to populate Tempo with traces

    2. Open browser to: `http://localhost:3000/explore`

    3. In Grafana Explore:
       - Select the **Tempo** datasource (top-left datasource picker)
       - Select the **Search** tab (not "TraceQL")
       - Set **Service Name** = `order-producer`
       - Set the time range to **Last 5 minutes** (top-right time picker)
       - Click **Run query** (top-right blue button)

    4. Wait for the trace results table to populate. The trace count should be roughly ~1500 (varies with load duration; the absolute number doesn't matter — the OFF/ON ratio is what matters).

    5. Capture a screenshot of the **full Tempo Search results panel** (the panel showing the trace list AND the result count). Use whatever screenshot tool the operator prefers (gnome-screenshot, scrot, flameshot, browser DevTools "Capture screenshot", etc.). The capture should clearly show:
       - The Service Name = order-producer query at the top
       - The time range "Last 5 minutes"
       - The result count or visible long list of traces (the count is the load-bearing detail)

    6. Save the file as `docs/screenshots/step-11-tail-sampling-OFF.png` (PNG format, no compression artifacts that obscure the trace count).

    7. Verify the file exists and is a valid PNG:
       - Run: `file docs/screenshots/step-11-tail-sampling-OFF.png` — should report `PNG image data, ...`
       - Run: `ls -la docs/screenshots/step-11-tail-sampling-OFF.png` — should show non-zero file size (typically 50-500 KB)

    8. Commit the screenshot:
       - Run: `git add docs/screenshots/step-11-tail-sampling-OFF.png`
       - Run: `git commit -m "docs(11): capture step-11-tail-sampling-OFF.png baseline (D-T9, pre-Phase-11)"`
       - Run: `git log --oneline -1` — confirm the commit landed on main

    9. Confirm sequencing invariant: `git diff HEAD^ -- infra/observability/otelcol-config.yaml grafana/dashboards/ose-otel-demo.json` MUST be empty (this commit ONLY touches docs/screenshots/, NOT any Phase 11 source artifact).
  </how-to-verify>
  <resume-signal>Type "approved" when the OFF screenshot is committed to main and `git diff HEAD^ -- infra/observability/otelcol-config.yaml grafana/dashboards/ose-otel-demo.json` confirms no source artifact was touched. Then plans 11-02..11-06 may proceed.</resume-signal>
  <acceptance_criteria>
    - `test -f docs/screenshots/step-11-tail-sampling-OFF.png` exits 0
    - `file docs/screenshots/step-11-tail-sampling-OFF.png | grep -q 'PNG image data'` exits 0
    - `[ "$(stat -c%s docs/screenshots/step-11-tail-sampling-OFF.png)" -gt 10000 ]` exits 0 (PNG is at least 10KB — sanity check that it's not an empty file)
    - `git log --oneline -- docs/screenshots/step-11-tail-sampling-OFF.png | wc -l` is at least 1 (file is committed, not just present in working tree)
    - `git log -1 --name-only | grep -v "^docs/screenshots/step-11-tail-sampling-OFF.png" | grep -E "infra/observability/otelcol-config.yaml|grafana/dashboards/ose-otel-demo.json|producer-service|scripts/load.sh|mise.toml|README.md|docker-compose.yml"` returns empty (this commit did NOT touch any Phase 11 source artifact)
  </acceptance_criteria>
  <files>docs/screenshots/step-11-tail-sampling-OFF.png</files>
  <action>
    See <how-to-verify> above — this is a checkpoint:human-action task. The executor presents the <what-built> + <how-to-verify> blocks to the operator, pauses for the <resume-signal>, and after "approved" verifies the PNG exists, is committed, and the commit is isolated to docs/screenshots/. The 9 steps inside <how-to-verify> ARE the action: confirm prereqs, open Grafana Explore, query Tempo Search, capture the PNG, save to the canonical path, commit, confirm sequencing.
  </action>
  <verify>
    <automated>test -f docs/screenshots/step-11-tail-sampling-OFF.png && file docs/screenshots/step-11-tail-sampling-OFF.png | grep -q 'PNG image data' && [ "$(stat -c%s docs/screenshots/step-11-tail-sampling-OFF.png)" -gt 10000 ] && [ "$(git log --oneline -- docs/screenshots/step-11-tail-sampling-OFF.png | wc -l)" -ge 1 ]</automated>
  </verify>
  <done>OFF screenshot captured + committed to main; commit is isolated to docs/screenshots/ (no source artifact touched in the same commit); main HEAD is now at "OFF baseline + 1 commit"; plans 11-02..11-06 may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → repo | Operator commits a binary PNG; no untrusted external input crosses any application boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | I (Information disclosure) | docs/screenshots/step-11-tail-sampling-OFF.png | accept | Screenshot is from a local laptop workshop stack (localhost:3000); contains synthetic load data only (WIDGET-EXPRESS / WIDGET-STANDARD payloads from load.sh). No PII, no secrets, no production data. Public-readable workshop repo per PROJECT.md "Output medium". |
| T-11-01-02 | D (Denial of service) | Tomcat worker pool | accept | This task does not run any code; it is operator-driven screenshot capture. |
</threat_model>

<verification>
- OFF screenshot file is present, valid PNG, committed to main
- Commit is isolated to docs/screenshots/ (no source artifact touched in the same commit)
- main HEAD is now at "OFF baseline + 1 commit"; plans 11-02..11-06 can proceed
</verification>

<success_criteria>
1. `docs/screenshots/step-11-tail-sampling-OFF.png` exists, is a valid PNG ≥ 10KB, and is committed to main
2. The committing commit touched ONLY the screenshot file (no infra YAML, no Java, no script, no dashboard JSON, no README)
3. The git log records the OFF baseline so the README §11 caption (added in plan 11-06) can reference the exact commit SHA if desired
</success_criteria>

<output>
After completion, create `.planning/phases/11-tail-sampling-at-the-collector/11-01-SUMMARY.md`
</output>
