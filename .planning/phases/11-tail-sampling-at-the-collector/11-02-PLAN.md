---
phase: 11-tail-sampling-at-the-collector
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - infra/observability/otelcol-config.yaml
  - docker-compose.yml
autonomous: true
requirements: [TSAMP-01, TSAMP-02]
must_haves:
  truths:
    - "infra/observability/otelcol-config.yaml contains a tail_sampling: processor block with composite envelope and three sub-policies named keep-errors, keep-slow, probabilistic-fallback"
    - "The traces pipeline processors list reads [memory_limiter, tail_sampling, batch] (canonical OTel order, D-T1)"
    - "The misleading line-180 forecast comment about tail_sampling being inserted between batch and the exporter is replaced with the correct narrative"
    - "An explicit ~25-line comment block above composite documents the OR-semantics priority chain (TSAMP-02) and the composite envelope addendum (D-T4)"
    - "decision_wait: 10s and num_traces: 10000 are present (F2-2 mitigation)"
    - "docker-compose.yml otel-collector service command: array contains --feature-gates=processor.tailsamplingprocessor.recordpolicy with a WHY comment flagging the alpha gate (Route A per DISCUSSION-LOG.md plan-time amendment)"
    - "A `docker compose restart otel-collector` reloads the file without errors and the Collector starts cleanly"
  artifacts:
    - path: "infra/observability/otelcol-config.yaml"
      provides: "tail_sampling processor configuration with composite envelope"
      contains: "tail_sampling:"
    - path: "docker-compose.yml"
      provides: "alpha feature gate enablement for sub-policy span attribution (Route A)"
      contains: "processor.tailsamplingprocessor.recordpolicy"
  key_links:
    - from: "infra/observability/otelcol-config.yaml tail_sampling: block"
      to: "docker-compose.yml --feature-gates flag"
      via: "container startup args enable the recordpolicy gate that emits sub-policy span attributes"
      pattern: "processor.tailsamplingprocessor.recordpolicy"
    - from: "infra/observability/otelcol-config.yaml policy_order: [keep-errors, keep-slow, probabilistic-fallback]"
      to: "mise.toml verify:tail-sampling task (added in plan 11-04)"
      via: "EXPECTED_SUBS literal must match these names verbatim"
      pattern: "keep-errors|keep-slow|probabilistic-fallback"
---

<objective>
Add the `tail_sampling` processor to `infra/observability/otelcol-config.yaml` between `memory_limiter` and `batch` per D-T1 canonical pipeline order. The block uses the composite envelope (D-T2) with three sub-policies — `keep-errors` (status_code ERROR keep-100%), `keep-slow` (latency >1s keep-100%), `probabilistic-fallback` (20%) — gated by per-branch rate-limits per D-T3. The TSAMP-02 mandatory OR-semantics comment block sits above composite per D-T4. Fix the misleading line-180 forecast comment per D-T1.

Add the alpha feature gate `processor.tailsamplingprocessor.recordpolicy` to the otel-collector service `command:` array in `docker-compose.yml` per Route A (DISCUSSION-LOG.md plan-time amendment, 2026-05-03) so sub-policy attribution appears as Tempo span attributes (`tailsampling.composite_policy`).

Purpose: This is the headline Collector configuration that delivers TSAMP-01 + TSAMP-02. Once landed and the Collector restarted, every error trace AND every latency-policy-eligible trace is preserved; ~20% of remaining traces flow through.

Output: `infra/observability/otelcol-config.yaml` extended with the tail_sampling block and pipeline reference; `docker-compose.yml` otel-collector command extended with the alpha feature gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tail-sampling-at-the-collector/11-CONTEXT.md
@.planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md
@.planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md
@.planning/phases/11-tail-sampling-at-the-collector/11-DISCUSSION-LOG.md
@infra/observability/otelcol-config.yaml
@docker-compose.yml

<interfaces>
<!-- Existing anchors in infra/observability/otelcol-config.yaml -->
<!-- Line 96:  processors: -->
<!-- Lines 97-105:  memory_limiter: block -->
<!-- Lines 107-112: batch: block -->
<!-- Lines 178-184: traces: pipeline (line 183 is `processors: [memory_limiter, batch]`) -->
<!-- Lines 179-180: misleading forecast comment to be REPLACED per D-T1 -->

<!-- Existing anchors in docker-compose.yml -->
<!-- Line 105: otel-collector: -->
<!-- Line 107: image: otel/opentelemetry-collector-contrib:0.151.0 -->
<!-- Line 109: command: ["--config=/etc/otelcol-contrib/config.yaml"]   ← extends to two-element array -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert tail_sampling processor block + fix line-180 forecast comment in otelcol-config.yaml</name>
  <files>infra/observability/otelcol-config.yaml</files>
  <read_first>
    - infra/observability/otelcol-config.yaml — current state (lines 96-112 processors block; lines 178-184 traces pipeline; lines 179-180 forecast comment to be replaced)
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §1 lines 130-157 — verbatim replacement text for the line-180 forecast comment
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §2.2 lines 185-311 — verbatim YAML block (composite + sub-policies + rate_allocation + ~25-line comment block)
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §2.4 lines 389-408 — verbatim OR-semantics priority chain quote (already embedded in §2.2 YAML; reference for fidelity)
    - .planning/phases/11-tail-sampling-at-the-collector/11-CONTEXT.md `<decisions>` D-T1, D-T2, D-T3, D-T4, D-T15
    - .planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md row 1 — analog (existing memory_limiter / batch processor blocks at lines 96-112; pipeline at lines 178-184)
    - .planning/phases/11-tail-sampling-at-the-collector/11-DISCUSSION-LOG.md lines 235-256 — Route A confirmation (composite envelope locked)
  </read_first>
  <action>
**Step A — Replace the misleading forecast comment.** The current file (lines 179-180) contains:

```yaml
    # WHY: traces — apps emit OTLP → memory_limiter → batch → Tempo via OTLP HTTP.
    # Phase 11 inserts tail_sampling between batch and the exporter.
```

Replace these two lines with the verbatim block from RESEARCH §1 lines 147-155:

```yaml
    # WHY: traces — apps emit OTLP → memory_limiter → tail_sampling → batch → Tempo
    # via OTLP HTTP. tail_sampling is placed BEFORE batch (canonical OTel order:
    # see https://github.com/open-telemetry/opentelemetry-collector-contrib/blob/v0.151.0/processor/tailsamplingprocessor/README.md
    # — "This processor … reassembles spans into new batches, causing them to lose
    # their original context"). Running batch AFTER tail_sampling means the batch
    # processor only ever sees KEPT traces (~30% of incoming volume at workshop
    # load), so batches are smaller and arrive at Tempo more efficiently.
```

**Step B — Update the traces pipeline processors list.** Find line 183 (`      processors: [memory_limiter, batch]` in the `traces:` pipeline block — NOT the metrics or logs pipelines, both of which also contain `processors: [memory_limiter, batch]` and MUST remain unchanged). Use the surrounding context (`traces:` is at line 181 per current file; verify by reading the immediate 5-line window before the edit) to disambiguate. Change ONLY the `traces:` pipeline's processors line to:

```yaml
      processors: [memory_limiter, tail_sampling, batch]
```

The `metrics:` pipeline (line 194) and `logs:` pipeline (line 201) keep their `[memory_limiter, batch]` lists unchanged — tail_sampling is a traces-only processor.

**Step C — Insert the tail_sampling: block inside the existing processors: section.** The processors block opens at line 96. Insert the new block AFTER the `batch:` block ends (after line 112, before the next ASCII-bar comment header at line 114) so the in-file ORDERING is `memory_limiter → batch → tail_sampling`. (The pipeline ORDERING in the traces: list is what matters for execution order; the file-level ORDER of processor definitions is conventionally arbitrary in OTel Collector configs. Placing tail_sampling AFTER batch in the file body keeps the existing memory_limiter/batch declarations untouched.)

Paste this verbatim from RESEARCH §2.2 (preserve indentation: each YAML key uses 2-space indent matching the existing memory_limiter/batch blocks in this file):

```yaml
  # ──────────────────────────────────────────────────────────────────
  # tail_sampling — Phase 11 / TSAMP-01..03 (composite envelope, D-T2)
  # ──────────────────────────────────────────────────────────────────
  #
  # WHY this processor exists: the SDK exports 100% of spans at
  # parentBased(alwaysOn()) (TSAMP invariant — head sampling lives in
  # Phase 16, never here). The Collector buffers each trace for
  # decision_wait, then DECIDES which complete traces reach Tempo based
  # on the assembled trace's full content (status_code, total latency,
  # probabilistic roll). This is the headline differentiator from head
  # sampling: the decision uses information that does not exist at span
  # creation time.
  #
  # WHY composite envelope (D-T2): wraps the three TSAMP-01 policies in
  # a single outer block that adds two production-realistic teaching
  # surfaces on top of the OR-semantics lesson:
  #   1. Per-branch RATE-LIMITS — each sub-policy has its own
  #      max-spans-per-second cap; bursts visibly clamp.
  #   2. policy_order — sub-policies are evaluated in the listed order;
  #      composite returns SAMPLED on the FIRST sub-policy that votes
  #      sample within its rate cap (so a trace that is BOTH error AND
  #      slow is sampled exactly ONCE — matched by keep-errors first,
  #      keep-slow never gets a vote — see D-T8).
  #
  # WHY the four-tier OR-semantics priority chain (TSAMP-02, F2-1):
  # the upstream tail_sampling processor evaluates ALL top-level
  # policies and applies a priority decision logic — NOT first-match.
  # Verbatim from the v0.151.0 README "Policy Decision Flow" section:
  #
  #   "When there's a 'drop' decision, the trace is not sampled;
  #    When there's an 'inverted not sample' decision, the trace is
  #      not sampled (deprecated);
  #    When there's a 'sample' decision, the trace is sampled;
  #    When there's an 'inverted sample' decision and no 'not sample'
  #      decisions, the trace is sampled (deprecated);
  #    In all other cases, the trace is NOT sampled."
  #
  # NOTE the composite-specific addendum from the same README section:
  # "if the policy is within an `and` or `composite` policy, the
  # resulting decision will be either sampled or not sampled" — i.e.,
  # inverted decisions collapse inside composite/and. With our config,
  # the priority chain reduces to "ANY top-level sample wins" because
  # we use no `drop` policies (drop-policy for /actuator/health is
  # deferred per CONTEXT.md).
  #
  # decision_wait + num_traces sizing (F2-2 mitigation): at workshop
  # load (~200 rps steady, ~10s window = ~2000 trace-fragments in flight),
  # 10000 buffer slots give 5x headroom. decision_wait=10s vs default
  # 30s is the workshop-responsiveness tradeoff (attendees see traces
  # within ~10s of POSTing).
  tail_sampling:
    decision_wait: 10s
    num_traces: 10000
    expected_new_traces_per_sec: 50
    policies:
      - name: composite-policy
        type: composite
        composite:
          # WHY 1000 spans/s ceiling: at workshop load (~200 rps × ~10
          # spans per trace = ~2000 spans/s if everything were sampled),
          # 1000 is half — enough to clamp visibly under burst, generous
          # enough that steady load never hits the global ceiling.
          # NOTE the unit is SPANS per second, NOT traces per second.
          max_total_spans_per_second: 1000
          # WHY this order: composite evaluates sub-policies in
          # policy_order and STOPS at the first vote-to-sample (within
          # rate cap). Errors are the highest-priority signal — they
          # must always survive. Slow second — never drop a slow trace
          # under workshop conditions. Probabilistic last — the
          # "representative sample of normal traffic" backstop.
          policy_order: [keep-errors, keep-slow, probabilistic-fallback]
          composite_sub_policy:
            # WHY keep-errors: production rule #1 — never drop a trace
            # that contains an error span. v1.0's TRACE-09 sets
            # span.setStatus(StatusCode.ERROR) on the deterministic
            # 10% failure path (every 10th order at consumer side).
            - name: keep-errors
              type: status_code
              status_code:
                status_codes: [ERROR]
            # WHY keep-slow: production rule #2 — never drop a trace
            # whose total duration exceeds the SLO threshold. Phase 11
            # adds a WIDGET-SLOW SKU branch in OrderService.place()
            # that Thread.sleep(1500)s inside the producer INTERNAL
            # span — the trace's MAX-span duration always > 1s.
            - name: keep-slow
              type: latency
              latency:
                threshold_ms: 1000
            # WHY probabilistic-fallback: 20% of remaining traces
            # (matches TSAMP-03 "approximately 20%" target). Tempo
            # never goes empty during workshop demos.
            - name: probabilistic-fallback
              type: probabilistic
              probabilistic:
                sampling_percentage: 20
          # WHY rate_allocation: each sub-policy's vote is GATED by
          # its own spans-per-second budget BEFORE composite returns.
          # If a sub-policy's budget is exhausted in the current 1s
          # window, composite treats its vote as not-sample and
          # continues to the next sub-policy in policy_order.
          #   - keep-errors: 100% of max_total_spans_per_second = 1000
          #     spans/s. At ~10 spans per error trace, that's ~100
          #     error traces/s — well above the workshop's actual ~20
          #     errors/s. EFFECTIVELY UNLIMITED for workshop load.
          #   - keep-slow: 20 spans/s = ~2 traces/s at ~10 spans each
          #     (matches SLOW_RPS=2 default). Slow traces beyond the
          #     cap fall through to probabilistic.
          #   - probabilistic-fallback: 40 spans/s = ~4 traces/s. ~20%
          #     of the remaining ~180 rps non-error non-slow load.
          rate_allocation:
            - policy: keep-errors
              percent: 100
            - policy: keep-slow
              percent: 2
            - policy: probabilistic-fallback
              percent: 4
```

**Step D — Validate compose can parse the file.** Run `docker compose config --quiet` (no service argument; validates the entire compose file plus mounted configs). If it errors, the YAML has a syntax problem — fix and re-run.

**Step E — Restart the Collector to pick up the new config.** `docker compose restart otel-collector` then `docker compose logs --tail=50 otel-collector` — confirm no `error` lines mention `tail_sampling` and the line `Everything is ready. Begin running and processing data.` (or equivalent collector-contrib v0.151.0 startup-complete log line) appears. The recordpolicy feature gate is added in Task 2 — until then, the Collector will load tail_sampling but only emit `policy=composite-policy` as the metric label (per RESEARCH §2.3); that's expected at this point.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "^  tail_sampling:" infra/observability/otelcol-config.yaml && grep -q "decision_wait: 10s" infra/observability/otelcol-config.yaml && grep -q "num_traces: 10000" infra/observability/otelcol-config.yaml && grep -q "type: composite" infra/observability/otelcol-config.yaml && grep -q "max_total_spans_per_second: 1000" infra/observability/otelcol-config.yaml && grep -q "policy_order: \[keep-errors, keep-slow, probabilistic-fallback\]" infra/observability/otelcol-config.yaml && grep -q "name: keep-errors" infra/observability/otelcol-config.yaml && grep -q "name: keep-slow" infra/observability/otelcol-config.yaml && grep -q "name: probabilistic-fallback" infra/observability/otelcol-config.yaml && grep -q "status_codes: \[ERROR\]" infra/observability/otelcol-config.yaml && grep -q "threshold_ms: 1000" infra/observability/otelcol-config.yaml && grep -q "sampling_percentage: 20" infra/observability/otelcol-config.yaml && grep -q "processors: \[memory_limiter, tail_sampling, batch\]" infra/observability/otelcol-config.yaml && ! grep -q "Phase 11 inserts tail_sampling between batch and the exporter" infra/observability/otelcol-config.yaml && grep -q "memory_limiter → tail_sampling → batch" infra/observability/otelcol-config.yaml && docker compose config --quiet && echo "task1 ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^  tail_sampling:" infra/observability/otelcol-config.yaml` exits 0 (block exists at the correct 2-space indentation under `processors:`)
    - `grep -q "decision_wait: 10s" infra/observability/otelcol-config.yaml` exits 0 (TSAMP-01 / F2-2)
    - `grep -q "num_traces: 10000" infra/observability/otelcol-config.yaml` exits 0 (TSAMP-01 / F2-2)
    - `grep -q "type: composite" infra/observability/otelcol-config.yaml` exits 0 (D-T2 composite envelope)
    - `grep -q "max_total_spans_per_second: 1000" infra/observability/otelcol-config.yaml` exits 0 (D-T3 global cap)
    - `grep -q "policy_order: \[keep-errors, keep-slow, probabilistic-fallback\]" infra/observability/otelcol-config.yaml` exits 0 (D-T15 policy names + D-T2 ordering)
    - `grep -q "name: keep-errors" infra/observability/otelcol-config.yaml` exits 0
    - `grep -q "name: keep-slow" infra/observability/otelcol-config.yaml` exits 0
    - `grep -q "name: probabilistic-fallback" infra/observability/otelcol-config.yaml` exits 0
    - `grep -q "status_codes: \[ERROR\]" infra/observability/otelcol-config.yaml` exits 0 (TSAMP-01 status_code policy keep-100%)
    - `grep -q "threshold_ms: 1000" infra/observability/otelcol-config.yaml` exits 0 (TSAMP-01 latency keep-100% above 1s)
    - `grep -q "sampling_percentage: 20" infra/observability/otelcol-config.yaml` exits 0 (TSAMP-01 probabilistic 20% fallback)
    - `grep -q "processors: \[memory_limiter, tail_sampling, batch\]" infra/observability/otelcol-config.yaml` exits 0 (D-T1 traces pipeline order)
    - `! grep -q "Phase 11 inserts tail_sampling between batch and the exporter" infra/observability/otelcol-config.yaml` exits 0 (the wrong forecast comment is gone)
    - `grep -q "memory_limiter → tail_sampling → batch" infra/observability/otelcol-config.yaml` exits 0 (corrected D-T1 narrative is present)
    - `grep -q "Policy Decision Flow" infra/observability/otelcol-config.yaml` exits 0 (TSAMP-02 OR-semantics quote present in comment block)
    - `grep -q "drop" infra/observability/otelcol-config.yaml && grep -q "sample" infra/observability/otelcol-config.yaml` exits 0 (priority-chain wording in TSAMP-02 comment block)
    - `grep -q "rate_allocation:" infra/observability/otelcol-config.yaml` exits 0 (D-T3 rate-limit block present)
    - `grep -A4 "rate_allocation:" infra/observability/otelcol-config.yaml | grep -q "percent: 100"` exits 0 (keep-errors=100% per D-T3)
    - `grep -A8 "rate_allocation:" infra/observability/otelcol-config.yaml | grep -q "percent: 2"` exits 0 (keep-slow=2% per D-T3)
    - `grep -A8 "rate_allocation:" infra/observability/otelcol-config.yaml | grep -q "percent: 4"` exits 0 (probabilistic-fallback=4% per D-T3)
    - `grep -c "processors: \[memory_limiter, batch\]" infra/observability/otelcol-config.yaml` outputs `2` (metrics + logs pipelines unchanged; only the traces pipeline got tail_sampling inserted)
    - `docker compose config --quiet` exits 0 (compose validates the file with the new YAML mounted)
  </acceptance_criteria>
  <done>The tail_sampling processor is defined with composite envelope + 3 sub-policies + per-branch rate-limits, correctly inserted in the traces pipeline (and ONLY the traces pipeline) between memory_limiter and batch, the line-180 forecast comment is replaced with the corrected narrative, the TSAMP-02 OR-semantics priority chain quote is embedded in the ~25-line comment block above composite, and `docker compose config --quiet` validates the file. The Collector restart succeeds with no tail_sampling errors in the logs.</done>
</task>

<task type="auto">
  <name>Task 2: Add --feature-gates=processor.tailsamplingprocessor.recordpolicy to docker-compose.yml otel-collector command (Route A)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml — current state (line 105 otel-collector service; line 109 single-element command array)
    - .planning/phases/11-tail-sampling-at-the-collector/11-DISCUSSION-LOG.md lines 235-256 — Route A confirmation; this file edit is a Route A consequence not in CONTEXT.md
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md §2.3 lines 313-388 — composite envelope collapses metric label; recordpolicy gate is the Route A fix
    - .planning/phases/11-tail-sampling-at-the-collector/11-RESEARCH.md "Example 2" lines 1028-1042 — feature gate enablement example
    - .planning/phases/11-tail-sampling-at-the-collector/11-PATTERNS.md row 6 — analog (existing single-element command: arrays at lines 109/155/187/210; Phase 11 extends otel-collector to 2 elements with WHY block)
  </read_first>
  <action>
**Edit `docker-compose.yml` line 109** to extend the `otel-collector` service `command:` array from one element to two elements with a WHY comment between them.

Find this line (currently line 109):

```yaml
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
```

Replace it with this multi-line array (preserve the 4-space leading indent that puts the field under the `otel-collector:` service):

```yaml
    command: [
      "--config=/etc/otelcol-contrib/config.yaml",
      # WHY (Phase 11 / DISCUSSION-LOG Route A): alpha feature gate that enables
      # sub-policy attribution on sampled spans via the `tailsampling.composite_policy`
      # span attribute. Without this gate, composite envelope collapses sub-policy
      # names at the metric layer (only "composite-policy" appears as policy= label).
      # Gate is alpha at v0.151.0 — could rename or default-on in v0.152+; revisit
      # at the next collector-contrib bump. See `mise run verify:tail-sampling`
      # (Tier 2) for the runtime contract this gate satisfies.
      "--feature-gates=processor.tailsamplingprocessor.recordpolicy"
    ]
```

**Validate**: run `docker compose config --quiet` to confirm the YAML parses and the multi-line array is well-formed. Then `docker compose up -d otel-collector` (recreate the container so the new command line is applied — `restart` does NOT pick up command/args changes; `up -d` re-creates with new args).

**Verify the gate is active** by reading the container logs:
- `docker compose logs --tail=100 otel-collector | grep -i "feature.gate\|recordpolicy"` — the collector logs at startup which feature gates are enabled. The line should look like `Feature gate processor.tailsamplingprocessor.recordpolicy is enabled` (or similar — exact wording is collector-contrib v0.151.0-defined). If the gate name is rejected (collector exits with "unknown feature gate"), the gate may have been renamed in this version — check `docker run --rm otel/opentelemetry-collector-contrib:0.151.0 --feature-gates=help` to confirm.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "processor.tailsamplingprocessor.recordpolicy" docker-compose.yml && grep -q "DISCUSSION-LOG Route A" docker-compose.yml && grep -q "alpha feature gate" docker-compose.yml && grep -q "tailsampling.composite_policy" docker-compose.yml && docker compose config --quiet && docker compose up -d otel-collector && sleep 5 && docker compose ps otel-collector | grep -q "running\|healthy" && echo "task2 ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "processor.tailsamplingprocessor.recordpolicy" docker-compose.yml` exits 0 (gate flag present)
    - `grep -q '"--feature-gates=processor.tailsamplingprocessor.recordpolicy"' docker-compose.yml` exits 0 (correct flag syntax with `=` separator)
    - `grep -q '"--config=/etc/otelcol-contrib/config.yaml"' docker-compose.yml` exits 0 (existing config flag preserved)
    - `grep -q "DISCUSSION-LOG Route A" docker-compose.yml` exits 0 (lineage comment present per PATTERNS.md row 6)
    - `grep -q "alpha feature gate" docker-compose.yml` exits 0 (alpha-fragility caveat present)
    - `grep -q "tailsampling.composite_policy" docker-compose.yml` exits 0 (cross-reference to the span attribute the gate enables)
    - `grep -q "verify:tail-sampling" docker-compose.yml` exits 0 (cross-link to the runtime gate the comment promises)
    - `docker compose config --quiet` exits 0 (multi-line array parses)
    - `docker compose ps otel-collector` shows the container in `running` state (gate did not crash the collector at startup — i.e., the gate name is still valid at v0.151.0)
    - `docker compose logs --tail=200 otel-collector 2>&1 | grep -E "error|Error" | grep -i "feature.gate\|tail.sampling"` returns empty (no errors related to the new gate or processor)
  </acceptance_criteria>
  <done>The otel-collector service in docker-compose.yml uses a two-element command array with the alpha recordpolicy feature gate enabled, the WHY comment block flags the gate's alpha-fragility and cross-links to verify:tail-sampling Tier 2, the compose file validates, and the recreated container starts successfully without feature-gate-related errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apps → Collector OTLP receiver | Trusted (loopback localhost only) — workshop runs on a single laptop; no cross-host traffic |
| Collector → Tempo / Mimir / Loki backends | Trusted (compose-network internal only) |
| Operator → Collector :8888 self-metrics | Read-only Prometheus exposition endpoint (no auth required for workshop scope per STACK-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | T (Tampering) | tail_sampling processor configuration | accept | Workshop runs on a single laptop; YAML is part of the repo and reviewed via PR. No untrusted operator can modify infra/observability/otelcol-config.yaml at runtime. |
| T-11-02-02 | D (Denial of service via memory exhaustion) | otel-collector container | mitigate | `memory_limiter` processor sits BEFORE `tail_sampling` in the traces pipeline (verified by acceptance criterion `processors: [memory_limiter, tail_sampling, batch]`). At workshop load (~200 rps × 10s decision_wait × ~10 spans × ~1KB ≈ 20MB sustained tail_sampling buffer), well below the Collector's default 512MB heap. The `num_traces: 10000` cap also bounds the buffer. |
| T-11-02-03 | I (Information disclosure via misconfiguration) | feature-gates flag | accept | The `processor.tailsamplingprocessor.recordpolicy` gate enables span attributes that name the policies (`keep-errors`, `keep-slow`, `probabilistic-fallback`). These are operational metadata, not secrets. The WHY comment in docker-compose.yml flags the alpha-gate fragility surface (gate could rename in v0.152+). |
| T-11-02-04 | E (Elevation of privilege via container args) | docker-compose.yml command array | accept | The new `--feature-gates=...` arg is hard-coded in the repo — operators cannot pass arbitrary args at deploy time. Workshop scope per CLAUDE.md / PROJECT.md. |
| T-11-02-05 | R (Repudiation: workshop attendee copies tail_sampling YAML to production without sizing for prod scale) | future production deployment | accept | Out of scope for this phase. Mitigated by the WHY comments in the YAML (`# decision_wait + num_traces sizing` block explicitly says workshop responsiveness vs production). README §11 (added in plan 11-06) will repeat the production-vs-workshop caveat. |
</threat_model>

<verification>
- otelcol-config.yaml has tail_sampling block with composite + 3 sub-policies + rate_allocation, traces pipeline references it, line-180 fixed
- docker-compose.yml has the recordpolicy feature gate enabled
- Both files validate via `docker compose config --quiet`
- otel-collector container restarts cleanly (no errors related to new processor or gate)
- After ~30s of running load, `curl -s http://localhost:8888/metrics | grep otelcol_processor_tail_sampling_count_traces_sampled_total` returns at least one matching line — proves the processor is loaded and emitting decisions (this is the Tier 1 check the verify:tail-sampling task will automate in plan 11-04)
</verification>

<success_criteria>
1. TSAMP-01 satisfied — three policies present in correct names + thresholds + decision_wait/num_traces values
2. TSAMP-02 satisfied — explicit comment block above composite documents OR-semantics priority chain
3. D-T1 satisfied — pipeline placement is `[memory_limiter, tail_sampling, batch]` and the misleading line-180 forecast comment is fixed
4. Route A satisfied — alpha feature gate is enabled in docker-compose.yml; collector starts cleanly
5. The metrics + logs pipelines retain `[memory_limiter, batch]` (tail_sampling is traces-only)
</success_criteria>

<output>
After completion, create `.planning/phases/11-tail-sampling-at-the-collector/11-02-SUMMARY.md`
</output>
