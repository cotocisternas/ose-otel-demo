---
phase: 11-tail-sampling-at-the-collector
verified: 2026-05-03T08:00:00Z
status: human_needed
score: 7/8 must-haves verified
overrides_applied: 0
deferred:
  - truth: "docs/screenshots/step-11-tail-sampling-OFF.png exists, is a valid PNG ≥10KB, and is committed to main"
    addressed_in: "Phase 18"
    evidence: "ROADMAP.md Phase 11 success criterion 3 explicitly states: 'both PNGs generated by Phase 18 Playwright script'; Phase 18 success criterion 1 lists step-11-tail-sampling-OFF.png and step-11-tail-sampling-ON.png as outputs"
  - truth: "docs/screenshots/step-11-tail-sampling-ON.png exists, is a valid PNG ≥10KB, captured post-Phase-11 with tail sampling active"
    addressed_in: "Phase 18"
    evidence: "ROADMAP.md Phase 11 success criterion 3 and 11-06-PLAN.md explicitly state screenshot captures deferred to Phase 18 Playwright script; 11-06-SUMMARY.md acknowledges both screenshots as known pending debt"
human_verification:
  - test: "Run full E2E smoke: mise run infra:up && mise run dev && mise run load SLOW_RPS=2 (wait 60s) && mise run verify:tail-sampling"
    expected: "Exit 0 with message: verify:tail-sampling: GREEN — composite + sub-policy contracts intact."
    why_human: "Requires live Docker stack (otel-collector, Tempo, Mimir) plus running producer/consumer services and load generator; cannot simulate in a static analysis pass"
  - test: "Open Grafana at http://localhost:3000, navigate to ose-otel-demo dashboard, expand the Tail Sampling diagnostics (Phase 11) row after load has run ~60s"
    expected: "Panel 1 shows non-zero composite-policy/sampled series; Panel 4 shows p50/p95/p99 latency values in single-digit ms; Panel 5 shows in-memory trace count oscillating around 2000"
    why_human: "Live Grafana dashboard data cannot be verified statically; requires visual inspection of rendered panels with live Mimir data"
---

# Phase 11: Tail Sampling at the Collector — Verification Report

**Phase Goal:** Add composite tail_sampling processor to OTel Collector (keep-errors + keep-slow + probabilistic-fallback policies), WIDGET-SLOW SKU branch for latency triggers, verify:tail-sampling mise task, Grafana diagnostics dashboard row, README §11 workshop narrative, and paired OFF/ON screenshots.
**Verified:** 2026-05-03T08:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Collector traces pipeline contains tail_sampling processor with composite envelope, three named sub-policies (keep-errors / keep-slow / probabilistic-fallback), decision_wait: 10s, num_traces: 10000 | ✓ VERIFIED | `infra/observability/otelcol-config.yaml`: grep confirms all required keys; pipeline order `[memory_limiter, tail_sampling, batch]` present; `processors: [memory_limiter, batch]` appears only 2x (metrics + logs unchanged) |
| 2 | Collector config contains explicit comment block documenting OR-semantics priority chain (TSAMP-02) | ✓ VERIFIED | `infra/observability/otelcol-config.yaml`: grep confirms "Policy Decision Flow" verbatim quote present; "drop" + "inverted_not_sample" + "sample" + "inverted_sample" present in comment block |
| 3 | WIDGET-SLOW SKU branch in OrderService.place() sleeps 1500ms inside the existing INTERNAL span scope | ✓ VERIFIED | `producer-service/src/main/java/com/example/producer/domain/OrderService.java`: grep confirms `"WIDGET-SLOW".equals(payload.get("sku"))`, `Thread.sleep(1500L)`, `InterruptedException` handling with interrupt flag restore, branch inserted between UUID generation and publisher.publish |
| 4 | scripts/load.sh exposes SLOW_RPS (default 2) driving a fourth oha stream with sku=WIDGET-SLOW | ✓ VERIFIED | `scripts/load.sh`: `SLOW_RPS="${SLOW_RPS:-2}"` present; `PID_SLOW=$!` at line 170; PID_SLOW in cleanup trap and heartbeat printf; `bash -n` exits 0 |
| 5 | mise run verify:tail-sampling task exists with two-tier check (Collector :8888/metrics + Tempo :3200/api/search) | ✓ VERIFIED | `mise.toml`: `[tasks."verify:tail-sampling"]` block present after verify:images; EXPECTED_OUTER='composite-policy', EXPECTED_SUBS='keep-errors keep-slow probabilistic-fallback', 6×5s retry, GREEN success message; `mise tasks ls` parses it cleanly |
| 6 | Grafana dashboard has collapsed Tail Sampling diagnostics (Phase 11) row with 5 panels using canonical metric names | ✓ VERIFIED | `grafana/dashboards/ose-otel-demo.json`: JSON parses clean; row id=9 at y=21; panels 10-14 present; all 5 metric names confirmed; POLICY-NAMES CONTRACT D-T16 comment in row description; all panel IDs 1-14 unique; zero edits to existing panels 1-8 |
| 7 | README.md has ## Step 11 section (≥80 lines) with five sub-sections, OR-semantics four-tier chain, F2-3 double-filter callout, WIDGET-SLOW lesson, paired screenshot table, mise run invocations | ✓ VERIFIED | `README.md`: Step 11 at line 550 between Step 10 (line 457) and Concepts & FAQ (line 632); 81 lines; four-tier chain appears 4 times; F2-3 callout and OTEL_TRACES_SAMPLER escape hatch present; HTML table references both OFF and ON screenshot paths |
| 8 | docs/screenshots/step-11-tail-sampling-OFF.png and step-11-tail-sampling-ON.png exist as valid committed PNGs | DEFERRED | Neither file exists in working tree or git history; ROADMAP.md Phase 11 SC-3 explicitly states both PNGs are "generated by Phase 18 Playwright script"; 11-06-SUMMARY.md documents this as known deferred debt |

**Score:** 7/8 truths verified (1 deferred to Phase 18, 0 failed)

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | docs/screenshots/step-11-tail-sampling-OFF.png committed, valid PNG ≥10KB | Phase 18 | ROADMAP.md Phase 11 SC-3: "both PNGs generated by Phase 18 Playwright script"; Phase 18 SC-1 lists both filenames as outputs of `mise run screenshots` |
| 2 | docs/screenshots/step-11-tail-sampling-ON.png committed, valid PNG ≥10KB | Phase 18 | ROADMAP.md Phase 11 SC-3; 11-06-PLAN.md plans section: "screenshot captures (D-T9/D-T10) deferred to Phase 18 Playwright script"; 11-06-SUMMARY.md Known Stubs section |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `infra/observability/otelcol-config.yaml` | tail_sampling composite processor block | ✓ VERIFIED | All required keys present: composite envelope, 3 sub-policies, rate_allocation, OR-semantics comment, pipeline order |
| `docker-compose.yml` | alpha feature gate --feature-gates=processor.tailsamplingprocessor.recordpolicy | ✓ VERIFIED | Present with WHY comment, alpha caveat, and DISCUSSION-LOG Route A reference |
| `producer-service/src/main/java/com/example/producer/domain/OrderService.java` | WIDGET-SLOW SKU branch | ✓ VERIFIED | Branch with Thread.sleep(1500L), correct InterruptedException handling, class JavaDoc with SLOW_RPS caveat |
| `scripts/load.sh` | SLOW_RPS fourth oha stream | ✓ VERIFIED | SLOW_RPS env var, oha stream, PID_SLOW in cleanup and heartbeat, bash syntax clean |
| `mise.toml` | [tasks."verify:tail-sampling"] block | ✓ VERIFIED | Two-tier check present, EXPECTED_OUTER/EXPECTED_SUBS literals match YAML, mise lists task |
| `grafana/dashboards/ose-otel-demo.json` | Tail Sampling diagnostics collapsed row | ✓ VERIFIED | Row id=9, panels 10-14, all 5 canonical metric names, valid JSON, additive only |
| `README.md` | ## Step 11 section | ✓ VERIFIED | 81 lines, five sub-sections, four-tier chain, F2-3 callout, screenshot table |
| `docs/screenshots/step-11-tail-sampling-OFF.png` | Pre-Phase-11 Tempo trace count PNG | DEFERRED → Phase 18 | Not present; deferred per ROADMAP.md Phase 11 SC-3 |
| `docs/screenshots/step-11-tail-sampling-ON.png` | Post-Phase-11 Tempo trace count PNG | DEFERRED → Phase 18 | Not present; deferred per ROADMAP.md Phase 11 SC-3 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `otelcol-config.yaml` tail_sampling block | `docker-compose.yml` feature gate | Container startup args enable the recordpolicy gate | ✓ WIRED | Both files confirmed present; Collector restart smoke documented in 11-02-SUMMARY.md |
| `otelcol-config.yaml` policy_order names | `mise.toml` EXPECTED_SUBS literal | Policy-names contract — three artifacts must agree verbatim | ✓ WIRED | `policy_order: [keep-errors, keep-slow, probabilistic-fallback]` in YAML matches `EXPECTED_SUBS='keep-errors keep-slow probabilistic-fallback'` in mise.toml |
| `mise.toml` verify:tail-sampling Tier 2 | `docker-compose.yml` recordpolicy gate | Alpha gate must be enabled or Tier 2 fails | ✓ WIRED | Gate present in docker-compose.yml; cross-reference comment in mise.toml |
| `scripts/load.sh` SLOW_RPS oha stream (WIDGET-SLOW) | `OrderService.place()` WIDGET-SLOW branch Thread.sleep | JSON payload sku field | ✓ WIRED | load.sh sends `"sku":"WIDGET-SLOW"` via oha POST; OrderService checks `"WIDGET-SLOW".equals(payload.get("sku"))` |
| `README.md` §11 paired screenshot table | `docs/screenshots/step-11-tail-sampling-OFF.png` + `step-11-tail-sampling-ON.png` | HTML img src tags | DEFERRED | Table references both files; PNGs deferred to Phase 18 — broken images in README until Phase 18 ships |
| `grafana/dashboards/ose-otel-demo.json` PromQL queries | Mimir read path via prometheus datasource | `datasource.uid: "prometheus"` | ✓ WIRED | All 5 new panels use `"uid": "prometheus"` (≥23 references confirmed); Phase 10 D-01 contract preserved |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `grafana/dashboards/ose-otel-demo.json` Panel 1 | `count_traces_sampled_total` rate | Mimir ← Collector :8888/metrics ← tail_sampling processor decisions | Live data flows when stack is running | ✓ FLOWING (runtime-dependent) |
| `grafana/dashboards/ose-otel-demo.json` Panel 4 | `sampling_decision_timer_latency_milliseconds_bucket` | Histogram confirmed present at :8888/metrics by histogram-suffix verification (Case 1 in commit 4ab84ef) | _milliseconds suffix verified present | ✓ FLOWING |
| `mise.toml` verify:tail-sampling | Collector :8888/metrics + Tempo :3200/api/search | Live curl responses | Real-time data; graceful SKIP when stack offline | ✓ FLOWING (runtime-dependent) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| otelcol-config.yaml parses correctly | `docker compose config --quiet` | Not tested (no live stack) | ? SKIP — requires live Docker stack |
| OrderService.java compiles | `mvn -B -pl producer-service -am compile -q` | Confirmed in 11-03-SUMMARY.md self-check | ? SKIP — not run in this static pass |
| mise task lists verify:tail-sampling | `mise tasks ls \| grep verify:tail-sampling` | `verify:tail-sampling  Phase 11 invariant: ...` | ✓ PASS |
| load.sh syntax valid | `bash -n scripts/load.sh` | Exit 0 | ✓ PASS |
| Dashboard JSON parses | `python3 -c 'import json; json.load(open("grafana/dashboards/ose-otel-demo.json"))'` | Exit 0 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|---------|
| TSAMP-01 | 11-02, 11-03, 11-04 | Collector tail_sampling: status_code ERROR keep-100%, latency >1s keep-100%, probabilistic 20%, decision_wait: 10s, num_traces: 10000 | ✓ SATISFIED | All policy names, thresholds, and parameters confirmed in otelcol-config.yaml |
| TSAMP-02 | 11-02 | Collector config explicit comment block documenting OR-semantics priority chain | ✓ SATISFIED | "Policy Decision Flow" verbatim quote present in otelcol-config.yaml comment block above composite |
| TSAMP-03 | 11-01, 11-03, 11-06 | Workshop attendee observes trace count reduction; README §11 contains OFF/ON paired table | PARTIAL | README §11 table placeholder present; WIDGET-SLOW branch and load stream wired to drive latency policy; both PNG files deferred to Phase 18 — broken images until then |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `README.md` | ~595 (table) | `<img src="docs/screenshots/step-11-tail-sampling-OFF.png">` references a non-existent file | ⚠️ Warning | Broken image in rendered README until Phase 18 ships; does not affect workshop tooling or Collector behavior |
| `README.md` | ~596 (table) | `<img src="docs/screenshots/step-11-tail-sampling-ON.png">` references a non-existent file | ⚠️ Warning | Same as above; both are known debt per 11-06-SUMMARY.md |

No stub implementations found. No TODO/FIXME/placeholder patterns in the functional code (otelcol-config.yaml, OrderService.java, scripts/load.sh, mise.toml). The anti-patterns above are intentional placeholders per the Phase 18 deferred-screenshot contract.

### Human Verification Required

#### 1. End-to-End verify:tail-sampling GREEN

**Test:** Start the full stack (`mise run infra:up`), run both services (`mise run dev`), run load for 60+ seconds (`mise run load SLOW_RPS=2`), then run `mise run verify:tail-sampling`

**Expected:** Exit 0 with final line: `verify:tail-sampling: GREEN — composite + sub-policy contracts intact.` — Tier 1 confirms composite-policy series appearing in :8888/metrics; Tier 2 confirms all three sub-policy names (keep-errors, keep-slow, probabilistic-fallback) appear as `tailsampling.composite_policy` span attributes on Tempo-kept spans.

**Why human:** Requires live Docker stack (otel-collector at v0.151.0 with the alpha recordpolicy feature gate, Tempo at 2.10.5, Mimir, producer and consumer services running, load generator producing WIDGET-SLOW + error traces). Cannot replicate in a static analysis pass.

#### 2. Grafana Tail Sampling diagnostics row — live panel data

**Test:** After verify:tail-sampling GREEN, open `http://localhost:3000`, navigate to ose-otel-demo dashboard, expand the "Tail Sampling diagnostics (Phase 11)" collapsed row, inspect all 5 panels.

**Expected:** Panel 1 shows a non-zero `composite-policy / sampled` series; Panel 2 shows `composite-policy / not_sampled` series at ~4× the sampled rate; Panel 3 shows low/zero late-arriving spans; Panel 4 shows p50/p95/p99 latency in single-digit milliseconds; Panel 5 oscillates around 2000 in-memory traces.

**Why human:** Live Grafana rendering with actual Mimir-backed data cannot be verified statically. Visual confirmation that panels show real data (not "No data") is required.

### Gaps Summary

No blocking gaps — all functional deliverables (TSAMP-01 Collector config, TSAMP-02 OR-semantics comment, TSAMP-03 infrastructure wiring including WIDGET-SLOW branch, load stream, verify task, and dashboard row) are verified in the codebase.

The two missing screenshot files (step-11-tail-sampling-OFF.png and step-11-tail-sampling-ON.png) are explicitly deferred to Phase 18 in ROADMAP.md Phase 11 success criterion 3. The README §11 table serves as a placeholder per that contract; broken images are expected until Phase 18 ships.

The two human verification items (live E2E smoke and visual dashboard check) are standard runtime behaviors that require a running stack — not implementation gaps.

---

_Verified: 2026-05-03T08:00:00Z_
_Verifier: Claude (gsd-verifier)_
