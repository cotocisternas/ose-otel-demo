---
phase: 18-automated-screenshot-generation-playwright
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mise.toml
  - README.md
autonomous: true
requirements:
  - SCAP-01

must_haves:
  truths:
    - "`mise run screenshots` is a thin one-liner that invokes the new capture script"
    - "The old `docs:screenshots` bash task is deleted from mise.toml"
    - "The `scripts/screenshots/` directory and its files are deleted"
    - "README.md step-04 section renders the actual PNG instead of a TODO comment"
    - "The 'Verification screenshot pending (PREREQ-02)' note at the bottom of README.md is removed"
  artifacts:
    - path: "mise.toml"
      provides: "screenshots task one-liner; docs:screenshots task deleted"
      contains: "[tasks.screenshots]"
    - path: "README.md"
      provides: "step-04-metrics.png embed in place of TODO comment; PREREQ-02 pending note removed"
      contains: "docs/screenshots/step-04-metrics.png"
  key_links:
    - from: "mise.toml [tasks.screenshots]"
      to: "scripts/capture-screenshots.mjs"
      via: "run = \"node scripts/capture-screenshots.mjs\""
      pattern: "capture-screenshots\\.mjs"
---

<objective>
Delete the old `scripts/screenshots/` directory and its two files, replace the 99-line `[tasks."docs:screenshots"]` bash block in `mise.toml` with a 3-line `[tasks.screenshots]` one-liner, and update `README.md` to replace the step-04 TODO comment with an actual image embed (PREREQ-02 closure) and remove the "Verification screenshot pending" note.

Purpose: Clean up the v1.0 two-part screenshot system (bash driver + separate capture.mjs) and close PREREQ-02 at the documentation level.
Output: Deleted `scripts/screenshots/` directory, updated `mise.toml`, updated `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-automated-screenshot-generation-playwright/18-CONTEXT.md
@.planning/phases/18-automated-screenshot-generation-playwright/18-PATTERNS.md
</context>

<interfaces>
<!-- Key text to know before editing -->

From mise.toml (lines 153-156) — the shape to emulate for the new screenshots task:
```toml
[tasks.load]
description = "Continuous POST /orders load (~1 req/sec, ~50/50 express/standard). Run alongside `mise run dev`. Ctrl-C stops both child loaders."
run = "./scripts/load.sh"
```

From mise.toml (lines 163-261) — the entire block to DELETE:
- Starts at: `[tasks."docs:screenshots"]`
- Ends at: the closing `"""` of the run block, just before `[tasks."verify:bom"]`
- This is ~99 lines of bash

From README.md (lines 222-224) — the TODO comment block to REPLACE:
```markdown
<!-- TODO(DOC-04 v1.x): docs/screenshots/step-04-metrics.png — Mimir RED metrics panel.
     so the screenshot embed can be re-introduced without re-touching the surrounding prose.
-->
```

From README.md (line 548) — the "Verification screenshot pending" sentence to REMOVE:
```
**Verification screenshot pending** (PREREQ-02 / DOC-04 deferred — see TODO at Step 4). When captured, `docs/screenshots/step-04-metrics.png` will show the post-decomposition Grafana metrics-panel — the dashboard JSON itself is unchanged since v1.0 Phase 7, so the migration is invisible to the dashboard artifact.
```

Pattern for image embed (from README.md line 109):
```markdown
![Phase 1 baseline — empty Tempo trace search](docs/screenshots/step-01-empty-tempo.png)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete scripts/screenshots/ directory and update mise.toml</name>
  <files>mise.toml</files>
  <read_first>
    - mise.toml — read lines 153-270 to identify the exact bounds of the docs:screenshots block and the load task shape to emulate
  </read_first>
  <action>
**Step A: Delete the old scripts/screenshots/ directory**

Run this command (both files must be deleted; the directory is then empty and also gone):
```bash
rm -rf /home/coto/dev/demo/ose-otel-demo/scripts/screenshots/
```

This removes:
- `scripts/screenshots/capture.mjs` (replaced by scripts/capture-screenshots.mjs from Plan 01)
- `scripts/screenshots/package.json` (replaced by scripts/package.json from Plan 01)
- `scripts/screenshots/` (the directory itself)

**Step B: Edit mise.toml — delete docs:screenshots task, add screenshots task**

Read `mise.toml` first to confirm the exact line numbers. Then make two edits:

Edit 1 — DELETE the entire `[tasks."docs:screenshots"]` block (approximately lines 157-261, from the comment block through the closing `"""`). The block starts at the comment line `# Screenshot capture pipeline (Phase 7 / DOC-04 / D-05).` and ends at the closing `"""` just before `[tasks."verify:bom"]`. Remove all of it including the surrounding blank lines between it and `[tasks.load]` and the next task.

Edit 2 — INSERT the new `[tasks.screenshots]` block immediately after `[tasks.load]` (which ends at `run = "./scripts/load.sh"`). Add ONE blank line between the load task and the new screenshots task, then:

```toml
# ──────────────────────────────────────────────────────────────────
# Screenshot capture pipeline (Phase 18 / SCAP-01/02/03).
# Replaces the v1.0 docs:screenshots bash driver.
# Pre-requisites: mise run infra:up && mise run dev (for v2.0 captures).
# Set FORCE=1 to re-capture existing PNGs.
# ──────────────────────────────────────────────────────────────────
[tasks.screenshots]
description = "Capture all workshop screenshots to docs/screenshots/. Set FORCE=1 to re-capture existing PNGs. Requires infra + dev running for v2.0 captures."
run = "node scripts/capture-screenshots.mjs"
```

The new task is a thin one-liner (D-S4/D-S5). All logic lives in the script.

**Positioning rule:** The new `[tasks.screenshots]` block must appear BETWEEN `[tasks.load]` and `[tasks."verify:bom"]`, replacing the deleted `[tasks."docs:screenshots"]` block at the same location.
  </action>
  <verify>
    <automated>grep -c 'tasks.screenshots' /home/coto/dev/demo/ose-otel-demo/mise.toml</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/screenshots/` directory no longer exists: `test ! -d scripts/screenshots && echo ok` outputs `ok`
    - `grep 'tasks."docs:screenshots"' mise.toml` returns no match (deleted)
    - `grep 'tasks.screenshots' mise.toml` returns a match (added)
    - `grep 'capture-screenshots.mjs' mise.toml` returns a match (one-liner points to new script)
    - `grep 'docs:screenshots' mise.toml` returns no match (all references to old task gone)
    - `mise task ls 2>/dev/null | grep screenshots` shows `screenshots` (not `docs:screenshots`)
  </acceptance_criteria>
  <done>scripts/screenshots/ deleted; mise.toml has screenshots one-liner task; docs:screenshots task is gone</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md — replace step-04 TODO with image embed, remove PREREQ-02 pending note</name>
  <files>README.md</files>
  <read_first>
    - README.md — read the full file to find the exact TODO comment block (around line 222) and the PREREQ-02 pending sentence (around line 548) before editing
  </read_first>
  <action>
Make two targeted edits to README.md:

**Edit 1 — Replace the step-04 TODO comment block (around lines 222-224) with an image embed**

FIND this exact block (3 lines, HTML comment):
```
<!-- TODO(DOC-04 v1.x): docs/screenshots/step-04-metrics.png — Mimir RED metrics panel.
     so the screenshot embed can be re-introduced without re-touching the surrounding prose.
-->
```

REPLACE with this image embed (matching the alt text style used at README.md line 263):
```markdown
![Step 4 — RED Metrics panel in Grafana (orders_created_total + http.server.request.duration quantiles)](docs/screenshots/step-04-metrics.png)
```

**Edit 2 — Remove the "Verification screenshot pending" paragraph (around line 548)**

FIND this exact sentence (it may be a standalone paragraph or part of a paragraph):
```
**Verification screenshot pending** (PREREQ-02 / DOC-04 deferred — see TODO at Step 4). When captured, `docs/screenshots/step-04-metrics.png` will show the post-decomposition Grafana metrics-panel — the dashboard JSON itself is unchanged since v1.0 Phase 7, so the migration is invisible to the dashboard artifact.
```

DELETE that sentence entirely (including any trailing blank line if it becomes double-spaced). If it is its own paragraph, delete the whole paragraph. Do not edit surrounding prose.

**No other README edits.** The screenshot reference embeds for step-11 and step-12 at lines ~592-593 and ~719 are already correct HTML/Markdown and require no changes (they reference paths that Plan 01 + mise run screenshots will produce).
  </action>
  <verify>
    <automated>grep -c "step-04-metrics.png" /home/coto/dev/demo/ose-otel-demo/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TODO(DOC-04" README.md` returns no match (TODO comment deleted)
    - `grep "docs/screenshots/step-04-metrics.png" README.md` returns at least 1 match (image embed present)
    - `grep "!\[Step 4" README.md` returns a match (alt text present)
    - `grep "Verification screenshot pending" README.md` returns no match (PREREQ-02 note removed)
    - `grep "PREREQ-02" README.md` returns no match (all PREREQ-02 references removed from README)
    - `grep "step-11-tail-sampling-OFF.png" README.md` still returns a match (pre-existing embed untouched)
    - `grep "step-12-exemplars.png" README.md` still returns a match (pre-existing embed untouched)
  </acceptance_criteria>
  <done>README.md renders the step-04-metrics.png image embed; TODO comment and PREREQ-02 pending note are deleted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → filesystem | Deleting scripts/screenshots/ and editing mise.toml + README.md are local filesystem operations with no network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | mise.toml task deletion | accept | The `docs:screenshots` task is being deliberately deleted; the new `screenshots` task replaces it; risk is only accidental deletion of adjacent tasks — reading the file first (as required by read_first) prevents this |
| T-18-06 | Information Disclosure | scripts/screenshots/ deletion | accept | `capture.mjs` and `package.json` contain no secrets; deletion is safe; both files are tracked in git history if needed |
</threat_model>

<verification>
After both tasks complete:
1. `test ! -d scripts/screenshots && echo "dir deleted"` outputs `dir deleted`
2. `grep 'tasks."docs:screenshots"' mise.toml` returns empty (deleted)
3. `grep 'tasks.screenshots' mise.toml` returns a match (added)
4. `grep 'TODO(DOC-04' README.md` returns empty (comment deleted)
5. `grep 'step-04-metrics.png' README.md` returns at least 1 line (image embed present)
6. `grep 'Verification screenshot pending' README.md` returns empty (note deleted)
</verification>

<success_criteria>
- `scripts/screenshots/` directory does not exist
- `mise.toml` has `[tasks.screenshots]` with `run = "node scripts/capture-screenshots.mjs"` and no `docs:screenshots` block
- `README.md` has `![Step 4 — RED Metrics...](docs/screenshots/step-04-metrics.png)` image embed where the TODO comment was
- `README.md` has no "Verification screenshot pending" paragraph
</success_criteria>

<output>
After completion, create `.planning/phases/18-automated-screenshot-generation-playwright/18-02-SUMMARY.md`
</output>
