---
phase: 18-automated-screenshot-generation-playwright
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mise.toml
  - README.md
autonomous: true
requirements:
  - SCAP-01

must_haves:
  truths:
    - "mise run screenshots task exists and is a thin one-liner"
    - "mise run docs:screenshots task is deleted"
    - "scripts/screenshots/ directory is deleted"
    - "README.md step-04 TODO comment is replaced with actual image embed"
    - "README.md PREREQ-02 pending note is removed"
  artifacts:
    - path: "mise.toml"
      provides: "thin screenshots task replacing docs:screenshots"
      contains: '[tasks.screenshots]'
    - path: "README.md"
      provides: "step-04 image embed + removal of PREREQ-02 deferral note"
      contains: 'docs/screenshots/step-04-metrics.png'
  key_links:
    - from: "mise.toml [tasks.screenshots]"
      to: "scripts/capture-screenshots.mjs"
      via: "run = \"node scripts/capture-screenshots.mjs\""
      pattern: "node scripts/capture-screenshots.mjs"
---

<objective>
Delete the obsolete scripts/screenshots/ directory and replace the two text changes needed in mise.toml and README.md. This plan runs in parallel with 18-01 (zero file overlap) and completes the non-script changes for the phase.

Purpose: Remove the v1.0 bash driver, add the thin screenshots task, and close PREREQ-02 by replacing the step-04 TODO comment with the actual image embed in README.md.

Output: mise.toml without [tasks."docs:screenshots"] and with [tasks.screenshots] one-liner; README.md with step-04 image embed replacing the TODO comment and the PREREQ-02 pending note removed; scripts/screenshots/ directory deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-automated-screenshot-generation-playwright/18-CONTEXT.md
@.planning/phases/18-automated-screenshot-generation-playwright/18-PATTERNS.md
</context>

<interfaces>
<!-- mise.toml task shape from analog [tasks.load] block (thin one-liner pattern): -->

[tasks.load]
description = "Continuous POST /orders load (~1 req/sec, ~50/50 express/standard). Run alongside `mise run dev`. Ctrl-C stops both child loaders."
run = "./scripts/load.sh"

<!-- README.md existing screenshot embed pattern (line 109): -->
![Phase 1 baseline — empty Tempo trace search](docs/screenshots/step-01-empty-tempo.png)

<!-- README.md two-column table embed pattern (lines 149-150): -->
<td><img src="docs/screenshots/step-02-disconnected-traces.png" alt="Step 2 — TWO disconnected traces in Tempo for one POST"></td>

<!-- Step-04 TODO comment block to REPLACE (README.md lines 222-228): -->
<!-- TODO(DOC-04 v1.x): docs/screenshots/step-04-metrics.png — Mimir RED metrics panel.
     Capture deferred per Phase 7 wave-4 SUMMARY (07-04); referenced here as a placeholder
     so the screenshot embed can be re-introduced without re-touching the surrounding prose.
     Until then, the dashboard URL below carries the visual: open the auto-provisioned
     "OSE OTel Demo — Three Signals" dashboard at http://localhost:3000/d/ose-otel-demo with
     `mise run load` running in another terminal — the per-priority `orders_created_total`
     panel and the `http_server_request_duration_seconds` p50/p95/p99 panel populate live. -->

<!-- PREREQ-02 pending note to REMOVE (README.md line 548): -->
**Verification screenshot pending** (PREREQ-02 / DOC-04 deferred — see TODO at Step 4). When captured, `docs/screenshots/step-04-metrics.png` will show the post-decomposition Grafana metrics-panel — the dashboard JSON itself is unchanged since v1.0 Phase 7, so the migration is invisible to the dashboard artifact.

<!-- [tasks."docs:screenshots"] block to DELETE (mise.toml lines 163-261, the full 99-line bash block) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete scripts/screenshots/ and update mise.toml</name>
  <files>mise.toml</files>
  <read_first>
    - mise.toml (read lines 150-270 to confirm exact position of [tasks."docs:screenshots"] block and [tasks.load] as the positioning reference)
  </read_first>
  <action>
**Step A: Delete the scripts/screenshots/ directory.**

Run: rm -rf /home/coto/dev/demo/ose-otel-demo/scripts/screenshots/

This removes scripts/screenshots/capture.mjs and scripts/screenshots/package.json (D-S1).

**Step B: Edit mise.toml.**

Make two changes to mise.toml:

CHANGE 1 — Delete the entire [tasks."docs:screenshots"] block.
The block spans from line 163 ([tasks."docs:screenshots"]) through line 261 (the closing `"""` of the `run` heredoc). This is the 99-line bash block containing the WORKTREE_DIR loop, PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install, and per-tag git worktree cycle. Delete all lines from `[tasks."docs:screenshots"]` through and including the final triple-quote `"""` closing the run string.

The deleted block begins with:
```
[tasks."docs:screenshots"]
description = "Capture per-step screenshots (DOC-04). Cycles step-NN-* tags via git worktree, drives Grafana via Playwright, writes docs/screenshots/*.png."
run = """
```
And ends with:
```
echo "=== screenshots complete ==="
ls -la "$OUTPUT_DIR"
"""
```
Also delete the comment block immediately above it (lines 157-162):
```
# ──────────────────────────────────────────────────────────────────
# Screenshot capture pipeline (Phase 7 / DOC-04 / D-05).
# Manual one-shot — run before completing Phase 7 to refresh docs/screenshots/.
# CI does NOT re-run this (per D-05).
# WARNING: cycles git tags (step-01-baseline ... step-06-tests) in a worktree.
# ──────────────────────────────────────────────────────────────────
```

CHANGE 2 — Add the [tasks.screenshots] one-liner block.

Insert the following new block immediately after the [tasks.load] block (which ends at line 156 with `run = "./scripts/load.sh"`). The new block is:

```toml
[tasks.screenshots]
description = "Capture all workshop screenshots to docs/screenshots/. Set FORCE=1 to re-capture existing PNGs. Prerequisites: mise run infra:up && mise run dev must be running."
run = "node scripts/capture-screenshots.mjs"
```

Position: immediately after the [tasks.load] block, before the former [tasks."docs:screenshots"] block location. The screenshots task must appear in the workshop helpers section alongside load, dev, demo:order.
  </action>
  <verify>
    <automated>grep -c 'tasks.screenshots' mise.toml</automated>
  </verify>
  <acceptance_criteria>
    - mise.toml contains '[tasks.screenshots]' (new task added)
    - mise.toml does NOT contain '[tasks."docs:screenshots"]' (old task deleted)
    - mise.toml contains 'node scripts/capture-screenshots.mjs' (correct one-liner)
    - scripts/screenshots/ directory no longer exists: test ! -d scripts/screenshots && echo "deleted"
    - scripts/screenshots/capture.mjs no longer exists: test ! -f scripts/screenshots/capture.mjs && echo "deleted"
  </acceptance_criteria>
  <done>scripts/screenshots/ deleted; mise.toml has screenshots task and no docs:screenshots task</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md — replace step-04 TODO and remove PREREQ-02 note</name>
  <files>README.md</files>
  <read_first>
    - README.md (read lines 218-235 for the TODO comment block; read lines 545-552 for the PREREQ-02 note)
  </read_first>
  <action>
Make two precise edits to README.md:

EDIT 1 — Replace the TODO comment block (lines 222-228) with the actual image embed.

FIND this exact block (including the blank line before "### Why it matters"):
```
<!-- TODO(DOC-04 v1.x): docs/screenshots/step-04-metrics.png — Mimir RED metrics panel.
     Capture deferred per Phase 7 wave-4 SUMMARY (07-04); referenced here as a placeholder
     so the screenshot embed can be re-introduced without re-touching the surrounding prose.
     Until then, the dashboard URL below carries the visual: open the auto-provisioned
     "OSE OTel Demo — Three Signals" dashboard at http://localhost:3000/d/ose-otel-demo with
     `mise run load` running in another terminal — the per-priority `orders_created_total`
     panel and the `http_server_request_duration_seconds` p50/p95/p99 panel populate live. -->
```

REPLACE with:
```
![Step 4 — RED Metrics panel in Grafana (orders_created_total + http_server_request_duration)](docs/screenshots/step-04-metrics.png)
```

EDIT 2 — Remove the PREREQ-02 pending note (line 548).

FIND this exact line:
```
**Verification screenshot pending** (PREREQ-02 / DOC-04 deferred — see TODO at Step 4). When captured, `docs/screenshots/step-04-metrics.png` will show the post-decomposition Grafana metrics-panel — the dashboard JSON itself is unchanged since v1.0 Phase 7, so the migration is invisible to the dashboard artifact.
```

DELETE this line entirely (including the surrounding blank lines if they become consecutive after deletion — reduce to a single blank line).

These are the only two changes to README.md. Do not alter any other content.
  </action>
  <verify>
    <automated>grep -c 'step-04-metrics.png' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains 'docs/screenshots/step-04-metrics.png' inside a markdown image embed (![...](docs/screenshots/step-04-metrics.png))
    - README.md does NOT contain 'TODO(DOC-04 v1.x): docs/screenshots/step-04-metrics.png'
    - README.md does NOT contain 'PREREQ-02 / DOC-04 deferred'
    - grep -c "step-04-metrics.png" README.md returns exactly 1 (the image embed, not the TODO comment)
  </acceptance_criteria>
  <done>README.md has step-04 image embed; TODO comment and PREREQ-02 note removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mise task → shell | run = "node scripts/capture-screenshots.mjs" executes a fixed script path — no env var interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Information Disclosure | scripts/screenshots/ deleted without backup | accept | Directory is tracked in git — fully recoverable via git checkout if needed; this is an intentional deletion per D-S1 |
</threat_model>

<verification>
- grep -c 'tasks.screenshots' mise.toml returns 1
- grep -c 'docs:screenshots' mise.toml returns 0
- test ! -d scripts/screenshots && echo OK
- grep -c '!\[Step 4' README.md returns 1
- grep -c 'PREREQ-02' README.md returns 0
</verification>

<success_criteria>
- scripts/screenshots/ directory deleted
- mise.toml has [tasks.screenshots] one-liner and no [tasks."docs:screenshots"] block
- README.md has step-04 image embed replacing TODO comment
- README.md has no PREREQ-02 pending note
</success_criteria>

<output>
After completion, create `.planning/phases/18-automated-screenshot-generation-playwright/18-02-SUMMARY.md`
</output>
