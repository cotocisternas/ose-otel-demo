---
phase: "14-jdbc-jpa-database-spans"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - consumer-service/pom.xml
  - consumer-service/src/main/resources/application.yaml
  - consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java
  - consumer-service/src/main/resources/schema.sql
autonomous: true
requirements:
  - DBSP-01

must_haves:
  truths:
    - "consumer-service compiles after the pom.xml change (starter-data-jpa added, starter-jdbc removed)"
    - "application.yaml no longer contains spring.sql.init.mode and does contain spring.jpa section"
    - "Phase 8 OrderRepository.java and schema.sql are deleted from the working tree"
  artifacts:
    - path: "consumer-service/pom.xml"
      provides: "spring-boot-starter-data-jpa dependency; spring-boot-starter-jdbc removed"
      contains: "spring-boot-starter-data-jpa"
    - path: "consumer-service/src/main/resources/application.yaml"
      provides: "JPA/Hibernate configuration; sql.init.mode removed"
      contains: "ddl-auto: update"
  key_links:
    - from: "consumer-service/pom.xml"
      to: "spring-boot-starter-data-jpa"
      via: "dependency element"
      pattern: "spring-boot-starter-data-jpa"
    - from: "consumer-service/src/main/resources/application.yaml"
      to: "spring.jpa.hibernate.ddl-auto"
      via: "YAML key"
      pattern: "ddl-auto"
---

<objective>
Replace Phase 8's raw-JDBC persistence foundation with Spring Data JPA. This plan performs the surgery: adds the JPA dependency (and removes the redundant JDBC dependency), updates application.yaml to drop schema.sql init and add Hibernate DDL config, and deletes the Phase 8 artifacts no longer needed.

Purpose: All subsequent Phase 14 plans depend on the pom.xml and application.yaml being in the JPA-ready state. The compile check at the end of this plan gates the rest.
Output: Modified pom.xml and application.yaml; deleted OrderRepository.java and schema.sql; clean compile.
</objective>

<execution_context>
@/home/coto/.claude/get-shit-done/workflows/execute-plan.md
@/home/coto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-jdbc-jpa-database-spans/14-CONTEXT.md
@.planning/phases/14-jdbc-jpa-database-spans/14-RESEARCH.md

<interfaces>
<!-- Key existing content the executor needs. Extracted from codebase. -->

From consumer-service/pom.xml (Phase 8 JDBC block, lines 153-173):
```xml
<!-- REMOVE (becomes redundant when spring-boot-starter-data-jpa is added) -->
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-jdbc</artifactId>
</dependency>

<!-- REMOVE (no longer needed — JPA/Hibernate manages DDL via ddl-auto) -->
<dependency>
  <groupId>org.postgresql</groupId>
  <artifactId>postgresql</artifactId>
</dependency>
<!-- NOTE: postgresql driver stays! Only starter-jdbc is removed. -->
```

Current application.yaml state (lines 1-17):
```yaml
spring:
  application:
    name: order-consumer
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/orders}
    username: ${SPRING_DATASOURCE_USERNAME:orders}
    password: ${SPRING_DATASOURCE_PASSWORD:orders}
  sql:
    init:
      mode: always   # ← REMOVE per D-J2
management:
  endpoints:
    web:
      exposure:
        include: health
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace starter-jdbc with starter-data-jpa in consumer-service/pom.xml</name>
  <files>consumer-service/pom.xml</files>
  <read_first>consumer-service/pom.xml</read_first>
  <action>
Edit consumer-service/pom.xml:

1. REMOVE the entire `spring-boot-starter-jdbc` dependency block (lines 153-163 approximately):
```xml
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-jdbc</artifactId>
</dependency>
```

2. ADD `spring-boot-starter-data-jpa` in its place (same location, after the Phase 8 comment block). The new block must carry a Phase 14 explanatory comment in the same style as the Phase 8 JDBC comment block:

```xml
<!--
  Phase 14: Spring Data JPA — replaces spring-boot-starter-jdbc (which becomes
  transitively redundant; removed above when JPA is added).
  spring-boot-starter-data-jpa is BOM-managed by Spring Boot 3.4.13; pulls
  Hibernate 6.x + Spring Data JPA 3.x + HikariCP 5.x + aspectjweaver
  (via spring-boot-starter-aop). No <version> tag needed.
  @EnableAspectJAutoProxy is activated automatically by Spring Boot's
  AopAutoConfiguration when aspectjweaver is on the classpath.
  Phase 8 contrast: starter-jdbc exposed raw JdbcTemplate (1 INSERT span);
  Phase 14 exposes @Transactional + JpaRepository (transaction parent + SELECT/INSERT waterfall).
-->
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>
```

3. Keep `org.postgresql:postgresql` — it is still required (JPA uses the same driver). Do NOT remove it.

4. Keep all other dependencies unchanged (OTel, logback, semconv, amqp, actuator, web, test).

After this edit the consumer-service dependency declaration replaces:
  starter-jdbc → starter-data-jpa
while keeping:
  postgresql, opentelemetry-*, semconv, logback bridge, amqp, actuator, web, test

Note: spring-boot-starter-data-jpa is BOM-managed by the Spring Boot 3.4.13 BOM declared in the parent pom.xml — NO `<version>` tag is added.
  </action>
  <verify>
    <automated>grep -c 'spring-boot-starter-data-jpa' /home/coto/dev/demo/ose-otel-demo/consumer-service/pom.xml</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'spring-boot-starter-data-jpa' consumer-service/pom.xml` outputs `1`
- `grep -c 'spring-boot-starter-jdbc' consumer-service/pom.xml` outputs `0`
- `grep -c 'postgresql' consumer-service/pom.xml` outputs at least `1` (driver kept)
- `grep 'spring-boot-starter-data-jpa' consumer-service/pom.xml` shows no `<version>` tag on that element
  </acceptance_criteria>
  <done>pom.xml declares spring-boot-starter-data-jpa (no version), has no spring-boot-starter-jdbc, and retains the postgresql driver dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Update application.yaml — remove sql.init, add spring.jpa block; delete Phase 8 files</name>
  <files>
    consumer-service/src/main/resources/application.yaml
    consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java
    consumer-service/src/main/resources/schema.sql
  </files>
  <read_first>
    consumer-service/src/main/resources/application.yaml
    consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java
    consumer-service/src/main/resources/schema.sql
  </read_first>
  <action>
STEP A: Rewrite consumer-service/src/main/resources/application.yaml.

Replace the entire file with this exact content (per RESEARCH.md Pattern 4 + D-J2):

```yaml
spring:
  application:
    name: order-consumer
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/orders}
    username: ${SPRING_DATASOURCE_USERNAME:orders}
    password: ${SPRING_DATASOURCE_PASSWORD:orders}
  # Phase 14: JPA config replaces spring.sql.init (D-J2: JPA owns DDL via entity annotations).
  # ddl-auto=update: Hibernate creates the 'orders' table on first run; patches on subsequent runs.
  # The Phase 8 'processed_orders' table remains in the database but is no longer written to.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
management:
  endpoints:
    web:
      exposure:
        include: health
```

Key changes vs current file:
- REMOVED: `spring.sql.init.mode: always` and the entire `spring.sql.init` block (D-J2)
- ADDED: `spring.jpa.hibernate.ddl-auto: update`, `show-sql: false`, `dialect: PostgreSQLDialect`

STEP B: Delete Phase 8 files (per D-J1):
- Delete `consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java`
- Delete `consumer-service/src/main/resources/schema.sql`

Use `git rm` for both files so the deletion is tracked in git:
```bash
git rm consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java
git rm consumer-service/src/main/resources/schema.sql
```

STEP C: Compile check. After the file changes, run a compile to verify the build still resolves (it will fail on ProcessingService referencing the deleted OrderRepository — that breakage is EXPECTED at this wave; the compile check confirms only that the pom.xml parses and dependencies resolve):
```bash
cd /home/coto/dev/demo/ose-otel-demo && mvn -pl consumer-service compile -Dmaven.test.skip=true 2>&1 | tail -30
```
The compile will fail with `cannot find symbol: class OrderRepository` — this is EXPECTED (ProcessingService still references it). That compile error proves the deletion took effect and the JPA dependency is on the classpath. Record this in your summary as an expected Wave 1 breakage that Wave 2 resolves.
  </action>
  <verify>
    <automated>
      cd /home/coto/dev/demo/ose-otel-demo &&
      grep -c 'ddl-auto' consumer-service/src/main/resources/application.yaml &&
      ! test -f consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java &&
      ! test -f consumer-service/src/main/resources/schema.sql
    </automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'ddl-auto' consumer-service/src/main/resources/application.yaml` outputs `1`
- `grep -c 'sql.init' consumer-service/src/main/resources/application.yaml` outputs `0`
- `grep -c 'show-sql' consumer-service/src/main/resources/application.yaml` outputs `1`
- `grep -c 'PostgreSQLDialect' consumer-service/src/main/resources/application.yaml` outputs `1`
- File `consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java` does NOT exist
- File `consumer-service/src/main/resources/schema.sql` does NOT exist
- `git status` shows `OrderRepository.java` and `schema.sql` as deleted (tracked deletion)
  </acceptance_criteria>
  <done>application.yaml contains JPA config and no sql.init block; both Phase 8 files are git-rm'd from the working tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application.yaml → Spring Boot datasource | Credentials sourced from env vars; no literals in config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Spoofing | spring.datasource.url env var | accept | URL sourced from env var `SPRING_DATASOURCE_URL`; existing pattern from Phase 8; no new surface |
| T-14-01-02 | Information Disclosure | ddl-auto=update on startup | accept | Workshop-local PostgreSQL only; `update` exposes no data; `create-drop` would be riskier (data loss); production workshops use their own database |
| T-14-01-03 | Tampering | Deletion of schema.sql | accept | schema.sql replaced by JPA DDL; Hibernate parameterized queries prevent injection; no user data in DDL path |
</threat_model>

<verification>
After this plan executes:
1. `grep -c 'spring-boot-starter-data-jpa' consumer-service/pom.xml` == 1
2. `grep -c 'spring-boot-starter-jdbc' consumer-service/pom.xml` == 0
3. `grep -c 'ddl-auto' consumer-service/src/main/resources/application.yaml` == 1
4. `grep -c 'sql.init' consumer-service/src/main/resources/application.yaml` == 0
5. `test -f consumer-service/src/main/java/com/example/consumer/db/OrderRepository.java` exits non-zero (file deleted)
6. `test -f consumer-service/src/main/resources/schema.sql` exits non-zero (file deleted)
7. Compile produces an expected `cannot find symbol: class OrderRepository` error (and NOT any JPA dependency resolution error)
</verification>

<success_criteria>
- consumer-service/pom.xml contains spring-boot-starter-data-jpa with no version tag
- consumer-service/pom.xml does not contain spring-boot-starter-jdbc
- consumer-service/src/main/resources/application.yaml contains spring.jpa.hibernate.ddl-auto=update
- consumer-service/src/main/resources/application.yaml does not contain spring.sql.init
- OrderRepository.java and schema.sql are deleted from the git index
- Compile error is on OrderRepository reference in ProcessingService (expected Wave 1 breakage), not on missing JPA dependency
</success_criteria>

<output>
After completion, create `.planning/phases/14-jdbc-jpa-database-spans/14-01-SUMMARY.md`
</output>
