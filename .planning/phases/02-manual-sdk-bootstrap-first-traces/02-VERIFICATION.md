---
phase: 02-manual-sdk-bootstrap-first-traces
verified: 2026-05-01T00:00:00Z
status: human_needed
score: 11/11 must-haves verified
overrides_applied: 0
re_verification: null
human_verification:
  - test: "TWO distinct trace IDs in Tempo per POST /orders"
    expected: "Open Grafana → Tempo Explore (http://localhost:3000, admin/admin); search service.name=order-producer and service.name=order-consumer; both show traces, but the producer's traceID differs from the consumer's traceID"
    why_human: "Runtime telemetry inspection in Grafana Tempo UI; the SUMMARY 02-06 captured this proof (PT=f00aab524fbbe2f4f90fa2cd4e6b7a57 != CT=d7480cc2579a5fc9471330a0c9474228) but it cannot be re-asserted statically — the telemetry has since been flushed when the apps stopped"
  - test: "Ctrl-C flushes the last span batch"
    expected: "Send POST /orders, immediately Ctrl-C the producer; the trace from that POST appears in Tempo within 12 seconds — proving destroyMethod=close cascades close→shutdown(10s)→BSP.flush"
    why_human: "Requires running both apps + infra and observing trace count grow after SIGTERM; SUMMARY 02-06 confirmed BEFORE_PT=1, AFTER_PT=2 with a 1-second graceful shutdown. Cannot be re-asserted programmatically without re-launching the stack"
  - test: "Producer span attributes correctly populated in Tempo"
    expected: "Open the producer's PRODUCER span in Tempo and verify 4 attribute keys: messaging.system=rabbitmq, messaging.destination.name=orders.created, messaging.operation.type=send (NOT bare messaging.operation), messaging.rabbitmq.destination.routing_key=order.created. Open the SERVER span and verify 7 HTTP attrs (method/path/scheme/server.address/server.port/route + status_code)"
    why_human: "Visual span-attribute inspection in Grafana UI; SUMMARY 02-04 captured this proof at trace 25eb831a77261da7c8e2832a71b2fee5 with kinds {SERVER:1, INTERNAL:1, PRODUCER:1}; static code review confirms the constants resolve to the expected keys but only runtime tools can render the actual attribute values"
  - test: "Consumer's CONSUMER span has empty parentSpanId (Context.root() honored)"
    expected: "Inspect the consumer trace; the root CONSUMER span ('orders.created process') has no parentSpanId (Context.root() forces a fresh root) — this is the broken-then-fixed pedagogical state Phase 3 will fix"
    why_human: "Runtime parent-chain assertion; SUMMARY 02-05 captured trace 56093b9c02831f1f273c5e1e4fd97c22 with empty CONSUMER.parentSpanId and INTERNAL.parentSpanId == CONSUMER.spanId. Re-asserting requires fresh telemetry"
  - test: "RabbitMQ message lacks traceparent header (broken-state proof)"
    expected: "RabbitMQ Mgmt UI (http://localhost:15672, guest/guest) → Queues → orders.created → Get messages with Reject requeue=true; verify the message properties contain JSON payload but NO 'traceparent' header. This proves PROP-01 is correctly NOT yet implemented"
    why_human: "Negative-evidence runtime check; the absence of a header in a queued message can only be observed in the running broker"
---

# Phase 2: Manual SDK Bootstrap & First Traces — Verification Report

**Phase Goal:** Workshop attendee can read `OtelSdkConfiguration.java` line-by-line in **both** services (deliberately duplicated, not extracted to a shared library), see the smallest possible OpenTelemetry surface — `OpenTelemetrySdk.builder()` with `Resource`, `SdkTracerProvider`, `BatchSpanProcessor`, `OtlpGrpcSpanExporter`, explicit sampler, and graceful shutdown — and observe that issuing a `POST /orders` produces **two separate traces** in Tempo (one for the producer, one for the consumer). The visible "brokenness" of unpropagated traces IS the phase deliverable; it sets up the broken-then-fixed payoff in Phase 3.

**Verified:** 2026-05-01
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP.md Phase 2 Success Criteria + PLAN frontmatters)

| #   | Truth                                                                                      | Status                  | Evidence                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------ | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | POST /orders produces TWO distinct traces in Tempo with correct service.name labels (no `unknown_service:java`) | VERIFIED (static + summary attestation) | SUMMARY 02-06 captured runtime: PT=f00aab524fbbe2f4f90fa2cd4e6b7a57, CT=d7480cc2579a5fc9471330a0c9474228, unknown_service:java count=0. Static: both `OtelSdkConfiguration.java` files set `ServiceAttributes.SERVICE_NAME` to `order-producer`/`order-consumer` via `Resource.getDefault().merge(...)`. Routes to human verification for re-confirmation against live infra |
| 2   | Ctrl-C flushes the last batch of spans (`@Bean(destroyMethod="close")` works)             | VERIFIED (static + summary attestation) | Both files declare `@Bean(destroyMethod = "close")` on the `OpenTelemetry` factory; SUMMARY 02-06 captured BEFORE_PT=1, AFTER_PT=2 with 1s graceful-shutdown latency. Routes to human verification for re-confirmation                                                                                |
| 3   | Both `OtelSdkConfiguration.java` files heavily commented per DOC-03                       | VERIFIED                | producer = 137 comment lines (>=40 required); consumer = 131 comment lines (>=40 required); both contain the multi-paragraph sampler tradeoff with `traceIdRatioBased(0.1)` reference per D-14                                                                                                       |
| 4   | Producer trace = SERVER + INTERNAL + PRODUCER spans; Consumer trace = CONSUMER + INTERNAL spans | VERIFIED (static + summary attestation) | SUMMARY 02-04: producer trace 25eb831a... = 3 spans {kind 2 SERVER, kind 1 INTERNAL, kind 4 PRODUCER}, names ["POST /orders","OrderService.place","orders.created publish"]. SUMMARY 02-05: consumer trace 56093b9c... = 2 spans {kind 5 CONSUMER, kind 1 INTERNAL}, names ["orders.created process","ProcessingService.process"]. Static: 5 spanBuilder sites confirmed at HttpServerSpanFilter / OrderService.place / OrderPublisher.publish / OrderListener.onOrder / ProcessingService.process. Routes to human verification |
| 5   | README explicitly states the per-service duplication is intentional (DOC-05 callout)      | VERIFIED                | README.md L90 `## Why is OtelSdkConfiguration.java duplicated?` heading present; L92 contains "duplicated per service on purpose", "@AutoConfiguration", and "hide one of the two readings"                                                                                                            |
| 6   | Annotated tag `step-02-traces` exists on main; reproduces this state                      | VERIFIED                | `git for-each-ref refs/tags/step-02-traces` returns `tag` (NOT `commit` — annotated, not lightweight); points at SHA dac865f; `git ls-tree step-02-traces` confirms all 3 config files + 4 instrumented Java files exist at the tagged commit                                                          |
| 7   | Tracer beans constructor-injected (D-02) — no GlobalOpenTelemetry, no setter, no @Autowired field | VERIFIED                | OrderService(OrderPublisher, Tracer); OrderPublisher(RabbitTemplate, Tracer); HttpServerSpanFilter(Tracer); OrderListener(ProcessingService, Tracer); ProcessingService(Tracer) — 5/5 single-constructor injection. Zero `GlobalOpenTelemetry` / `buildAndRegisterGlobal` / `AutoConfiguredOpenTelemetrySdk` references in main code (only mentioned in disclaiming comments)                              |
| 8   | All 5 spans use exact D-01 inline template with D-03 catch shape                          | VERIFIED                | Each span site contains: spanBuilder + setSpanKind + setAttribute(s) + startSpan; try (Scope = makeCurrent()) body; catch (RuntimeException \| ServletException \| IOException) recordException + setStatus(StatusCode.ERROR) + throw; finally span.end(). Both consumer and producer spans use semconv-aligned constants throughout |
| 9   | mvn -DskipTests install exits 0; mise run verify:bom exits 0                              | VERIFIED                | Verifier executed `mvn -DskipTests -q install` — exits 0 with no output. `mise run verify:bom` outputs `Phase 2 baseline confirmed: one version per OpenTelemetry artifact across all modules.`                                                                                                       |
| 10  | Five OTel deps present in BOTH POMs (3 BOM-managed + 2 pinned semconv)                    | VERIFIED                | Both pom.xml files contain: opentelemetry-api/-sdk/-exporter-otlp (no `<version>` — BOM-managed); opentelemetry-semconv `1.40.0`; opentelemetry-semconv-incubating `1.40.0-alpha`. `mvn dependency:tree -Dincludes=io.opentelemetry` confirms all artifacts at exactly one version each              |
| 11  | Forbidden artifacts ABSENT: opentelemetry-sdk-extension-autoconfigure, opentelemetry-logback-appender-1.0, legacy io.opentelemetry:opentelemetry-semconv | VERIFIED                | grep returns 0 matches in both POMs for `opentelemetry-sdk-extension-autoconfigure` (note: `-spi` suffix transitive is harmless and present, but the runtime artifact is absent) and `opentelemetry-logback-appender`. Legacy semconv coord (`<groupId>io.opentelemetry</groupId>` + `<artifactId>opentelemetry-semconv</artifactId>` without the `.semconv.` group) is absent |

**Score:** 11/11 truths VERIFIED

### Required Artifacts

| Artifact                                                                                     | Expected                                                                                                                       | Status      | Details                                                                                                                                                                                                                                            |
| -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `producer-service/pom.xml`                                                                   | 5 OTel deps (3 BOM-managed + 2 pinned semconv)                                                                                 | VERIFIED    | All 5 dependencies present at lines 57-99; descriptions/comments match plan; forbidden artifacts absent                                                                                                                                              |
| `consumer-service/pom.xml`                                                                   | IDENTICAL 5 OTel deps to producer                                                                                              | VERIFIED    | All 5 dependencies present at lines 57-99; identical block to producer (modulo Spring starter ordering noise unrelated to OTel)                                                                                                                       |
| `mise.toml` `[tasks."verify:bom"]`                                                           | Inverted Phase 2 invariant — asserts one-version-per-artifact                                                                  | VERIFIED    | Block present; description: `Phase 2 invariant: one version per io.opentelemetry* artifact across the reactor`; success message: `Phase 2 baseline confirmed: one version per OpenTelemetry artifact across all modules.` (verified by execution)    |
| `producer-service/.../config/OtelSdkConfiguration.java`                                      | @Configuration with @Bean(destroyMethod="close") OpenTelemetry, @Bean Tracer, @Bean HttpServerSpanFilter — heavy DOC-03 comments | VERIFIED    | 214 lines, 137 comment lines; all required builder calls present (Resource.getDefault().merge, OtlpGrpcSpanExporter, BSP.builder, Sampler.parentBased(alwaysOn), SdkTracerProvider.builder, ContextPropagators.create); HttpServerSpanFilter @Bean factory at L210 |
| `producer-service/.../config/HttpServerSpanFilter.java`                                      | OncePerRequestFilter wrapping non-/actuator HTTP requests in SERVER span with 7 HTTP semconv attrs                            | VERIFIED    | 123 lines; extends OncePerRequestFilter (NOT @Component); shouldNotFilter checks startsWith("/actuator/"); doFilterInternal uses D-01 inline template with HTTP_REQUEST_METHOD, URL_PATH, URL_SCHEME, SERVER_ADDRESS, SERVER_PORT, HTTP_ROUTE pre-chain + HTTP_RESPONSE_STATUS_CODE post-chain; jakarta.servlet imports |
| `consumer-service/.../config/OtelSdkConfiguration.java`                                      | Mirror of producer's with EXACTLY 5 known differences; NO HttpServerSpanFilter @Bean (D-07)                                    | VERIFIED    | 203 lines, 131 comment lines; structurally identical except: package=`com.example.consumer.config`; SERVICE_NAME=`order-consumer`; tracer scope=`com.example.consumer`; JavaDoc cross-reference points at producer; "Why no HttpServerSpanFilter?" paragraph present; NO @Bean HttpServerSpanFilter factory |
| `producer-service/.../domain/OrderService.java`                                              | Tracer-injected; place(...) wrapped in INTERNAL span "OrderService.place"                                                     | VERIFIED    | 64 lines; constructor `OrderService(OrderPublisher publisher, Tracer tracer)`; private final Tracer field; spanBuilder("OrderService.place") + setSpanKind(INTERNAL); UUID + publisher.publish + return INSIDE try block; D-03 catch + finally span.end()  |
| `producer-service/.../messaging/OrderPublisher.java`                                         | Tracer-injected; publish(...) wrapped in PRODUCER span with 4 messaging semconv attrs (D-11 + RESEARCH FLAG #1)               | VERIFIED    | 85 lines; constructor `OrderPublisher(RabbitTemplate rabbitTemplate, Tracer tracer)`; spanBuilder("orders.created publish") + setSpanKind(PRODUCER); 4 attrs MESSAGING_SYSTEM=RABBITMQ value enum, MESSAGING_DESTINATION_NAME=RabbitConfig.QUEUE, MESSAGING_OPERATION_TYPE=SEND value enum, MESSAGING_RABBITMQ_DESTINATION_ROUTING_KEY=RabbitConfig.ROUTING_KEY; teaching comment about semconv 1.40.0 rename present |
| `consumer-service/.../messaging/OrderListener.java`                                          | Tracer-injected; onOrder(...) wrapped in CONSUMER span with `setParent(Context.root())` (D-10) + verbatim teaching comment    | VERIFIED    | 81 lines; constructor `OrderListener(ProcessingService processingService, Tracer tracer)`; spanBuilder("orders.created process") + setParent(Context.root()) + setSpanKind(CONSUMER) + 3 messaging attrs; verbatim D-10 4-line teaching comment present (lines 47-50) including "propagator.extract(Context.root(), messageProperties, getter)"; LOG.info preserved INSIDE try block |
| `consumer-service/.../domain/ProcessingService.java`                                         | Tracer-injected; process(...) wrapped in INTERNAL span "ProcessingService.process"                                            | VERIFIED    | 54 lines; constructor `ProcessingService(Tracer tracer)`; spanBuilder("ProcessingService.process") + setSpanKind(INTERNAL); Phase 1 placeholder comments preserved INSIDE try block; D-03 catch + finally span.end()                                  |
| `README.md` `## Reading the code` section                                                    | DOC-03 pointer to both OtelSdkConfiguration.java + HttpServerSpanFilter.java                                                  | VERIFIED    | Section present at L79; 3 intra-repo links; mentions HttpServerSpanFilter; closing paragraph names 5 span sites and the D-01 template                                                                                                                |
| `README.md` `## Why is OtelSdkConfiguration.java duplicated?` section                        | DOC-05 callout — single paragraph, names duplication intentional, warns against @AutoConfiguration                            | VERIFIED    | Section present at L90; contains "duplicated per service on purpose", "@AutoConfiguration", "hide one of the two readings"                                                                                                                              |
| `README.md` Workshop checkpoints — step-02-traces marked **Current.**                        | step-02-traces description includes "intentional setup"; **Current.** marker moved from step-01-baseline                       | VERIFIED    | L70 step-01-baseline lacks **Current.**; L71 step-02-traces has **Current.** + "intentional setup for the Phase 3 propagation lesson"                                                                                                                  |
| Annotated git tag `refs/tags/step-02-traces`                                                 | Annotated (NOT lightweight) tag at SHA where all 6 success criteria are simultaneously green                                  | VERIFIED    | `git for-each-ref refs/tags/step-02-traces` returns `tag` (annotated); SHA dac865f; tag message lists all 6 success criteria explicitly                                                                                                              |

### Key Link Verification

| From                                              | To                                                                  | Via                                                                                       | Status     | Details                                                                                                                                                                                                                              |
| ------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Both pom.xml service modules                       | parent pom.xml `<dependencyManagement>` (BOM-managed versions)       | No `<version>` tag on opentelemetry-api/-sdk/-exporter-otlp; managed via opentelemetry-bom:1.61.0 | WIRED      | `mvn dependency:tree` resolves all 3 BOM-managed artifacts at 1.61.0; `mvn install` enforcer rule `dependencyConvergence` passes                                                                                                     |
| OtelSdkConfiguration.@Bean openTelemetry()         | OTEL_EXPORTER_OTLP_ENDPOINT env var                                   | `Optional.ofNullable(System.getenv(...)).orElse("http://localhost:4317")` fed to `OtlpGrpcSpanExporter.builder().setEndpoint(...)` | WIRED      | Both files line ~114-118 read env var with fallback; mise.toml line 22 pre-wires the var                                                                                                                                              |
| OtelSdkConfiguration.@Bean(destroyMethod="close") | BatchSpanProcessor flush on shutdown                                  | `OpenTelemetrySdk.close()` → `shutdown().join(10s)` → `BatchSpanProcessor.worker.shutdown()` | WIRED      | `@Bean(destroyMethod = "close")` declared on `OpenTelemetry openTelemetry()` factory in both files; SUMMARY 02-06 confirmed BEFORE/AFTER trace-count proof                                                                              |
| HttpServerSpanFilter (producer-only)              | Spring MVC filter chain                                               | Spring Boot 3.4 auto-discovers @Bean Filter; @Bean factory in OtelSdkConfiguration line 210-213 | WIRED      | Filter is constructed via @Bean factory; class extends OncePerRequestFilter — Spring auto-wraps via FilterRegistrationBean; SUMMARY 02-02 verified via Tempo trace POST /orders → SERVER span                                          |
| OrderService → OrderPublisher (intra-producer)    | INTERNAL span makeCurrent() Scope keeps active during PRODUCER span | return statement INSIDE try block; PRODUCER span builder picks up parent via Context.current() | WIRED      | OrderService.place line 51 `return orderId` is INSIDE try block (between L48 try and L52 catch); SUMMARY 02-04 confirmed parent-child chain SERVER → INTERNAL → PRODUCER in trace 25eb831a...                                          |
| OrderListener → ProcessingService (intra-consumer)| CONSUMER span makeCurrent() Scope keeps active during INTERNAL span | processingService.process(message) INSIDE try block at L68; INTERNAL spanBuilder picks up Context.current() | WIRED      | SUMMARY 02-05 confirmed: `INTERNAL.parentSpanId == CONSUMER.spanId` in trace 56093b9c...                                                                                                                                              |
| OrderListener CONSUMER span                       | Phase 3 `propagator.extract(...)` replacement                         | Multi-line teaching comment at L47-50 previewing the Phase 3 idiom verbatim per D-10        | WIRED      | Comment text matches CONTEXT.md D-10 verbatim; "propagator.extract(Context.root(), messageProperties, getter)" present                                                                                                                |
| `git tag step-02-traces`                          | Working two-trace state                                               | Tag points at SHA dac865f; verified annotated; commit contains all 3 config files + 4 instrumented Java files | WIRED      | `git ls-tree step-02-traces` shows: producer/HttpServerSpanFilter.java + producer/OtelSdkConfiguration.java + consumer/OtelSdkConfiguration.java; instrumentation files (OrderService, OrderPublisher, OrderListener, ProcessingService) all carry their span instrumentation at the tagged commit |

### Data-Flow Trace (Level 4)

| Artifact                       | Data Variable                            | Source                                                              | Produces Real Data                       | Status                  |
| ------------------------------ | ---------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------- | ----------------------- |
| OtelSdkConfiguration (both)    | `endpoint` (String)                      | `System.getenv("OTEL_EXPORTER_OTLP_ENDPOINT")` with fallback         | YES (mise.toml pre-wires; default fallback exists) | FLOWING                 |
| OtelSdkConfiguration (both)    | `Resource resource`                      | `Resource.getDefault().merge(Resource.create(Attributes.builder()...))` | YES (4 semconv constants populated)      | FLOWING                 |
| HttpServerSpanFilter           | SERVER span attribute values             | `request.getMethod()`, `request.getRequestURI()`, `request.getServerName()`, etc. | YES (live HttpServletRequest)            | FLOWING                 |
| OrderService.place             | `String orderId`                         | `UUID.randomUUID().toString()`                                       | YES                                      | FLOWING                 |
| OrderPublisher.publish         | PRODUCER span attribute values           | `MessagingSystemIncubatingValues.RABBITMQ`, `RabbitConfig.QUEUE`, etc. | YES (semconv-aligned enum + constants)    | FLOWING                 |
| OrderListener.onOrder          | CONSUMER span; `Object orderId`          | `message.get("orderId")`; semconv enums                               | YES (live AMQP body via @RabbitListener)  | FLOWING                 |
| ProcessingService.process      | INTERNAL span; (no business state)       | (Phase 1 placeholder body — Phase 3 adds APP-04)                      | YES (span itself flows; body intentionally empty per CONTEXT.md) | FLOWING (span only)     |

All wired artifacts produce real data. ProcessingService.process has an intentionally empty body (Phase 1 placeholder per CONTEXT.md `<deferred>`); the INTERNAL span itself still emits and is captured in Tempo (SUMMARY 02-05 confirms).

### Behavioral Spot-Checks

| Behavior                                      | Command                                                                                                       | Result                                                                                              | Status |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------ |
| Reactor builds clean                          | `mvn -DskipTests -q install` (run by verifier)                                                                | exit 0; silent success                                                                              | PASS   |
| Phase 2 BOM invariant holds                   | `mise run verify:bom` (run by verifier)                                                                       | "Phase 2 baseline confirmed: one version per OpenTelemetry artifact across all modules."           | PASS   |
| Producer dependency:tree resolves correctly   | `mvn -pl producer-service dependency:tree -Dincludes=io.opentelemetry`                                         | api/sdk/exporter-otlp at 1.61.0; semconv at 1.40.0; semconv-incubating at 1.40.0-alpha — all present | PASS   |
| Both OtelSdkConfiguration.java files compile  | (covered by `mvn install`)                                                                                     | exit 0                                                                                              | PASS   |
| Annotated tag step-02-traces is annotated     | `git for-each-ref refs/tags/step-02-traces`                                                                    | `tag refs/tags/step-02-traces` (NOT `commit` — annotated)                                            | PASS   |
| Tag commit contains expected Java files       | `git ls-tree -r step-02-traces \| grep -E "(OtelSdkConfiguration\|HttpServerSpanFilter)\.java"`               | 3 files: producer's 2 + consumer's 1                                                                | PASS   |
| End-to-end trace generation (POST /orders)    | (Requires running stack; SUMMARY 02-06 captured proof)                                                          | SUMMARY: PT=f00aab... != CT=d7480... ; both service.name labels correct ; unknown_service:java=0    | SKIP — routed to human verification (live infra needed) |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                              | Status     | Evidence                                                                                                                                                                                                                                          |
| ----------- | ----------- | -------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TRACE-01    | 02-01, 02-02, 02-03 | Each service contains its own `OtelSdkConfiguration.java` that builds OpenTelemetrySdk manually          | SATISFIED  | Both files exist; manual `OpenTelemetrySdk.builder()...build()`; no agent, no autoconfigure, no shared library                                                                                                                                       |
| TRACE-02    | 02-02, 02-03 | Resource carries semconv 1.40.0 service.* + deployment.environment.name; never `unknown_service:java`     | SATISFIED  | Both files use `ServiceAttributes.SERVICE_NAME/NAMESPACE/INSTANCE_ID` + `DeploymentIncubatingAttributes.DEPLOYMENT_ENVIRONMENT_NAME`; SUMMARY 02-06 confirmed unknown_service:java count=0 in Tempo                                                  |
| TRACE-03    | 02-02, 02-03 | SdkTracerProvider with BatchSpanProcessor + OtlpGrpcSpanExporter to :4317 + explicit Sampler.parentBased(alwaysOn) | SATISFIED  | `SdkTracerProvider.builder().setSampler(Sampler.parentBased(Sampler.alwaysOn())).addSpanProcessor(BatchSpanProcessor.builder(OtlpGrpcSpanExporter...).build()).build()` chain present in both files; multi-paragraph sampler teaching comment with traceIdRatioBased(0.1) reference |
| TRACE-04    | 02-02, 02-03 | OpenTelemetrySdk registered as @Bean(destroyMethod="close")                                              | SATISFIED  | `@Bean(destroyMethod = "close")` on `OpenTelemetry openTelemetry()` factory in both files; SUMMARY 02-06 confirmed BEFORE/AFTER trace count grew across SIGTERM (1s shutdown latency)                                                              |
| TRACE-05    | 02-02       | SERVER span on POST /orders with HTTP semconv attrs                                                       | SATISFIED  | HttpServerSpanFilter sets HTTP_REQUEST_METHOD, URL_PATH, URL_SCHEME, SERVER_ADDRESS, SERVER_PORT, HTTP_ROUTE, HTTP_RESPONSE_STATUS_CODE — all from `io.opentelemetry.semconv.*` constants                                                          |
| TRACE-06    | 02-04, 02-05 | INTERNAL span wraps each domain method (OrderService.place, ProcessingService.process)                   | SATISFIED  | Both methods wrapped in D-01 inline template with `setSpanKind(SpanKind.INTERNAL)`; span names "OrderService.place" + "ProcessingService.process"                                                                                                   |
| TRACE-07    | 02-04       | PRODUCER span wraps publish call with messaging semconv attrs                                            | SATISFIED  | OrderPublisher.publish uses spanBuilder("orders.created publish") + setSpanKind(PRODUCER) + 4 messaging attrs (system, destination_name, operation_type=SEND value enum, rabbitmq.destination.routing_key); uses NEW MESSAGING_OPERATION_TYPE not deprecated MESSAGING_OPERATION |
| TRACE-08    | 02-05       | CONSUMER span wraps listener handler with messaging semconv attrs (operation_type=process)               | SATISFIED  | OrderListener.onOrder uses spanBuilder("orders.created process") + setSpanKind(CONSUMER) + 3 messaging attrs (system, destination_name, operation_type=PROCESS value enum); .setParent(Context.root()) per D-10                                    |
| DOC-03      | 02-02, 02-03, 02-06 | OtelSdkConfiguration.java heavily commented (the code IS the textbook); README pointer                  | SATISFIED  | producer = 137 comment lines, consumer = 131 comment lines (both >= 40 required); multi-paragraph sampler tradeoff present in both. README.md "## Reading the code" section points to both files                                                    |
| DOC-05      | 02-06       | README explicitly states per-service duplication of OtelSdkConfiguration is intentional                  | SATISFIED  | README.md "## Why is OtelSdkConfiguration.java duplicated?" section contains "duplicated per service on purpose", "@AutoConfiguration", "hide one of the two readings"                                                                                |
| WORK-01 (step-02-traces) | 02-06 | Annotated git tag step-02-traces exists on main and reproduces this state                          | SATISFIED  | `git for-each-ref refs/tags/step-02-traces` returns `tag` (annotated); SHA dac865f; tag message documents all 6 Phase 2 success criteria; ls-tree confirms all expected files at the tagged commit                                                  |

**Total requirements:** 11 declared in PLAN frontmatters, all 11 SATISFIED.
**Orphaned requirements (in REQUIREMENTS.md mapped to Phase 2 but not in any plan):** None. All Phase 2 IDs in REQUIREMENTS.md (TRACE-01..08, DOC-03, DOC-05) appear in plan frontmatters.

### Anti-Patterns Found

| File                                                                  | Line  | Pattern                                                                                                                            | Severity | Impact                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `consumer-service/.../domain/ProcessingService.java`                  | 41-42 | Empty try-block body (Phase 1 placeholder comment only)                                                                            | INFO     | Intentional per CONTEXT.md `<deferred>` line 202 — Phase 3 wires APP-04 throw site here. The INTERNAL span itself still emits; this is not a stub of the SPAN, it's a PHASE-DEFERRED business body. INTERNAL span emission verified in SUMMARY 02-05            |
| `README.md`                                                           | 5     | "The current `main` branch as of `step-01-baseline` shows the **uninstrumented baseline**"                                          | WARNING  | (REVIEW CR-01 BLOCKER) — at the `step-02-traces` tag, this sentence is now FALSE. The OTel SDK is on the classpath. Plan 02-06 deferred the larger README rewrite to Phase 7 / DOC-01. The annotated tag preserves this contradiction permanently.                |
| `README.md`                                                           | 66    | "In Phase 1 there is **no telemetry** — ... `mise run verify:bom` should report zero OpenTelemetry libraries on the classpath"     | WARNING  | (REVIEW CR-01) — at `step-02-traces`, `mise run verify:bom` reports the OPPOSITE message ("Phase 2 baseline confirmed: one version per OpenTelemetry artifact across all modules"). The README's instruction conflicts with the actual output an attendee will see. |
| `README.md`                                                           | 96-98 | "The following are deliberate Phase 1 omissions" / "- No `OtelSdkConfiguration.java` (Phase 2)"                                    | WARNING  | (REVIEW CR-01) — `OtelSdkConfiguration.java` now EXISTS in both services. The "What's NOT here yet" list is stale at this tag.                                                                                                                                   |
| `producer-service/.../config/HttpServerSpanFilter.java`               | 66, 84 | `request.getRequestURI().startsWith("/actuator/")` — null-deref risk if URI is null in unusual dispatcher contexts                | INFO     | (REVIEW WR-02) — Servlet 5 spec allows null in error-dispatch edge cases. Workshop default (no context-path) makes this hypothetical, but the spec-compliance gap is real.                                                                                       |
| `*OtelSdkConfiguration.java` (both)                                   | ~114  | `Optional.ofNullable(getenv).orElse(...)` doesn't handle empty string                                                              | INFO     | (REVIEW WR-03) — empty `OTEL_EXPORTER_OTLP_ENDPOINT=""` would crash startup with `IllegalArgumentException: endpoint must start with http://`. Workshop default (mise.toml pre-wires correct value) makes this hypothetical.                                       |
| `producer-service/.../messaging/OrderPublisher.java`                  | 55-56 | `MESSAGING_DESTINATION_NAME` set to QUEUE name (`orders.created`) instead of EXCHANGE name on producer side                       | INFO     | (REVIEW WR-04) — Per OTel messaging semconv (RabbitMQ profile), producer-side MESSAGING_DESTINATION_NAME should be the exchange. The codebase intentionally uses the queue for symmetry with consumer (CONTEXT.md D-04 + D-11 explicitly authorize this); a workshop teaching tradeoff |
| `producer-service/.../config/HttpServerSpanFilter.java`               | 102   | `HTTP_ROUTE = path` (literal URI) instead of matched route template                                                                | INFO     | (REVIEW WR-05) — `http.route` should be the route template (`/orders/{id}`), not the literal URI. This codebase has only `/orders` (no path variables), so the bug is latent. Code comment at L72-75 acknowledges Spring's `@RequestMapping` route is unavailable pre-dispatch |
| `consumer-service/.../messaging/OrderListener.java`                   | 46-68 | No null/shape validation on incoming AMQP message                                                                                  | INFO     | (REVIEW WR-06) — workshop AMQP boundary doesn't validate the message map; bad payloads could loop in re-delivery. Out of scope for Phase 2 lesson per CONTEXT.md (Phase 3 adds the deterministic failure path)                                                    |
| `*OtelSdkConfiguration.java` (both)                                   | ~35   | Inconsistent arrow style: producer uses `->`, consumer uses `→` in JavaDoc per-service-duplication callout                        | INFO     | (REVIEW IN-01) — sixth, unintended difference between the two files. Workshop's `diff` walk-through will show character-level noise the README doesn't call out                                                                                                  |
| `*OtelSdkConfiguration.java` (both)                                   | ~166  | Forward-pointer references to `TracingMessagePostProcessor` / `TracingMessageListenerAdvice` not yet existing                     | INFO     | (REVIEW IN-04) — IDE will show these as red unresolved symbols at the `step-02-traces` checkpoint. Phase 3 lands the classes                                                                                                                                       |
| `*` (5 span sites)                                                    | catch | Catch swallows `Error` types (uses `RuntimeException \| ServletException \| IOException`)                                          | INFO     | (REVIEW WR-01) — `OutOfMemoryError` etc. would skip recordException + setStatus(ERROR), making span report success. Workshop volume is laptop-class so unlikely; cumulative across 5 sites                                                                       |

**Categorized:**
- 0 BLOCKERS (the README CR-01 issue, while called BLOCKER in the REVIEW, does not break the phase goal — the tag exists and reproduces the state; the README is misleading at that tag but not goal-failing. Routed to human verification for read-through)
- 3 WARNINGS (README staleness at L5, L66, L96-98 — same root cause; called CR-01 in REVIEW)
- 10 INFO (REVIEW WR-01..08 + IN-01..04 — REVIEW-flagged items that are documented in CONTEXT.md as deferred or are pedagogical tradeoffs)

### Human Verification Required

The following 5 items REQUIRE a running stack (producer + consumer + RabbitMQ + grafana/otel-lgtm) to assert. Each was confirmed during plan execution (captured in 02-04, 02-05, 02-06 SUMMARYs) but cannot be re-asserted statically by the verifier; they require human re-run against live infra to be re-confirmed at the verification timestamp:

#### 1. TWO distinct trace IDs in Tempo per POST /orders

**Test:** Bring up infra (`mise run infra:up`); start both apps (`mise run dev`); send `mise run demo:order` (or `curl -X POST http://localhost:8080/orders -H 'Content-Type: application/json' -d '{"sku":"WIDGET-1","quantity":3}'`); open Grafana → Tempo Explore (http://localhost:3000, admin/admin); search Service Name = `order-producer`, then `order-consumer`.
**Expected:** Both queries return at least one trace within ~12 seconds (BSP flush). Note both trace IDs (visible at the top of the trace detail panel). They MUST be different. Search Service Name = `unknown_service:java` and confirm zero traces.
**Why human:** Live Tempo telemetry inspection. SUMMARY 02-06 captured proof at session-time but telemetry has flushed.

#### 2. Ctrl-C flushes the last span batch

**Test:** With both apps running, send a fresh `POST /orders`. Immediately Ctrl-C the producer terminal (or `kill <pid>`). Wait ~12 seconds. Re-search Tempo for `service.name=order-producer`.
**Expected:** The trace from the just-emitted POST is visible — the BSP flushed it during the close().shutdown(10s) cascade. Producer process exits within ~12 seconds.
**Why human:** Requires SIGTERM + Tempo trace count delta observation against a running process.

#### 3. Producer span attributes correctly populated in Tempo

**Test:** Click into a producer trace in Tempo. Inspect each span:
- SERVER span "POST /orders": confirm 7 HTTP attrs (`http.request.method=POST`, `url.path=/orders`, `url.scheme=http`, `server.address=localhost`, `server.port=8080`, `http.route=/orders`, `http.response.status_code=202`)
- INTERNAL span "OrderService.place": no semconv attrs mandated
- PRODUCER span "orders.created publish": confirm 4 messaging attrs (`messaging.system=rabbitmq`, `messaging.destination.name=orders.created`, `messaging.operation.type=send`, `messaging.rabbitmq.destination.routing_key=order.created`). Confirm the bare deprecated `messaging.operation` key is ABSENT.
**Expected:** All attribute keys/values present as listed. The "operation_type" key (NOT bare "operation") is the deciding artifact for RESEARCH FLAG #1 compliance.
**Why human:** Visual span attribute inspection in Grafana UI.

#### 4. Consumer's CONSUMER span has empty parentSpanId

**Test:** Click into a consumer trace in Tempo. Inspect the root CONSUMER span "orders.created process".
**Expected:** parentSpanId is EMPTY (or all-zeros). The INTERNAL span "ProcessingService.process" has parentSpanId equal to the CONSUMER's spanId. The trace ID does NOT match any producer trace.
**Why human:** Visual parent-chain assertion in Tempo's span detail panel; SUMMARY 02-05 captured proof at trace 56093b9c... but telemetry has flushed.

#### 5. RabbitMQ message lacks `traceparent` header

**Test:** Open RabbitMQ Mgmt UI (http://localhost:15672, guest/guest) → Queues → click `orders.created` → "Get messages" with Ack mode = "Reject requeue true", Messages = 1.
**Expected:** The peeked message contains a JSON payload with `sku`, `quantity`, `orderId` keys. The message properties / headers section does NOT contain a `traceparent` header. (If you see one, propagation is accidentally working — Phase 2's broken state is broken in the wrong direction.)
**Why human:** Negative-evidence runtime check in the running broker.

### Gaps Summary

No actionable gaps blocking Phase 2 goal achievement. All 11 must-have truths VERIFIED, all 11 declared requirements SATISFIED, all 14 artifacts present and substantive, all 8 key links wired. The annotated tag `step-02-traces` exists at SHA dac865f and reproduces the state. The Phase 2 invariant (one version per OTel artifact) is preserved.

The 5 human verification items are NOT gaps — they are runtime telemetry confirmations that the static + summary evidence already attests but cannot be re-asserted programmatically without re-launching the stack. Each item was confirmed at plan-execution time (captured in 02-04 / 02-05 / 02-06 SUMMARYs) and is requested for re-confirmation at the verification timestamp.

The 13 anti-patterns in Step 7 (1 INFO from intentional Phase 1 placeholder; 3 WARNINGs from README CR-01 staleness; 9 INFO from REVIEW.md WR-01..08 / IN-01..04) are pedagogical tradeoffs documented in CONTEXT.md or hypothetical edge cases that don't affect Phase 2's broken-then-fixed-pedagogy goal. The most user-visible issue — README contradicting reality at the tag — is captured as a WARNING and is documented in `02-REVIEW.md` as a known pre-Phase-7 condition.

---

_Verified: 2026-05-01_
_Verifier: Claude (gsd-verifier)_
