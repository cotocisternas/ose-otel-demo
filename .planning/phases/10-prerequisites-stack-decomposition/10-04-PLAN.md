---
phase: 10-prerequisites-stack-decomposition
plan: 04
type: execute
wave: 2
depends_on: [10-02, 10-03]
files_modified:
  - docker-compose.yml
  - mise.toml
  - grafana/prometheus.yaml
autonomous: true
requirements:
  - STACK-01
  - STACK-02
  - STACK-03
  - STACK-04
  - STACK-05
tags:
  - docker-compose
  - mise
  - infrastructure-decomposition

must_haves:
  truths:
    - "`docker-compose.yml` defines exactly 10 services: 5 existing data services (rabbitmq, valkey, postgres, redis-exporter, postgres-exporter) + 5 new observability services (otel-collector, tempo, mimir, loki, grafana)"
    - "The `lgtm:` service block is REMOVED entirely from docker-compose.yml (D-08)"
    - "All 5 new image tags are exact-patch pins: `otel/opentelemetry-collector-contrib:0.151.0`, `grafana/tempo:2.10.5`, `grafana/mimir:3.0.6`, `grafana/loki:3.7.1`, `grafana/grafana:13.0.1`"
    - "5 new named volumes declared: `tempo-data`, `tempo-wal`, `mimir-data`, `loki-data`, `grafana-data`; old `lgtm-data` volume removed (D-09)"
    - "All host port mappings exposed per D-10: 4317, 4318, 13133, 8888, 8889 (Collector); 3200 (Tempo); 9009 (Mimir); 3100 (Loki); 3000 (Grafana)"
    - "depends_on semantics per D-16: collector→{tempo,mimir,loki}, tempo→mimir, loki→mimir, grafana→{tempo,mimir,loki} — all `service_started`"
    - "Each new service has a `healthcheck:` block per RESEARCH §Example 4 (wget --spider on /ready or /api/health or :13133/)"
    - "`grafana/prometheus.yaml` is DELETED via `git rm` (D-15 — its scrape config now lives in otelcol-config.yaml)"
    - "`mise run preflight` checks 14 ports: 3000, 4317, 4318, 5672, 15672, 15692, 6379, 5432, 3200, 9009, 3100, 13133, 8888, 8889 (D-11)"
    - "`mise run verify:datasources` task exists (D-03)"
    - "`mise run verify:images` task exists (D-14) — fails on floating tags"
  artifacts:
    - path: "docker-compose.yml"
      provides: "10-service workshop stack (5 data + 5 observability) with healthchecks, depends_on, named volumes, host port maps"
      contains: "otel-collector"
    - path: "mise.toml"
      provides: "Extended preflight (14 ports), new verify:datasources task, new verify:images task"
      contains: "verify:datasources"
    - path: "grafana/prometheus.yaml"
      provides: "DELETED — its scrape config migrated to otelcol-config.yaml"
  key_links:
    - from: "docker-compose.yml otel-collector service"
      to: "infra/observability/otelcol-config.yaml"
      via: "bind-mount ./infra/observability/otelcol-config.yaml:/etc/otelcol-contrib/config.yaml:ro"
      pattern: "otelcol-config.yaml:/etc/otelcol-contrib/config.yaml"
    - from: "docker-compose.yml tempo service"
      to: "infra/observability/tempo-config.yaml + tempo-data + tempo-wal volumes"
      via: "bind-mount + 2 named-volume mounts (F1-2 — separate WAL volume)"
      pattern: "tempo-config.yaml:/etc/tempo.yaml"
    - from: "docker-compose.yml grafana service"
      to: "grafana/datasources.yaml + grafana/dashboards/dashboards.yaml + grafana/dashboards/*.json"
      via: "3 bind-mounts at standalone Grafana 13 paths (Pitfall 8)"
      pattern: "grafana/datasources.yaml:/etc/grafana/provisioning/datasources/"
    - from: "mise.toml verify:images task"
      to: "docker-compose.yml image: lines"
      via: "bash regex grep gating floating tags"
      pattern: "verify:images"
---

<objective>
Replace the v1.0 single-container `lgtm:` block in `docker-compose.yml` with five separate observability services (otel-collector, tempo, mimir, loki, grafana) wired together via Docker service-name DNS, named volumes, healthchecks, and depends_on chains. Five new named volumes replace `lgtm-data`. Delete the orphan `grafana/prometheus.yaml` (its scrape config migrated to otelcol-config.yaml in Plan 02). Extend `mise.toml`: grow `preflight`'s port list to 14 entries (D-11); add `verify:datasources` (D-03) and `verify:images` (D-14) tasks following the `verify:bom` structural template.

Purpose: Wave 2 — depends on Plans 02 + 03 because docker-compose mounts those YAMLs. Once landed, Wave 3 (Plan 05) can run end-to-end smoke and dashboard verification.

Output: One major docker-compose.yml rewrite (the lgtm block replaced by 5 service blocks; volumes section grows from 1 to 5 entries), one moderate mise.toml extension, one file deletion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-prerequisites-stack-decomposition/10-CONTEXT.md
@.planning/phases/10-prerequisites-stack-decomposition/10-RESEARCH.md
@.planning/phases/10-prerequisites-stack-decomposition/10-PATTERNS.md
@docker-compose.yml
@mise.toml
@grafana/prometheus.yaml

<interfaces>
<!-- Plan 02 + Plan 03 outputs that Plan 04 mounts: -->
<!--   infra/observability/otelcol-config.yaml   → /etc/otelcol-contrib/config.yaml (Collector)                  -->
<!--   infra/observability/tempo-config.yaml     → /etc/tempo.yaml                  (Tempo)                       -->
<!--   infra/observability/mimir-config.yaml     → /etc/mimir.yaml                  (Mimir)                       -->
<!--   infra/observability/loki-config.yaml      → /etc/loki/local-config.yaml      (Loki — overrides bundled)    -->
<!--   infra/observability/loki-rules/           → /loki/rules:ro                   (D-07 — empty rules dir)      -->
<!--   grafana/datasources.yaml                  → /etc/grafana/provisioning/datasources/datasources.yaml (Grafana)-->
<!--   grafana/dashboards/dashboards.yaml        → /etc/grafana/provisioning/dashboards/dashboards.yaml (Grafana) -->
<!--   grafana/dashboards/                       → /var/lib/grafana/dashboards:ro   (Grafana — JSON files)        -->

<!-- Existing v1.0 services that stay UNCHANGED in docker-compose.yml: -->
<!--   rabbitmq (lines 6-28)  ·  valkey (lines 31-42)  ·  postgres (lines 45-60)                                  -->
<!--   redis-exporter (lines 68-76)  ·  postgres-exporter (lines 81-89)                                            -->

<!-- Healthcheck commands (live-verified per RESEARCH §Example 4): -->
<!--   tempo:        wget --spider --quiet http://localhost:3200/ready                                            -->
<!--   mimir:        wget --spider --quiet http://localhost:9009/ready                                            -->
<!--   loki:         wget --spider --quiet http://localhost:3100/ready                                            -->
<!--   grafana:      wget --spider --quiet http://localhost:3000/api/health  (NOT /ready)                          -->
<!--   collector:    wget --spider --quiet http://localhost:13133/                                                  -->
<!-- Cadence: interval 10s, timeout 5s, retries 10, start_period {15s,20s,20s,20s,30s} per backend.               -->

<!-- mise.toml templates (existing — extended in this plan): -->
<!--   [tasks.preflight] port-list loop (lines 57-64) → grow from 5 ports to 14 ports                              -->
<!--   [tasks."verify:bom"] (lines 247-290) → structural template for verify:datasources + verify:images          -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite docker-compose.yml — replace lgtm: with 5 observability services + 5 named volumes</name>
  <read_first>
    - docker-compose.yml (entire current 6-service file — lines 1-122)
    - .planning/phases/10-prerequisites-stack-decomposition/10-CONTEXT.md (D-08, D-09, D-10, D-16 — single compose file, 5 named volumes, port map, depends_on semantics)
    - .planning/phases/10-prerequisites-stack-decomposition/10-RESEARCH.md (§Example 4 lines 944-1000 — verified healthcheck commands per backend; §Pitfall 6 — atomic lgtm-removal to avoid port 4317 collision; §Pitfall 8 — Grafana 13 standalone provisioning paths)
    - .planning/phases/10-prerequisites-stack-decomposition/10-PATTERNS.md (§docker-compose.yml lines 342-498 — healthcheck cadence template, lgtm-block lift-and-keep targets, named-volume pattern)
    - infra/observability/otelcol-config.yaml (Plan 02 output — verify the bind-mount target path matches what's expected)
    - infra/observability/tempo-config.yaml (Plan 02 output)
    - infra/observability/mimir-config.yaml (Plan 02 output)
    - infra/observability/loki-config.yaml (Plan 02 output)
    - grafana/datasources.yaml (Plan 03 output)
    - grafana/dashboards/dashboards.yaml (Plan 03 — modified options.path)
  </read_first>
  <behavior>
    - DELETE the entire `lgtm:` service block (current lines 91-119 inclusive)
    - DELETE the `lgtm-data:` entry under `volumes:` and add 5 new named volumes
    - INSERT a `# ===== Observability stack =====` comment-block divider after the `postgres-exporter` service
    - INSERT 5 new service blocks: `otel-collector`, `tempo`, `mimir`, `loki`, `grafana` (in this order — collector first since it's the ingress, grafana last since it depends on the rest)
    - Preserve the v1.0 lgtm Grafana env vars (`GF_SECURITY_ADMIN_USER=admin`, `GF_SECURITY_ADMIN_PASSWORD=admin`, `GF_AUTH_ANONYMOUS_ENABLED=true`, `GF_AUTH_ANONYMOUS_ORG_ROLE=Admin`, `GF_AUTH_DISABLE_LOGIN_FORM=true`) on the new `grafana:` service
    - Each new service: pinned image tag, `container_name: ose-otel-<name>`, healthcheck, depends_on (per D-16), volumes, ports, restart: unless-stopped
  </behavior>
  <action>
**Edit operation:** delete current lines 91-119 (the entire `lgtm:` block — start at the line `  lgtm:` and end at the line `    restart: unless-stopped` immediately preceding the empty line before `volumes:`). Also delete the existing `volumes:` section at lines 121-122 (`volumes:` + `  lgtm-data:`).

In the SAME write, insert the new content. Final structure of `docker-compose.yml` MUST be:

```yaml
# Infrastructure only — apps run on host via mise tasks.
# Phase 1: lgtm container runs but receives ZERO telemetry (no OTel libs in apps).
# Phase 10: lgtm REPLACED by 5 separate production-shape containers (otel-collector,
# tempo, mimir, loki, grafana). Pinned image tags for workshop reproducibility —
# `mise run verify:images` enforces no floating tags (D-14 / X-3).

services:
  rabbitmq:
    # ... UNCHANGED ...

  # Phase 8: Valkey (Redis-compatible cache) — producer idempotency
  valkey:
    # ... UNCHANGED ...

  # Phase 8: PostgreSQL — consumer persistence
  postgres:
    # ... UNCHANGED ...

  # Step 9 (quick-260502-j00): redis_exporter scrapes Valkey ...
  redis-exporter:
    # ... UNCHANGED ...

  # Step 9 (quick-260502-j00): postgres_exporter ...
  postgres-exporter:
    # ... UNCHANGED ...

  # ===== Observability stack (Phase 10 / D-08) =====
  # Five containers replace v1.0's grafana/otel-lgtm:0.26.0 all-in-one.
  # Pedagogical headline: "production observability is five separate services with
  # explicit pipelines, and replacing the all-in-one container changes ZERO Java
  # lines (STACK-03 — OTLP endpoint :4317 unchanged in OtelSdkConfiguration files)."

  otel-collector:
    # WHY pinned: collector-contrib v0.151.0 (Apr 2026); D-14 verify:images enforces.
    image: otel/opentelemetry-collector-contrib:0.151.0
    container_name: ose-otel-collector
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    volumes:
      - ./infra/observability/otelcol-config.yaml:/etc/otelcol-contrib/config.yaml:ro
    ports:
      - "4317:4317"      # OTLP gRPC ingest (debug: grpcurl -plaintext localhost:4317 list)
      - "4318:4318"      # OTLP HTTP ingest (debug: curl localhost:4318/v1/traces)
      - "13133:13133"    # health_check extension (debug: curl localhost:13133/)
      - "8888:8888"      # Collector self-metrics (debug: curl localhost:8888/metrics)
      - "8889:8889"      # prometheus_exporter receiver (Phase 11 use)
    healthcheck:
      # WHY: health_check extension exposes :13133 returning 200 OK on readiness.
      # Requires the `extensions: [health_check]` block in otelcol-config.yaml (Plan 02).
      test: ["CMD", "wget", "--spider", "--quiet", "http://localhost:13133/"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    depends_on:
      # WHY (D-16): service_started — these don't ship Docker HEALTHCHECKs
      # by default; rely on Collector's own retry-on-export to handle backend warmup.
      tempo:
        condition: service_started
      mimir:
        condition: service_started
      loki:
        condition: service_started
    restart: unless-stopped

  tempo:
    # WHY pinned: grafana/tempo:2.10.5 (Apr 2026); aligns with v0.26.0-era lgtm.
    image: grafana/tempo:2.10.5
    container_name: ose-otel-tempo
    command: ["-config.file=/etc/tempo.yaml"]
    volumes:
      - ./infra/observability/tempo-config.yaml:/etc/tempo.yaml:ro
      - tempo-data:/var/tempo                       # WHY (D-09): long-term sealed trace blocks
      - tempo-wal:/var/tempo/wal                    # WHY (D-09 / F1-2): WAL persists across restarts
                                                     #   so metrics_generator state survives infra:down/up
                                                     #   (otherwise service-graph re-primes for 1-5 min)
    ports:
      - "3200:3200"      # Tempo HTTP API (debug: curl localhost:3200/api/echo)
                         #                debug: curl 'localhost:3200/api/search?tags=service.name=order-producer'
    healthcheck:
      # WHY: Tempo's /ready returns 200 once the ingester+distributor are accepting writes.
      test: ["CMD", "wget", "--spider", "--quiet", "http://localhost:3200/ready"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s   # Tempo cold-start: ~5-15s observed
    depends_on:
      # WHY (D-16): Tempo's metrics_generator.remote_write retries on transient failures
      # while Mimir warms up — service_started is sufficient.
      mimir:
        condition: service_started
    restart: unless-stopped

  mimir:
    # WHY pinned: grafana/mimir:3.0.6 (Apr 2026); first 3.x line that ships
    # `multitenancy_enabled` (the v3 spelling of the v2 `auth_enabled` key).
    image: grafana/mimir:3.0.6
    container_name: ose-otel-mimir
    command: ["-config.file=/etc/mimir.yaml"]
    volumes:
      - ./infra/observability/mimir-config.yaml:/etc/mimir.yaml:ro
      - mimir-data:/data/mimir                      # WHY (D-09): TSDB blocks + ruler storage + compactor scratch
    ports:
      - "9009:9009"      # Mimir HTTP API (debug: curl localhost:9009/ready, /metrics)
                         #                debug: curl localhost:9009/api/v1/push  (POST PRW from outside)
    healthcheck:
      # WHY: Mimir's /ready returns 200 once all ring members (in-memory) are up.
      test: ["CMD", "wget", "--spider", "--quiet", "http://localhost:9009/ready"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s   # Mimir cold-start: ~10-25s observed (multi-component init)
    restart: unless-stopped

  loki:
    # WHY pinned: grafana/loki:3.7.1 (Mar 2026); supports OTLP-native receive on /otlp.
    image: grafana/loki:3.7.1
    container_name: ose-otel-loki
    command: ["-config.file=/etc/loki/local-config.yaml"]
    volumes:
      - ./infra/observability/loki-config.yaml:/etc/loki/local-config.yaml:ro
      - ./infra/observability/loki-rules:/loki/rules:ro     # WHY (D-07): bind-mount source for ruler;
                                                             #   read-only so Loki's internal cache
                                                             #   writes can't conflict (Pitfall 7).
                                                             #   Empty in Phase 10 (.gitkeep placeholder);
                                                             #   Phase 13 lands order-errors.yaml here.
      - loki-data:/loki                                      # WHY (D-09): chunks + cache + ring state
    ports:
      - "3100:3100"      # Loki HTTP API (debug: curl localhost:3100/ready)
                         #                debug: curl localhost:3100/loki/api/v1/rules
    healthcheck:
      # WHY: Loki's /ready returns 200 once schema_config + storage init complete.
      test: ["CMD", "wget", "--spider", "--quiet", "http://localhost:3100/ready"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s   # Loki cold-start: ~5-15s observed
    depends_on:
      mimir:
        condition: service_started   # WHY (D-16): Loki's ruler.remote_write retries on Mimir warmup
    restart: unless-stopped

  grafana:
    # WHY pinned: grafana/grafana:13.0.1 (Apr 2026); standalone variant.
    image: grafana/grafana:13.0.1
    container_name: ose-otel-grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      # WHY: Phase 7 D-01 carryforward — workshop UX requires zero-click access
      # to dashboards (anonymous Admin enabled). Production deployments require
      # real auth — README Step 10 must reiterate this.
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
    volumes:
      # WHY (Pitfall 8): standalone Grafana 13 reads provisioning from
      # /etc/grafana/provisioning/{datasources,dashboards}/ — these paths
      # do NOT exist inside lgtm and the lgtm-internal /otel-lgtm/grafana/conf/...
      # paths do NOT exist inside standalone Grafana. The split is real.
      - ./grafana/datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro
      - ./grafana/dashboards/dashboards.yaml:/etc/grafana/provisioning/dashboards/dashboards.yaml:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro    # WHY: dashboard JSON files
                                                                 # — D-01 makes them portable
                                                                 # (UIDs match datasources.yaml verbatim)
      - grafana-data:/var/lib/grafana                          # WHY (D-09): persists Grafana state
                                                                 #   across infra:down/up (user prefs,
                                                                 #   recently-viewed, etc.)
    ports:
      - "3000:3000"      # Grafana UI (anonymous Admin; admin/admin also works)
    healthcheck:
      # WHY: Grafana's /api/health returns {"database": "ok"} once provisioning finished.
      # NOT /ready — Grafana doesn't expose that path.
      test: ["CMD", "wget", "--spider", "--quiet", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s   # Grafana cold-start: ~5-20s (datasource + dashboard provisioning)
    depends_on:
      tempo:
        condition: service_started
      mimir:
        condition: service_started
      loki:
        condition: service_started
    restart: unless-stopped

# Five named volumes per D-09 — replacing v1.0's single lgtm-data.
# `infra:reset` (docker compose down -v) wipes all five.
# `infra:down` / `infra:up` preserves them — workshop attendees can take a break,
# restart, traces still in Tempo, dashboards still in Grafana.
volumes:
  tempo-data:        # WHY: Tempo long-term sealed trace blocks (/var/tempo/blocks)
  tempo-wal:         # WHY: Tempo WAL + metrics_generator state (/var/tempo/wal) — F1-2 separation
  mimir-data:        # WHY: Mimir TSDB + compactor + ruler storage (/data/mimir)
  loki-data:         # WHY: Loki chunks + cache + ring state (/loki)
  grafana-data:      # WHY: Grafana SQLite + user prefs + recently-viewed (/var/lib/grafana)
```

Validate the file with `docker compose config`:
```bash
cd /home/coto/dev/demo/ose-otel-demo
docker compose config --quiet 2>&1 | tee /tmp/phase10-compose-validate.log
echo "exit=$?"
```
Expect exit 0 (no errors). The `--quiet` form suppresses output on success.

Verify service count and image pins:
```bash
docker compose config --services | sort | tee /tmp/phase10-services.log
# Expect 10 services: grafana, loki, mimir, otel-collector, postgres, postgres-exporter, rabbitmq, redis-exporter, tempo, valkey
test "$(wc -l < /tmp/phase10-services.log)" -eq 10 || { echo 'expected 10 services'; cat /tmp/phase10-services.log; exit 1; }

docker compose config --images | sort | tee /tmp/phase10-images.log
# Expect 10 image entries (each service has a pinned image)
```

Bring up the new stack and confirm all 10 reach ready:
```bash
docker compose down 2>&1   # ensure no stale lgtm/etc holding ports (Pitfall 6)
docker compose up -d --wait --wait-timeout 180 2>&1 | tee /tmp/phase10-compose-up.log
docker compose ps --format json | jq -r '.[] | "\(.Service) \(.State) \(.Health // "N/A")"' | tee /tmp/phase10-compose-ps.log
# Assert: 5 obs services + 5 data services all "running"; new obs services show healthy
test "$(docker compose ps --status running --format json | jq -s 'length')" -eq 10 || { echo 'expected 10 running services'; exit 1; }
```
  </action>
  <verify>
    <automated>cd /home/coto/dev/demo/ose-otel-demo && docker compose config --quiet 2>&1 && test "$(docker compose config --services | wc -l)" -eq 10 && grep -q 'otel/opentelemetry-collector-contrib:0.151.0' docker-compose.yml && grep -q 'grafana/tempo:2.10.5' docker-compose.yml && grep -q 'grafana/mimir:3.0.6' docker-compose.yml && grep -q 'grafana/loki:3.7.1' docker-compose.yml && grep -q 'grafana/grafana:13.0.1' docker-compose.yml && ! grep -q 'grafana/otel-lgtm' docker-compose.yml && ! grep -E '^\s*lgtm:\s*$' docker-compose.yml && grep -q 'tempo-data:' docker-compose.yml && grep -q 'tempo-wal:' docker-compose.yml && grep -q 'mimir-data:' docker-compose.yml && grep -q 'loki-data:' docker-compose.yml && grep -q 'grafana-data:' docker-compose.yml && ! grep -q 'lgtm-data:' docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0 (no parse errors)
    - `docker compose config --services | wc -l` returns 10
    - Service names returned by `docker compose config --services` (sorted): exactly `grafana, loki, mimir, otel-collector, postgres, postgres-exporter, rabbitmq, redis-exporter, tempo, valkey`
    - File contains literal `image: otel/opentelemetry-collector-contrib:0.151.0`
    - File contains literal `image: grafana/tempo:2.10.5`
    - File contains literal `image: grafana/mimir:3.0.6`
    - File contains literal `image: grafana/loki:3.7.1`
    - File contains literal `image: grafana/grafana:13.0.1`
    - File does NOT contain literal `grafana/otel-lgtm` (anti-pattern absent — Pitfall 6 mitigation)
    - File does NOT contain a service block named `lgtm:` (regex `^\s*lgtm:\s*$` returns 0 hits)
    - File contains all 5 new named volumes: `tempo-data:`, `tempo-wal:`, `mimir-data:`, `loki-data:`, `grafana-data:`
    - File does NOT contain `lgtm-data:` (anti-pattern absent)
    - File contains `# ===== Observability stack` divider comment (D-08 visual separator)
    - File contains all D-10 host port mappings: `"4317:4317"`, `"4318:4318"`, `"13133:13133"`, `"8888:8888"`, `"8889:8889"`, `"3200:3200"`, `"9009:9009"`, `"3100:3100"`, `"3000:3000"`
    - Each new service has a healthcheck block (the literal `healthcheck:` keyword appears at least 5 NEW times beyond the 3 existing v1.0 healthchecks — total `grep -c '^\s\+healthcheck:' docker-compose.yml` ≥ 8)
    - tempo service block contains `tempo-data:/var/tempo` AND `tempo-wal:/var/tempo/wal` (F1-2 — separate volume mounts)
    - grafana service block contains all THREE provisioning bind-mounts: `:/etc/grafana/provisioning/datasources/datasources.yaml:ro`, `:/etc/grafana/provisioning/dashboards/dashboards.yaml:ro`, `:/var/lib/grafana/dashboards:ro` (Pitfall 8 — standalone Grafana 13 paths)
    - grafana service block preserves all 5 v1.0 GF_* env vars
    - Live `docker compose up -d --wait` command brings all 10 services to running state within 180s timeout
    - `docker compose ps --status running --format json | jq -s 'length'` returns 10
  </acceptance_criteria>
  <done>docker-compose.yml decomposes lgtm into 5 services with healthchecks, depends_on, volumes, ports; live `docker compose up --wait` succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete grafana/prometheus.yaml (D-15) and update mise.toml (D-11 preflight + D-03 verify:datasources + D-14 verify:images)</name>
  <read_first>
    - mise.toml (entire file — preflight task at lines 37-67, verify:bom at lines 247-290)
    - grafana/prometheus.yaml (full 76-line file — being DELETED; its scrape_configs block already migrated to otelcol-config.yaml in Plan 02)
    - .planning/phases/10-prerequisites-stack-decomposition/10-CONTEXT.md (D-03, D-11, D-14, D-15)
    - .planning/phases/10-prerequisites-stack-decomposition/10-RESEARCH.md (§Example 2 lines 850-887 verify:datasources; §Example 3 lines 895-941 verify:images; §Example 5 lines 1007-1015 preflight extension; §Cold-start cautions — Mimir 10-25s, Grafana provisioning races behind that on slower workshop laptops → motivates the bounded retry loop in verify:datasources below)
    - .planning/phases/10-prerequisites-stack-decomposition/10-PATTERNS.md (§mise.toml lines 502-621 — verify:bom structural template + section divider style)
    - infra/observability/otelcol-config.yaml (confirms scrape_configs block was successfully migrated — Plan 02 Task 1 acceptance)
  </read_first>
  <behavior>
    - `git rm grafana/prometheus.yaml` (D-15)
    - In `mise.toml`, replace the preflight port-list line `for port in 3000 4317 4318 5672 15672; do` with the 14-port D-11 list
    - Update the description line above the port loop to mention D-11 / 14 ports
    - Append two new task blocks `[tasks."verify:datasources"]` and `[tasks."verify:images"]` immediately after the existing `[tasks."verify:bom"]` block, with a section-divider comment block above each
    - The `verify:datasources` task body MUST wrap its curl + jq UID assertion in a bounded retry loop (6 attempts, 5s sleep between attempts → up to 30s total) so the gate is tolerant of Grafana's provisioning startup race on slower workshop laptops (RESEARCH cautioned Mimir cold-start 10-25s + Grafana datasource provisioning races behind that). Putting the retry inside the mise task body itself means every caller (including Plan 05's smoke test) gets the tolerance for free — no caller-side wrapping required.
  </behavior>
  <action>
**Step 1 — delete the orphan file:**
```bash
cd /home/coto/dev/demo/ose-otel-demo
git rm grafana/prometheus.yaml
```

**Step 2 — modify `mise.toml` preflight port list** (around lines 57-64):

Find this exact block:
```toml
echo "--- Port availability (3000, 4317, 4318, 5672, 15672) ---"
for port in 3000 4317 4318 5672 15672; do
  if ss -tln 2>/dev/null | grep -q ":${port} "; then
    echo "ERROR: Port ${port} is in use. Run: lsof -i:${port} to find the process."
    exit 1
  fi
  echo "  ${port}: free"
done
```

Replace with (D-11 — 14 ports):
```toml
echo "--- Port availability (D-11: 14 ports — 5 v1.0 + 1 v1.0 oversight (15692) + 8 v2.0 additions) ---"
# Five v1.0 ports preserved verbatim: 3000 (Grafana), 4317/4318 (OTLP gRPC/HTTP at the
# Collector ingress), 5672/15672 (RabbitMQ AMQP + management UI). 15692 (RabbitMQ
# prometheus plugin) was port-mapped in compose but absent from v1.0 preflight —
# Phase 10 corrects that oversight here. Eight new ports per D-10:
#   6379  (Valkey)              5432 (Postgres)        — were missing too (preflight oversight)
#   3200  (Tempo HTTP API)      9009 (Mimir HTTP API)  3100 (Loki HTTP API)
#   13133 (Collector health)    8888 (Collector self-metrics)  8889 (Collector prometheus_exporter)
for port in 3000 4317 4318 5672 15672 15692 6379 5432 3200 9009 3100 13133 8888 8889; do
  if ss -tln 2>/dev/null | grep -q ":${port} "; then
    echo "ERROR: Port ${port} is in use. Run: lsof -i:${port} to find the process."
    exit 1
  fi
  echo "  ${port}: free"
done
```

**Step 3 — append two new tasks at the END of `mise.toml`** (after the existing `[tasks."ui:rabbitmq"]` block at line 298 — keep the file's existing trailing newline):

```toml

# ──────────────────────────────────────────────────────────────────
# Phase 10 — verify:datasources (D-03)
# ──────────────────────────────────────────────────────────────────
[tasks."verify:datasources"]
description = "Phase 10 invariant: Grafana provisioned datasource UIDs match the dashboard JSON contract"
run = """
set -e

# WHY: ose-otel-demo.json hardcodes uid='tempo', 'prometheus', 'loki' (NOT 'mimir' — D-01).
# This task fast-fails if any drift, BEFORE attendees see blank dashboards.
# Mirrors the verify:bom task pattern (Phase 2 invariant): compute expected, compute
# actual via curl + jq, diff, non-zero exit on drift.
#
# RETRY LOOP (6 attempts × 5s sleep → up to 30s total):
# RESEARCH cautioned that Mimir cold-start can be 10-25s on slower workshop laptops,
# and Grafana datasource provisioning races behind backend readiness. A single curl
# right after `docker compose up --wait` can hit Grafana before its provisioning
# pass finishes. The retry loop tolerates that startup race without forcing every
# caller (Plan 05 smoke, README Step 10 attendee invocation, CI) to wrap the call.

EXPECTED='loki
prometheus
tempo'

# Bounded retry: try 6 times, sleep 5s between attempts (up to 30s tolerance).
# WHY 6×5: Mimir cold-start 10-25s + Grafana provisioning lag → 30s upper bound
# is comfortable on slower laptops without making a healthy stack feel sluggish.
ATTEMPTS=6
SLEEP_SECS=5
LAST_ERR=""
for i in $(seq 1 $ATTEMPTS); do
  # Hit Grafana's HTTP API (no auth needed — anonymous Admin enabled in compose env).
  ACTUAL=$(curl -fsS http://localhost:3000/api/datasources 2>&1) || {
    LAST_ERR="curl failed: $ACTUAL"
    [ "$i" -lt "$ATTEMPTS" ] && { echo "verify:datasources: attempt $i/$ATTEMPTS — Grafana not ready yet ($LAST_ERR); retrying in ${SLEEP_SECS}s..."; sleep $SLEEP_SECS; continue; }
    echo "ERROR: verify:datasources timed out after $((ATTEMPTS * SLEEP_SECS))s — Grafana not reachable on :3000."
    echo "Last error: $LAST_ERR"
    echo "Run: mise run infra:up"
    exit 1
  }

  # Validate the JSON shape contains at least one entry with a uid; if Grafana
  # is up but provisioning hasn't completed, the array will be empty.
  if ! printf '%s\\n' "$ACTUAL" | jq -e 'type == "array" and length > 0' >/dev/null 2>&1; then
    LAST_ERR="Grafana returned empty datasource array (provisioning still in progress?)"
    [ "$i" -lt "$ATTEMPTS" ] && { echo "verify:datasources: attempt $i/$ATTEMPTS — $LAST_ERR; retrying in ${SLEEP_SECS}s..."; sleep $SLEEP_SECS; continue; }
    echo "ERROR: verify:datasources timed out after $((ATTEMPTS * SLEEP_SECS))s — $LAST_ERR"
    exit 1
  fi

  # We have a non-empty array; break and run the contract diff below.
  break
done

# Extract just the uid field from each entry, sort, dedupe.
ACTUAL_UIDS=$(printf '%s\\n' "$ACTUAL" | jq -r '.[].uid' | sort -u)

# Compare expected vs actual — diff returns non-zero on any drift.
if ! diff <(printf '%s\\n' "$EXPECTED") <(printf '%s\\n' "$ACTUAL_UIDS") > /tmp/ds-diff 2>&1; then
  echo "ERROR: Grafana datasource UIDs drifted from the dashboard contract."
  echo "Expected (3 UIDs):"
  printf '  %s\\n' "$EXPECTED"
  echo "Actual (provisioned by grafana/datasources.yaml):"
  printf '  %s\\n' "$ACTUAL_UIDS"
  echo
  echo "Diff:"
  cat /tmp/ds-diff
  echo
  echo "If you renamed a datasource, the ose-otel-demo dashboard JSON must be updated to match (NOT recommended — keep the contract)."
  exit 1
fi

echo "Phase 10 datasource contract: 3 UIDs match (loki, prometheus, tempo)."
"""

# ──────────────────────────────────────────────────────────────────
# Phase 10 — verify:images (D-14 / X-3 — STACK-02 floating-tag guardrail)
# ──────────────────────────────────────────────────────────────────
[tasks."verify:images"]
description = "Phase 10 invariant: every docker-compose image: tag is a fully-qualified patch version (no floating tags)"
run = """
set -e

# WHY: workshop reproducibility (X-3 / D-14). Floating tags break clones across cohorts.
# Floating patterns to reject:
#   - :latest                              (unconditional)
#   - :NN              (e.g., :17 :3)      (major-only)
#   - :NN.NN           (e.g., :3.7 :2.10)  (major.minor only — no patch)
#   - no tag at all    (e.g., 'image: rabbitmq')
#
# Acceptable patterns:
#   - :NN.NN.NN        (e.g., :3.7.1 :13.0.1)
#   - :NN.NN-suffix    (e.g., :4.3-management-alpine — has a flavor suffix; not floating)
#   - :NN.NN.NN-suffix (e.g., :17-alpine)
#   - vX.Y.Z           (e.g., v1.83.0 — Prometheus-style v-prefix)

# Extract image: lines, ignore comments.
LINES=$(grep -E '^\\s*image:' docker-compose.yml | grep -v '^\\s*#')

# Floating-tag regex:
#   - no colon at all (no tag)
#   - :latest
#   - :digit-only at end of line
#   - :digit.digit (without trailing .digit) at end of line
FLOATING=$(printf '%s\\n' "$LINES" | grep -E '(image:\\s*[^:[:space:]]+\\s*$|:latest\\s*$|:[0-9]+\\s*$|:[0-9]+\\.[0-9]+\\s*$)' || true)

if [ -n "$FLOATING" ]; then
  echo "ERROR: Phase 10 invariant violated — docker-compose.yml contains floating image tags:"
  printf '%s\\n' "$FLOATING"
  echo
  echo "Pin every image to an exact patch version. Examples:"
  echo "  rabbitmq:4.3-management-alpine     # OK (flavor suffix is not floating)"
  echo "  grafana/grafana:13.0.1             # OK (full patch version)"
  echo "  grafana/grafana:13.0               # WRONG (minor only — floating)"
  echo "  grafana/grafana:latest             # WRONG (latest — floating)"
  exit 1
fi

# Also confirm we have at least 5 image: lines (the v2.0 5-container minimum).
COUNT=$(printf '%s\\n' "$LINES" | wc -l)
if [ "$COUNT" -lt 5 ]; then
  echo "WARNING: only $COUNT image: lines found — v2.0 expects at least 5 observability containers."
fi

echo "Phase 10 image-pin contract: $COUNT image:s, all pinned to exact patch versions."
"""
```

**Step 4 — exercise the new tasks against the live stack** (the stack should still be up from Plan 04 Task 1):
```bash
cd /home/coto/dev/demo/ose-otel-demo
mise run preflight  # may FAIL because ports are now in use by infra:up; that's expected — preflight is meant for pre-up state
                    # As a sanity check after infra:down: mise run infra:down && mise run preflight && mise run infra:up

mise run verify:images   # expect green (all 10 images pinned)
mise run verify:datasources   # expect green (3 UIDs: loki, prometheus, tempo)
                              # Note: the task body now retries up to 6×5s internally — no caller-side
                              # sleep/retry wrapping needed even right after `docker compose up --wait`.
```

If `verify:datasources` ever still fails after the bounded retry, that's a real drift signal (not a startup race) — investigate the diff output and `docker compose logs grafana 2>&1 | grep -i provisioning` rather than retrying further. Capture the output to `/tmp/phase10-verify-tasks.log`.
  </action>
  <verify>
    <automated>cd /home/coto/dev/demo/ose-otel-demo && ! test -f grafana/prometheus.yaml && grep -q 'verify:datasources' mise.toml && grep -q 'verify:images' mise.toml && grep -q '3000 4317 4318 5672 15672 15692 6379 5432 3200 9009 3100 13133 8888 8889' mise.toml && grep -q 'ATTEMPTS=6' mise.toml && grep -q 'SLEEP_SECS=5' mise.toml && grep -Pzo '(?s)\[tasks\."verify:datasources"\].*?for i in \$\(seq 1 \$ATTEMPTS\); do' mise.toml >/dev/null && mise run verify:images 2>&1 | grep -q 'image-pin contract' && mise run verify:datasources 2>&1 | grep -q 'datasource contract: 3 UIDs match'</automated>
  </verify>
  <acceptance_criteria>
    - `grafana/prometheus.yaml` no longer exists on disk (D-15)
    - `git status` shows `grafana/prometheus.yaml` as deleted
    - `mise.toml` contains a `[tasks."verify:datasources"]` block (D-03)
    - `mise.toml` contains a `[tasks."verify:images"]` block (D-14)
    - `mise.toml` contains the literal port list `3000 4317 4318 5672 15672 15692 6379 5432 3200 9009 3100 13133 8888 8889` in the preflight task (D-11)
    - `mise.toml` does NOT contain the literal old port list `for port in 3000 4317 4318 5672 15672; do` (the 5-port form has been replaced)
    - **Race-tolerance gate (Warning #3 mitigation):** the `[tasks."verify:datasources"]` body in `mise.toml` contains a bounded retry loop with the literal tokens `ATTEMPTS=6`, `SLEEP_SECS=5`, and `for i in $(seq 1 $ATTEMPTS); do` — verifying the retry semantics are codified inside the task body itself (NOT delegated to callers). Concretely, this regex must match within the verify:datasources block: `(?s)\[tasks\."verify:datasources"\].*?for i in \$\(seq 1 \$ATTEMPTS\); do`
    - The retry loop covers BOTH failure modes: (a) Grafana not yet reachable on :3000, AND (b) Grafana reachable but `/api/datasources` returns an empty array (provisioning still in progress) — both branches `continue` to the next attempt rather than fast-failing
    - On exhausted retries (after ~30s), the task exits non-zero with a "timed out after 30s" message — NOT a generic "Grafana not reachable"
    - `mise run verify:images` exits 0 and prints "Phase 10 image-pin contract"
    - `mise run verify:datasources` exits 0 (no caller-side sleep needed even right after `docker compose up --wait`) and prints "Phase 10 datasource contract: 3 UIDs match (loki, prometheus, tempo)"
    - The new mise tasks each have a `# ──────────────────...` section-divider header above them (matching mise.toml convention)
    - The verify:images task body contains the literal floating-tag warning examples in its error-message block
  </acceptance_criteria>
  <done>grafana/prometheus.yaml deleted; mise.toml has extended preflight + 2 new verify tasks; verify:datasources body codifies a 6×5s retry loop tolerant of Grafana provisioning startup race; both new verify tasks pass against the live stack from Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose default network | Internal — service-name DNS only |
| Backend admin ports on host | D-10 — workshop-debug accessible from localhost |
| Bind-mounted YAML configs | Workshop-controlled, no user input dimension |
| Pinned image registry pulls | Docker Hub / official Grafana registry — pinned by exact patch version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.04-01 | Tampering / Supply chain | Floating image tag could pull arbitrary image content | mitigate | D-14 `verify:images` mise task — fast-fails on `:latest`, major-only, or major.minor-only tags. STACK-02 success gate. |
| T-10.04-02 | Denial of Service | Stale lgtm container holds port 4317 (Pitfall 6) | mitigate | Atomic switchover: the same compose-rewrite that adds 5 services REMOVES the entire `lgtm:` block in one edit. README test plan recommends `docker compose down` before first `up` after the switch. |
| T-10.04-03 | Spoofing | Container name collision (e.g., two `ose-otel-loki`) | mitigate | Each service has explicit `container_name: ose-otel-<name>` — Docker enforces uniqueness; collision fails at `docker compose up`. |
| T-10.04-04 | Information Disclosure | Backend admin ports (3200/9009/3100/13133/8888/8889) on shared workshop laptop | accept | D-10 explicit — workshop convenience. README must advise `docker compose down` between sessions on shared machines. Same scope as Plan 02 T-10.02-03. |
| T-10.04-05 | Tampering | Bind-mount of host config YAMLs | accept | All bind-mounts are `:ro` (read-only) on whitelisted paths under `infra/observability/` and `grafana/`. Loki rules dir specifically `:ro` (D-07 + Pitfall 7). |
| T-10.04-06 | Denial of Service | Backend warmup race (Tempo writes to Mimir before Mimir is ready) | mitigate | `depends_on: service_started` (D-16) is necessary but not sufficient; the actual mitigation is built into Tempo's `metrics_generator.remote_write` retry loop and Loki's ruler.remote_write retry — both backends ship retry behavior. README Step 10 mentions the 1-5 minute service-graph re-prime window after `infra:reset`. |
</threat_model>

<verification>
1. `docker compose config --quiet` exits 0
2. 10 services total (5 data + 5 observability)
3. All 5 new image tags are exact-patch
4. lgtm anti-pattern absent (no `grafana/otel-lgtm` substring; no `lgtm:` service block; no `lgtm-data:` volume)
5. 5 new named volumes declared
6. Each new service has healthcheck + depends_on per D-16
7. Pitfall 8 mounts present on grafana service (3 separate provisioning bind-mounts)
8. `grafana/prometheus.yaml` deleted (D-15)
9. mise preflight grew to 14 ports (D-11)
10. New mise tasks `verify:datasources` and `verify:images` exist and pass against live stack
11. `verify:datasources` body codifies the 6×5s bounded retry loop (race-tolerance gate)
</verification>

<success_criteria>
- docker-compose.yml validated by `docker compose config --quiet`
- All 10 services start under `docker compose up -d --wait` within 180s timeout
- `mise run verify:images` PASSES (all images pinned to exact patches)
- `mise run verify:datasources` PASSES (3 UIDs match: loki, prometheus, tempo) with the in-task retry loop tolerating Grafana provisioning startup race on slower workshop laptops
- `grafana/prometheus.yaml` removed from disk and from git index
- mise preflight covers all 14 ports per D-11
- v1.0 carryforward env vars on grafana service (anonymous Admin) preserved
- depends_on chains match D-16
</success_criteria>

<output>
After completion, create `.planning/phases/10-prerequisites-stack-decomposition/10-04-SUMMARY.md` describing:
- docker-compose.yml line-count delta (current 122 lines → expected ~210 lines)
- All 10 services confirmed running via `docker compose ps`
- mise.toml line-count delta + new task descriptions (highlight the verify:datasources retry loop — 6×5s — as a Warning #3 mitigation that tolerates Grafana provisioning startup race)
- grafana/prometheus.yaml deletion confirmed
- Live verify task outputs
- Anything Plan 05 needs to know (e.g., observed cold-start times — does Mimir need start_period > 30s on a slower machine?)
</output>
</output>
