---
phase: 12-exemplars-metrics-to-trace-click-through
plan: 04
type: execute
wave: 2
depends_on:
  - "12-01"
  - "12-02"
  - "12-03"
autonomous: false
requirements:
  - EXMP-04

must_haves:
  truths:
    - "Workshop attendee can run mise run verify:exemplars and see a GREEN result after generating load"
    - "The ose-otel-demo dashboard shows exemplar dots on the HTTP Request Duration panel"
    - "Clicking an exemplar dot navigates to the originating trace in Tempo"
    - "README §12 is present with all seven content subsections per D-E8"
    - "README §12 includes placeholder screenshot reference step-12-exemplars.png per D-E9/D-E10"
    - "README §12 includes the consumer asymmetry teaching note per D-E11"
  artifacts:
    - path: "README.md"
      provides: "§12 workshop walkthrough per D-E8 (~100-150 lines)"
      contains: "Step 12: Exemplars"
  key_links:
    - from: "README.md §12"
      to: "grafana/datasources.yaml exemplarTraceIdDestinations"
      via: "Phase 10 D-02 pre-wired — three-layer narrative explains all three layers including pre-existing datasource wiring"
      pattern: "exemplarTraceIdDestinations"
    - from: "README.md §12"
      to: "infra/observability/mimir-config.yaml limits.max_global_exemplars_per_user"
      via: "annotated config excerpt explaining real Mimir enablement gate"
      pattern: "max_global_exemplars_per_user"
---

<objective>
Complete Phase 12 by:

1. Writing README §12 — the Phase-11-equivalent depth (~100-150 lines) workshop walkthrough for exemplars (D-E8). Seven subsections covering: what exemplars are, the three layers, annotated config excerpts, cardinality safety, mise run verify:exemplars, click-through UX, placeholder screenshot.
2. Running the end-to-end smoke test (all four changes deployed) and conducting the human-verify checkpoint: attendee can click an exemplar dot and land on a trace in Tempo.

Purpose: README §12 is the teaching artifact that explains the three-layer plumbing and contextualizes the three small code changes. The human-verify checkpoint confirms EXMP-04's "one click, no manual trace-ID copy-paste" success criterion is live and working.

Output: README.md has a new §12 section. The phase is verified end-to-end and the git tag `step-12-exemplars` is ready to be applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-exemplars-metrics-to-trace-click-through/12-CONTEXT.md
@.planning/phases/12-exemplars-metrics-to-trace-click-through/12-RESEARCH.md
@.planning/phases/12-exemplars-metrics-to-trace-click-through/12-PATTERNS.md
@.planning/phases/12-exemplars-metrics-to-trace-click-through/12-01-SUMMARY.md
@.planning/phases/12-exemplars-metrics-to-trace-click-through/12-02-SUMMARY.md
@.planning/phases/12-exemplars-metrics-to-trace-click-through/12-03-SUMMARY.md

<interfaces>
<!-- README insertion context. -->

README §12 inserts BETWEEN:
- Line 631: end of §11 F2-3 double-filter trap blockquote (closing `>` line)
- Line 632: `## Concepts & FAQ` heading

README heading style (from §11):
- `## Step 11: Tail Sampling at the Collector — what survives the Tempo write path`
- Sub-headings: `### What you'll learn`, `### Checkpoint`, `### Run`, `### What to look for`, `### Why it matters`

Phase 12 heading: `## Step 12: Exemplars — three lines that make histogram charts clickable`
Sub-headings to use: `### What you'll learn`, `### Checkpoint`, `### Run`, `### What to look for`, `### Why it matters`

Screenshot placeholder pattern (D-E9/D-E10):
```html
<img src="docs/screenshots/step-12-exemplars.png"
     alt="Exemplar dots on http.server.request.duration — click any dot to land on the originating trace in Tempo."
     width="900" />
<!-- Screenshot captured by Phase 18 Playwright automation -->
```

Consumer asymmetry note (D-E11, one sentence to embed in "What you'll learn"):
"The consumer's counter exemplars exist in Mimir but are only visible via the API, not as chart dots — histograms are the natural exemplar surface."

git diff size expectation: `git diff step-11-tail-sampling..HEAD` should show ~80 lines of new README content, 2 Java file changes (4 lines each), 1 YAML change (7 lines), 1 JSON change (70 lines), 1 TOML change (35 lines) — matching Phase 12 scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart infra and run end-to-end smoke test</name>
  <files></files>
  <read_first>
    - .planning/phases/12-exemplars-metrics-to-trace-click-through/12-01-SUMMARY.md (confirm SDK changes applied)
    - .planning/phases/12-exemplars-metrics-to-trace-click-through/12-02-SUMMARY.md (confirm mimir-config.yaml updated)
    - .planning/phases/12-exemplars-metrics-to-trace-click-through/12-03-SUMMARY.md (confirm dashboard + mise task added)
  </read_first>
  <action>
Run the following commands in sequence to bring up the updated infrastructure and verify the exemplar pipeline end-to-end:

```bash
# 1. Restart Mimir to pick up the new limits.max_global_exemplars_per_user config
docker compose restart mimir
# Wait for Mimir to be ready
sleep 15
curl -fsS http://localhost:9009/ready | grep -q ready && echo "Mimir ready"

# 2. Restart the Collector (picks up any otelcol-config.yaml changes if not already live)
docker compose restart otel-collector
sleep 10
curl -fsS http://localhost:13133/ | grep -q "Server available" && echo "Collector ready"

# 3. Restart both app services if running (picks up the ExemplarFilter + scope fix)
# (If running via mise dev, Ctrl-C and restart — otherwise restart the java processes)
# Verify the services compile:
mvn -B -pl producer-service,consumer-service compile -q && echo "Both services compile: OK"

# 4. Generate load to populate the exemplar pipeline
mise run load &
LOAD_PID=$!
sleep 45

# 5. Run verify:exemplars
mise run verify:exemplars

# 6. Stop load
kill $LOAD_PID 2>/dev/null || true
```

If `verify:exemplars` fails, diagnose using the error output's numbered checklist before proceeding to the README task.

Expected verify:exemplars output: `verify:exemplars: GREEN — exemplars with trace_id are present in Mimir.`
  </action>
  <verify>
    <automated>mise run verify:exemplars</automated>
  </verify>
  <acceptance_criteria>
    - `mise run verify:exemplars` exits 0 with "GREEN" in the output
    - `docker compose logs mimir | grep -c 'max_global_exemplars_per_user'` is 0 (no config error — Mimir accepted the limits block)
    - `curl -fsS 'http://localhost:9009/prometheus/api/v1/query_exemplars?query=http_server_request_duration_seconds_bucket&start=now-10m&end=now' | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['data'], 'no exemplars'"` exits 0
  </acceptance_criteria>
  <done>Mimir is running with exemplar storage enabled. After generating load, mise run verify:exemplars exits 0 confirming exemplars with trace_id are present in the last 10 minutes.</done>
</task>

<task type="auto">
  <name>Task 2: Write README §12 section</name>
  <files>
    README.md
  </files>
  <read_first>
    - README.md (read lines 550-635 — the full §11 section + the Concepts & FAQ heading — to understand the exact insertion point, heading style, sub-section structure, and HTML table pattern used in §11)
  </read_first>
  <action>
Insert the §12 section into README.md at the insertion point: between line 631 (the F2-3 blockquote closing line) and line 632 (`## Concepts & FAQ`).

Write the following section verbatim (follow the §11 structure and workshop voice established in CONTEXT.md D-E8):

```markdown

## Step 12: Exemplars — three lines that make histogram charts clickable

### What you'll learn

- An **exemplar** is a single sample observation — a `{trace_id, span_id, value, timestamp}` tuple — attached to a histogram data point by the SDK when a span is active at measurement time. It is the link between a metric and the trace that produced it.
- The three-layer plumbing: **(1)** `ExemplarFilter.traceBased()` on the SDK's `SdkMeterProvider` tells the SDK when to attach exemplars; **(2)** the OTel Collector's PRW translator forwards exemplars unconditionally to Mimir when the OTLP data points carry them; **(3)** Mimir stores them (requires `limits.max_global_exemplars_per_user > 0` — the default is 0, which silently discards all exemplars); and **(4)** Grafana's `exemplarTraceIdDestinations` provisioning (pre-wired in Phase 10) routes the `trace_id` exemplar label to the Tempo datasource so one click is enough.
- Both `OtelSdkConfiguration.buildMeterProvider()` methods get `.setExemplarFilter(ExemplarFilter.traceBased())` — the consumer has counter metrics, not a histogram, so counter exemplars go to Mimir but don't show as chart dots. The consumer's counter exemplars exist in Mimir and are queryable via the API — histograms are the natural exemplar visualization surface.
- `HttpServerSpanFilter.doFilterInternal()` is restructured from try-with-resources to manual scope management so `requestDuration.record()` fires while the SERVER span is still current — if scope closes before `record()`, the SDK sees `SpanContext.invalid()` and attaches no exemplar regardless of filter setting.

### Checkpoint

Workshop is at `step-12-exemplars` — the orchestrator applies this annotated tag atomically with the phase-completion merge per WORK-01 / D-21. `git checkout step-12-exemplars` jumps to this point. The previous tag is `step-11-tail-sampling`; `git diff step-11-tail-sampling..step-12-exemplars` shows the focused diff: two Java files (ExemplarFilter + scope fix), one YAML file (mimir-config.yaml limits block), one JSON file (dashboard exemplar row), one TOML file (verify:exemplars task), and this README section.

### Run

```bash
mise run preflight
mise run infra:up
# Restart Mimir to apply the limits.max_global_exemplars_per_user config:
docker compose restart mimir
mise run dev
# In another terminal — generate enough load for exemplars to appear:
mise run load
# Wait ~30 seconds for the PeriodicMetricReader (10s interval) to export metrics:
sleep 30
# Verify the exemplar pipeline is active end-to-end:
mise run verify:exemplars
# Open Grafana and look at the Exemplars (Phase 12) row:
mise run ui:grafana
```

### What to look for

**Exemplar dots on the histogram panel.** Open the `ose-otel-demo` dashboard in Grafana at `:3000`. Look for the "Exemplars (Phase 12)" row — it is open by default. The "HTTP Request Duration (with Exemplars)" panel shows p50/p95/p99 latency lines. After generating load with `mise run load`, small dots or diamonds appear overlaid on the lines — each dot represents one request. Hover over a dot to see the `trace_id` tooltip.

**Click-through to Tempo.** Click any exemplar dot. Grafana reads the `trace_id` label from the exemplar, looks up the `exemplarTraceIdDestinations` configuration in the Prometheus datasource provisioning (pre-wired in Phase 10 D-02 at `grafana/datasources.yaml` lines 50–56), and opens the trace waterfall in Tempo in the same browser tab. No manual trace-ID copy-paste required — this is the EXMP-04 success criterion.

**Verify the pipeline with `mise run verify:exemplars`.** This task queries Mimir's exemplar API directly:

```bash
# Behind the scenes — what verify:exemplars calls:
curl -fsS "http://localhost:9009/prometheus/api/v1/query_exemplars?query=http_server_request_duration_seconds_bucket&start=$(date -u -d '10 minutes ago' '+%Y-%m-%dT%H:%M:%SZ')&end=$(date -u '+%Y-%m-%dT%H:%M:%SZ')"
```

A successful response contains `{"status":"success","data":[{"seriesLabels":{...},"exemplars":[{"labels":{"trace_id":"<32hex>","span_id":"<16hex>"},...}]}]}`.

**Annotated config excerpts (the three layers).**

Layer 1 — SDK (`producer-service/src/main/java/com/example/producer/config/OtelSdkConfiguration.java`, same in consumer):
```java
return SdkMeterProvider.builder()
    .setResource(resource)
    .setExemplarFilter(ExemplarFilter.traceBased())   // Phase 12 — EXMP-01
    .registerMetricReader(metricReader)
    .build();
```

Layer 2 — Mimir (`infra/observability/mimir-config.yaml`):
```yaml
limits:
  max_global_exemplars_per_user: 100000  # WHY: 0 (default) = exemplar ingestion disabled
```
The Collector's PRW exporter needs no config change — exemplar forwarding in `collector-contrib v0.151.0` is unconditional (the `pkg/translator/prometheusremotewrite/helper.go::getPromExemplars()` function runs on every histogram data point automatically). Do not add `send_exemplars: true` — this key does not exist in the v0.151.0 config schema and will cause a parse error.

Layer 3 — Grafana datasource (`grafana/datasources.yaml`, pre-wired Phase 10 D-02, no changes in Phase 12):
```yaml
- name: Prometheus
  ...
  jsonData:
    exemplarTraceIdDestinations:
      - name: trace_id
        datasourceUid: tempo
        urlDisplayLabel: "Trace: ${__value.raw}"
```

**Cardinality safety.** Each exemplar carries only `trace_id` (32 hex chars) and `span_id` (16 hex chars) — approximately 60 bytes per exemplar, well under the 128-byte OpenMetrics limit. No business attributes (user IDs, order IDs, request paths) are attached — the SDK's `ExemplarFilter.traceBased()` only stamps the active span context, not the span attributes.

> **Production consideration.** The workshop runs Mimir with `multitenancy_enabled: false` and no auth on `:9009`. In production, the `/prometheus/api/v1/query_exemplars` endpoint should be restricted to authorized internal users — `trace_id` values are internal correlation IDs (not secrets), but an unrestricted endpoint leaks the set of recent traces to any caller.

### Why it matters

Exemplars close the gap between "I see a latency spike on the p99 panel" and "I need to find a trace to investigate." Without exemplars, the workflow is: (1) spot the latency spike in Grafana; (2) guess a time window; (3) go to Tempo Search; (4) filter by service + status + time range; (5) pick a representative trace. With exemplars, step 2–4 collapse to: (1) click the spike point; (2) arrive at the trace that produced it.

The three-layer architecture is also a teaching surface for the OTel data model: the SDK's `ExemplarFilter` injects trace context into metric data points (not into metric names or labels), the Collector passes it through opaquely, and Grafana's datasource config defines the routing from trace context to the trace backend. Each layer has exactly one concern — and Phase 12's diff proves it: one line per layer, zero cross-layer coupling.

**Screenshot placeholder:**

<img src="docs/screenshots/step-12-exemplars.png"
     alt="Exemplar dots on http.server.request.duration — click any dot to land on the originating trace in Tempo."
     width="900" />
<!-- Screenshot captured by Phase 18 Playwright automation -->
```

Write this section as a single atomic edit to README.md, inserting it at the line immediately after the F2-3 blockquote (after line 631) and before `## Concepts & FAQ` (line 632). Do not modify any existing content.
  </action>
  <verify>
    <automated>grep -c 'Step 12: Exemplars' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Step 12: Exemplars' README.md` outputs `1`
    - `grep -c 'verify:exemplars' README.md` outputs at least `1`
    - `grep -c 'max_global_exemplars_per_user' README.md` outputs at least `1`
    - `grep -c 'step-12-exemplars.png' README.md` outputs `1` (screenshot placeholder present)
    - `grep -c 'exemplarTraceIdDestinations' README.md` outputs at least `1` (three-layer explanation references the pre-wired datasource)
    - `grep -c 'counter exemplars exist in Mimir' README.md` outputs `1` (D-E11 asymmetry note)
    - `grep -c '## Concepts & FAQ' README.md` outputs `1` (heading not duplicated or removed)
    - README section appears between the F2-3 blockquote and `## Concepts & FAQ` (confirmed by line order in grep -n output)
  </acceptance_criteria>
  <done>README.md has a §12 section with all seven D-E8 subsections (what exemplars are, three layers, annotated config excerpts, cardinality safety, verify:exemplars, click-through UX, placeholder screenshot). The section is ~100-150 lines and inserted between §11 and Concepts & FAQ.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 12 changes are deployed:
1. Plan 01: Both OtelSdkConfiguration.java files have ExemplarFilter.traceBased(). HttpServerSpanFilter has scope.close() after record().
2. Plan 02: mimir-config.yaml has limits.max_global_exemplars_per_user: 100000. otelcol-config.yaml has WHY comment (no send_exemplars key).
3. Plan 03: ose-otel-demo.json has Exemplars (Phase 12) open row with histogram panel (id=16, exemplar: true). mise.toml has verify:exemplars task.
4. Plan 04 Task 1: Mimir restarted, load generated, mise run verify:exemplars is GREEN.
5. Plan 04 Task 2: README §12 written.
  </what-built>
  <how-to-verify>
1. Confirm verify:exemplars is GREEN: `mise run verify:exemplars` — expected output contains "GREEN — exemplars with trace_id are present in Mimir"
2. Open Grafana: `mise run ui:grafana` → navigate to the `ose-otel-demo` dashboard
3. Find the "Exemplars (Phase 12)" row — it should be OPEN (not collapsed) near the bottom of the dashboard, after the "Tail Sampling diagnostics" row
4. The "HTTP Request Duration (with Exemplars)" panel should show p50/p95/p99 lines with small dots or diamonds overlaid (exemplar dots). If no dots yet, run `mise run load && sleep 30` and refresh.
5. **Critical: click one exemplar dot** — Grafana should navigate directly to the Tempo trace waterfall for that trace. You should see the full producer trace with SERVER → INTERNAL → PRODUCER spans. This is EXMP-04.
6. Verify EXMP-04: the trace was opened with NO manual copy-paste of a trace ID — you clicked a dot and arrived.
7. Check §12 in README: `grep -c 'Step 12: Exemplars' README.md` → 1
  </how-to-verify>
  <resume-signal>Type "approved" if exemplar dots appear on the histogram and clicking one opens the trace in Tempo. Describe any issues if the click-through doesn't work.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| End-to-end exemplar pipeline | SDK → OTLP → Collector PRW → Mimir → Grafana → Tempo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | README §12 production consideration note | mitigate | README §12 includes a callout that in production, /prometheus/api/v1/query_exemplars should be restricted to authorized users; this surfaces the ASVS L1 access control concern to workshop attendees explicitly |
</threat_model>

<verification>
End-to-end Phase 12 verification:

```bash
# 1. Compile check
mvn -B -pl producer-service,consumer-service compile -q

# 2. SDK layer: ExemplarFilter in both services
grep 'setExemplarFilter' \
  producer-service/src/main/java/com/example/producer/config/OtelSdkConfiguration.java \
  consumer-service/src/main/java/com/example/consumer/config/OtelSdkConfiguration.java

# 3. Scope fix: manual scope management in place
grep -c 'try (Scope' producer-service/src/main/java/com/example/producer/config/HttpServerSpanFilter.java
# Expected: 0

# 4. Mimir layer: limits block present
grep 'max_global_exemplars_per_user' infra/observability/mimir-config.yaml

# 5. No send_exemplars in otelcol-config.yaml
grep -c 'send_exemplars' infra/observability/otelcol-config.yaml
# Expected: 0

# 6. Dashboard panel with exemplar: true
python3 -c "
import json
d = json.load(open('grafana/dashboards/ose-otel-demo.json'))
panel = next(p for p in d['panels'] if p.get('id') == 16)
assert all(t['exemplar'] == True for t in panel['targets'])
print('Panel 16: exemplar=true on all targets: PASS')
"

# 7. mise verify:exemplars task registered
grep 'verify:exemplars' mise.toml

# 8. README §12 present
grep -c 'Step 12: Exemplars' README.md

# 9. Live pipeline check (requires running infra + load)
mise run verify:exemplars
```
</verification>

<success_criteria>
- Both services compile without error
- Both OtelSdkConfiguration.java have setExemplarFilter(ExemplarFilter.traceBased()) in buildMeterProvider()
- HttpServerSpanFilter has NO try-with-resources scope pattern; scope.close() is after requestDuration.record()
- mimir-config.yaml has limits.max_global_exemplars_per_user: 100000
- otelcol-config.yaml has NO send_exemplars key and HAS a WHY comment about unconditional PRW forwarding
- ose-otel-demo.json has panel id=16 with exemplar: true on all three targets
- mise.toml has [tasks."verify:exemplars"] that queries Mimir API and asserts trace_id
- README.md has §12 section (~100-150 lines) with all seven D-E8 subsections
- mise run verify:exemplars exits 0 after generating load (live pipeline GREEN)
- Human verify: exemplar dot click in Grafana dashboard opens trace in Tempo (EXMP-04)
- git tag step-12-exemplars ready to apply per WORK-01
</success_criteria>

<output>
After completion, create `.planning/phases/12-exemplars-metrics-to-trace-click-through/12-04-SUMMARY.md`
</output>
