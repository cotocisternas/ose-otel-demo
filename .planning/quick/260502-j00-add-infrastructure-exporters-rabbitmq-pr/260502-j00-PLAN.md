---
quick_id: 260502-j00
mode: quick-full
description: "Step 9 — Add RabbitMQ Prometheus plugin + redis_exporter + postgres_exporter, mount custom prometheus.yaml into otel-lgtm, author 3rd 'Infra & Deps' Grafana dashboard, append README Step 9 chapter"
status: ready-for-execution
planned: 2026-05-02
revision_history:
  - "Revision 1/2: addressed B1, B2, B3 (3 blockers) + W1, W2, W3 (3 warnings) + L1 (1 low). Live-inspected bundled prometheus.yaml content embedded in T1 Step 1 verbatim."

must_haves:
  truths:
    - "After `mise run infra:up`, all six compose services (rabbitmq, valkey, postgres, lgtm, redis-exporter, postgres-exporter) reach a running state and the three exporters are reachable on the compose default network."
    - "RabbitMQ's `rabbitmq_prometheus` plugin is enabled at startup; `curl -s http://localhost:15692/metrics` returns Prometheus text including `rabbitmq_build_info` and `rabbitmq_queue_messages`."
    - "The bundled Prometheus inside the lgtm container scrapes the three new targets — its `up{job=~\"rabbitmq|redis_exporter|postgres_exporter\"}` series report value 1 — AND the bundled OTLP-ingest pathway is preserved (verified by `target_info{service_name=\"order-producer\"}` returning ≥1 series after `mise run dev`)."
    - "Existing dashboards `ose-otel-demo` and `ose-otel-noc` continue to auto-provision and render unchanged (Phase 7 invariants preserved)."
    - "A new dashboard at `/d/ose-otel-infra` auto-provisions, lists in Grafana with tags `infra` + `otel`, and exposes 4 always-visible header stat panels plus 4 collapsed rows (HTTP/AMQP, Cache, Database, Broker) — `redis_up`, `pg_up`, and `up{job=\"rabbitmq\"}` panels read green after `mise run load` is running."
    - "The Database row of the new dashboard shows BOTH `db_client_connection_count` (from consumer's HikariCP gauge, `service_name=order-consumer`) AND `pg_stat_database_numbackends{datname=\"orders\"}` (from postgres_exporter), demonstrating the two-lens story."
    - "README contains a `## Step 9: Dashboards & Infrastructure Telemetry` section with copy-pasteable commands, an explicit production-fidelity callout about reusing `orders/orders` for postgres_exporter, and at least one new FAQ entry covering the app-vs-infra two-lens lesson."

  artifacts:
    - path: "grafana/prometheus.yaml"
      provides: "Custom Prometheus scrape config mounted over /otel-lgtm/prometheus.yaml. MUST contain the bundled `otlp:` ingest block (with `keep_identifying_resource_attributes: true` and the full `promote_resource_attributes` list), the bundled `global:` (`scrape_native_histograms: true`) and `storage:` (`out_of_order_time_window: 10m`) blocks verbatim, AND a new `scrape_configs:` section with three jobs (rabbitmq, redis_exporter, postgres_exporter) — each carrying static label `source: infra-exporter`."
      contains_all:
        - "otlp:"
        - "keep_identifying_resource_attributes: true"
        - "promote_resource_attributes:"
        - "scrape_native_histograms: true"
        - "scrape_configs:"
        - "job_name: rabbitmq"
        - "job_name: redis_exporter"
        - "job_name: postgres_exporter"
    - path: "grafana/dashboards/ose-otel-infra.json"
      provides: "3rd workshop dashboard (uid `ose-otel-infra`, schemaVersion 39, refresh 10s) with header stat row + 4 collapsed rows (collapsed: true); cross-links to `/d/ose-otel-demo` and `/d/ose-otel-noc`."
      contains: "\"uid\": \"ose-otel-infra\""
    - path: "docker-compose.yml"
      provides: "Adds redis-exporter and postgres-exporter services pinned to specific tags; adds RABBITMQ_PLUGINS env var on rabbitmq service; exposes :15692 on rabbitmq host; bind-mounts ./grafana/prometheus.yaml into lgtm at /otel-lgtm/prometheus.yaml."
      contains: "rabbitmq_prometheus"
    - path: "README.md"
      provides: "## Step 9 section between current ## Step 8 and ## Concepts & FAQ; new FAQ entry on app-vs-infra two-lens telemetry."
      contains: "Step 9: Dashboards & Infrastructure Telemetry"

  key_links:
    - from: "docker-compose.yml lgtm service"
      to: "grafana/prometheus.yaml"
      via: "bind-mount volume entry `./grafana/prometheus.yaml:/otel-lgtm/prometheus.yaml:ro`"
      pattern: "prometheus.yaml:/otel-lgtm/prometheus.yaml"
    - from: "docker-compose.yml rabbitmq service"
      to: "rabbitmq_prometheus plugin endpoint :15692"
      via: "RABBITMQ_PLUGINS env var enables plugin at boot; ports list exposes 15692"
      pattern: "RABBITMQ_PLUGINS.*rabbitmq_prometheus"
    - from: "grafana/prometheus.yaml scrape_configs"
      to: "rabbitmq:15692, redis-exporter:9121, postgres-exporter:9187"
      via: "static_configs targets resolved on compose default network by service name"
      pattern: "redis-exporter:9121"
    - from: "grafana/dashboards/ose-otel-infra.json panels"
      to: "Mimir/Prometheus and Tempo datasources"
      via: "every panel target carries datasource {type, uid: prometheus|tempo}"
      pattern: "\"uid\": \"prometheus\""
    - from: "grafana/dashboards/dashboards.yaml provider"
      to: "ose-otel-infra.json"
      via: "directory scan of /otel-lgtm/grafana/conf/provisioning/dashboards (no provisioning manifest changes required)"
      pattern: "ose-otel-infra"
---

<objective>
Add Step 9 of the workshop: three infrastructure exporters (RabbitMQ Prometheus plugin, redis_exporter, postgres_exporter) plus a 3rd "Infra & Deps" Grafana dashboard that teaches the app-vs-infra two-lens telemetry lesson.

Purpose: Workshop attendees see Phase 8's Valkey + PostgreSQL telemetry in Grafana out-of-the-box and learn to read app-emitted OTel metrics side-by-side with infrastructure exporter metrics — the canonical "client-side latency vs server-side queue depth" production observability skill.

Output:
- Modified `docker-compose.yml` (3 changes: rabbitmq env var + port; new redis-exporter service; new postgres-exporter service; lgtm prometheus.yaml mount)
- New `grafana/prometheus.yaml` (custom config with bundled `otlp:`/`global:`/`storage:` blocks preserved verbatim + new `scrape_configs:` section with 3 jobs)
- New `grafana/dashboards/ose-otel-infra.json` (hand-authored dashboard, ~16-18 panels)
- README "## Step 9" section + FAQ entries
- Live-verified via `docker compose up -d --wait` + `mise run load` + dashboard inspection
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/quick/260502-j00-add-infrastructure-exporters-rabbitmq-pr/260502-j00-CONTEXT.md
@.planning/quick/260502-j00-add-infrastructure-exporters-rabbitmq-pr/260502-j00-RESEARCH.md
@docker-compose.yml
@grafana/dashboards/dashboards.yaml
@grafana/dashboards/ose-otel-demo.json
@grafana/dashboards/ose-otel-noc.json
@README.md

<interfaces>
Key facts the executor needs without further codebase exploration:

**otel-lgtm bind mount path for Prometheus config:** `/otel-lgtm/prometheus.yaml` (documented in grafana/docker-otel-lgtm README; mount is read-only).

**Existing dashboards.yaml provider config:** scans `/otel-lgtm/grafana/conf/provisioning/dashboards` — already mounted in compose. NO changes to dashboards.yaml required for the new dashboard.

**Datasource UIDs in otel-lgtm 0.26.0** (verified at Phase 7): `prometheus`, `tempo`, `loki`. Use these as fixed UIDs in dashboard panel targets.

**Dashboard portability rules** (extracted from `ose-otel-demo.json` + `ose-otel-noc.json`):
- Top-level fields present: `uid`, `title`, `description`, `tags`, `schemaVersion: 39`, `refresh`, `time`, `panels`, `links`, `annotations.list`, `editable: true`, `graphTooltip: 0`, `fiscalYearStartMonth: 0`, `liveNow: false`
- Top-level fields ABSENT: `id`, `version`, `iteration`
- Each panel `target` includes its own `datasource: {type, uid}` — never relies on a dashboard-level default
- Each panel target carries unique `refId` (A, B, C...)

**HikariCP gauge metric (Phase 8):** Java emits `db.client.connection.count`; OTLP server-side rewrite produces `db_client_connection_count` in Prometheus with attribute keys translated to label names (e.g., `service_name`, `state`).

**RabbitMQ plugin enable mechanism:** Setting `RABBITMQ_PLUGINS: "rabbitmq_management rabbitmq_prometheus"` env var on the rabbitmq service activates the plugin at boot; default metrics port is `15692` (`/metrics` endpoint).

**redis_exporter env contract:** `REDIS_ADDR=redis://valkey:6379` (no auth, RESP2/RESP3 native against Valkey 8.x); default port `9121`.

**postgres_exporter env contract:** `DATA_SOURCE_NAME=postgresql://orders:orders@postgres:5432/orders?sslmode=disable` (reuses application user — Path A per RESEARCH §4; production callout MUST land in README); default port `9187`.

**Pinned image tags (from RESEARCH §Stack Pins):**
- `oliver006/redis_exporter:v1.83.0`
- `prometheuscommunity/postgres-exporter:v0.19.1`
- `rabbitmq:4.3-management` (unchanged — plugin enabled via env var)
- `grafana/otel-lgtm:0.26.0` (unchanged)

**Span-name literals in producer/consumer code (verified by reading source):**
- Producer Valkey SET: span name is exactly `"SET"` (`InstrumentedJedisPool.java` line 83 — `tracer.spanBuilder("SET")`).
- Consumer Postgres INSERT: span name is exactly `"INSERT processed_orders"` (`OrderRepository.java` line 82 — `tracer.spanBuilder("INSERT " + TABLE)` where `TABLE = "processed_orders"`).
- Use these literals (NOT regex) in Tempo TraceQL queries.

**Bundled prometheus.yaml content (live-inspected from running ose-otel-lgtm container, lgtm 0.26.0):**

```yaml
---
global:
  scrape_native_histograms: true
otlp:
  keep_identifying_resource_attributes: true
  promote_resource_attributes:
    - service.instance.id
    - service.name
    - service.namespace
    - service.version
    - cloud.availability_zone
    - cloud.region
    - container.name
    - deployment.environment # backward compatibility
    - deployment.environment.name
    - k8s.cluster.name
    - k8s.container.name
    - k8s.cronjob.name
    - k8s.daemonset.name
    - k8s.deployment.name
    - k8s.job.name
    - k8s.namespace.name
    - k8s.node.name
    - k8s.pod.name
    - k8s.replicaset.name
    - k8s.statefulset.name
    - host.name
    - postgresql.database.name
    - postgresql.schema.name
    - postgresql.table.name
    - postgresql.index.name
    - database # used by otelcol/receiver/mongodb
    - kafka.cluster.alias
storage:
  tsdb:
    out_of_order_time_window: 10m
```

CRITICAL: The bundled file has NO `scrape_configs:` section — Prometheus inside lgtm receives app-emitted metrics via the `otlp:` ingest pathway (NOT via a self-scrape job). The override file MUST preserve the `otlp:`, `global:`, and `storage:` blocks verbatim, then ADD a `scrape_configs:` section for the three new exporter jobs. There is no `otel-collector` scrape job to preserve.
</interfaces>
</context>

<tasks>

<task id="T1" type="auto">
  <name>Task 1: Extend docker-compose.yml — RabbitMQ Prometheus plugin + 2 exporter services + custom prometheus.yaml mount</name>
  <files>docker-compose.yml, grafana/prometheus.yaml</files>
  <action>
**Step 1 — Author `grafana/prometheus.yaml`** (per RESEARCH Pitfall #1 — overwriting the bundled file without preserving its `otlp:` ingest block breaks app-emitted OTLP metrics in Mimir):

The bundled `/otel-lgtm/prometheus.yaml` in lgtm 0.26.0 has been live-inspected (see `<interfaces>` block above for the verbatim content). It contains NO `scrape_configs:` — only `global:`, `otlp:`, and `storage:` blocks. The `otlp:` block is what allows Prometheus inside lgtm to ingest app-emitted OTLP metrics (the workshop's `service_name=order-producer` / `order-consumer` series rely on this).

Write `grafana/prometheus.yaml` with EXACTLY this content (preserve the bundled blocks verbatim, then ADD `scrape_configs:`):

```yaml
# grafana/prometheus.yaml — Step 9 (quick-260502-j00) override of the bundled
# /otel-lgtm/prometheus.yaml. The `global:`, `otlp:`, and `storage:` blocks
# below are copied verbatim from grafana/otel-lgtm:0.26.0's bundled file —
# changing them breaks app-emitted OTLP metric ingest into Mimir. The
# `scrape_configs:` section is the Step 9 addition: three infrastructure
# exporters scraped over the compose default network. The `source: infra-exporter`
# static label is the disambiguator for the two-lens telemetry story
# (app-emitted OTel metrics vs exporter-scraped Prometheus metrics).
---
global:
  scrape_native_histograms: true
otlp:
  keep_identifying_resource_attributes: true
  promote_resource_attributes:
    - service.instance.id
    - service.name
    - service.namespace
    - service.version
    - cloud.availability_zone
    - cloud.region
    - container.name
    - deployment.environment # backward compatibility
    - deployment.environment.name
    - k8s.cluster.name
    - k8s.container.name
    - k8s.cronjob.name
    - k8s.daemonset.name
    - k8s.deployment.name
    - k8s.job.name
    - k8s.namespace.name
    - k8s.node.name
    - k8s.pod.name
    - k8s.replicaset.name
    - k8s.statefulset.name
    - host.name
    - postgresql.database.name
    - postgresql.schema.name
    - postgresql.table.name
    - postgresql.index.name
    - database # used by otelcol/receiver/mongodb
    - kafka.cluster.alias
storage:
  tsdb:
    out_of_order_time_window: 10m

scrape_configs:
  # Step 9 (quick-260502-j00): infrastructure exporters scraped over the
  # compose default network. Static label `source: infra-exporter` is the
  # disambiguator for the two-lens story (app-emitted vs exporter-scraped).
  - job_name: rabbitmq
    static_configs:
      - targets: ['rabbitmq:15692']
        labels:
          source: infra-exporter

  - job_name: redis_exporter
    static_configs:
      - targets: ['redis-exporter:9121']
        labels:
          source: infra-exporter

  - job_name: postgres_exporter
    static_configs:
      - targets: ['postgres-exporter:9187']
        labels:
          source: infra-exporter
```

If a future lgtm version changes the bundled file, the executor MAY re-inspect it via `docker exec ose-otel-lgtm cat /otel-lgtm/prometheus.yaml`, but for the current pinned `grafana/otel-lgtm:0.26.0` the verbatim content above is correct. Do NOT invent new top-level keys; do NOT remove the `otlp:` block.

**Step 2 — Modify `docker-compose.yml`:**

(a) On the `rabbitmq` service, add an `environment:` block (currently absent) with:
```yaml
    environment:
      RABBITMQ_PLUGINS: "rabbitmq_management rabbitmq_prometheus"
```
And add `"15692:15692"   # Prometheus plugin /metrics — Step 9` to the `ports:` list (under the existing 5672 + 15672 entries). Leave the healthcheck untouched.

(b) On the `lgtm` service, append one entry to the `volumes:` list:
```yaml
      - ./grafana/prometheus.yaml:/otel-lgtm/prometheus.yaml:ro  # Step 9 — custom scrape config (preserves bundled otlp ingest block + adds rabbitmq/redis/postgres exporters)
```

(c) Append two new services AFTER the existing `postgres` service and BEFORE the `lgtm` service (or at the bottom of `services:` if compose ordering is preferred — both work; place to keep semantic grouping with the dependency they observe):

```yaml
  # Step 9: redis_exporter scrapes Valkey and exposes Prometheus metrics on :9121.
  # No host port mapping needed — only the bundled Prometheus inside lgtm scrapes it.
  redis-exporter:
    image: oliver006/redis_exporter:v1.83.0
    container_name: ose-otel-redis-exporter
    environment:
      REDIS_ADDR: "redis://valkey:6379"
    depends_on:
      valkey:
        condition: service_healthy
    restart: unless-stopped

  # Step 9: postgres_exporter — connects via the application's `orders` user
  # (Path A per RESEARCH §4; production-fidelity callout lives in README Step 9).
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.19.1
    container_name: ose-otel-postgres-exporter
    environment:
      DATA_SOURCE_NAME: "postgresql://orders:orders@postgres:5432/orders?sslmode=disable"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
```

Per RESEARCH Pitfall #5: do NOT add healthchecks on the two exporter containers — `redis_up == 0` / `pg_up == 0` on the dashboard is a teaching moment, not a startup blocker.

Do not modify `valkey` or `postgres` services. Do not modify the existing healthchecks. Do not introduce a `networks:` block — both new services join the implicit `<project>_default` network like the existing 4 services.
  </action>
  <verify>
    <automated>
docker compose config -q && \
grep -q "rabbitmq_prometheus" docker-compose.yml && \
grep -q "15692:15692" docker-compose.yml && \
grep -q "oliver006/redis_exporter:v1.83.0" docker-compose.yml && \
grep -q "prometheuscommunity/postgres-exporter:v0.19.1" docker-compose.yml && \
grep -q "/otel-lgtm/prometheus.yaml:ro" docker-compose.yml && \
test -f grafana/prometheus.yaml && \
grep -q "^otlp:" grafana/prometheus.yaml && \
grep -q "keep_identifying_resource_attributes: true" grafana/prometheus.yaml && \
grep -q "promote_resource_attributes:" grafana/prometheus.yaml && \
grep -q "scrape_native_histograms: true" grafana/prometheus.yaml && \
grep -q "out_of_order_time_window: 10m" grafana/prometheus.yaml && \
grep -q "^scrape_configs:" grafana/prometheus.yaml && \
grep -q "job_name: rabbitmq" grafana/prometheus.yaml && \
grep -q "job_name: redis_exporter" grafana/prometheus.yaml && \
grep -q "job_name: postgres_exporter" grafana/prometheus.yaml && \
grep -q "source: infra-exporter" grafana/prometheus.yaml && \
echo COMPOSE+PROMETHEUS OK
    </automated>
  </verify>
  <done>
`docker compose config -q` exits 0 (compose file valid YAML and schema-correct). `grafana/prometheus.yaml` exists with the bundled `otlp:` ingest block, `global:` block, and `storage:` block preserved verbatim, plus a new `scrape_configs:` section with three jobs (rabbitmq, redis_exporter, postgres_exporter), each carrying static label `source: infra-exporter`. `docker-compose.yml` has rabbitmq env var, port 15692, two new exporter services with pinned tags, and the lgtm prometheus.yaml volume mount. No changes to existing healthchecks or to valkey/postgres/lgtm service definitions beyond the documented edits.
  </done>
</task>

<task id="T2" type="auto">
  <name>Task 2: Author grafana/dashboards/ose-otel-infra.json — 3rd 'Infra & Deps' dashboard</name>
  <files>grafana/dashboards/ose-otel-infra.json</files>
  <action>
Hand-author a new dashboard JSON file at `grafana/dashboards/ose-otel-infra.json` following the panel set in RESEARCH §8 and the portability rules in RESEARCH §5 / context `<interfaces>`.

**Top-level structure (must match Phase 7 conventions verified in `ose-otel-demo.json`):**

- `uid`: `"ose-otel-infra"` (must NOT collide with `ose-otel-demo` or `ose-otel-noc`)
- `title`: `"OSE OTel Demo — Infra & Deps"`
- `description`: explain that this is the Step 9 dashboard pairing app-emitted OTel telemetry with dedicated exporter metrics; reference the two-lens story.
- `tags`: `["ose-otel-demo", "infra", "otel"]`
- `schemaVersion`: `39`
- `refresh`: `"10s"`
- `time`: `{ "from": "now-15m", "to": "now" }`
- `editable`: `true`, `graphTooltip`: `0`, `fiscalYearStartMonth`: `0`, `liveNow`: `false`
- `annotations.list`: copy the standard `Annotations & Alerts` builtin from `ose-otel-demo.json`
- `links`: array with two entries linking to `/d/ose-otel-demo` (title "Workshop dashboard") and `/d/ose-otel-noc` (title "NOC dashboard"). Use Grafana's standard link schema (`type: "link"`, `targetBlank: false`, `keepTime: true`, `includeVars: false`).
- ABSENT top-level fields: `id`, `version`, `iteration`. Verify with `python3 -c "import json; d=json.load(open('grafana/dashboards/ose-otel-infra.json')); assert 'id' not in d and 'version' not in d and 'iteration' not in d"`.
- NO templating variables (per RESEARCH §5: query-derived vars on empty datasources at infra:up render the dashboard "broken-looking"; this dashboard is metric-heavy and needs none).

**Panel set — 18 panels per RESEARCH §8.** Use a 24-column grid. Header is always visible at `y=0`; each subsequent row is a Grafana `row` panel **collapsed by default (`collapsed: true`)** — this is a deliberate choice for the Infra & Deps dashboard so attendees expand one row at a time and read the two-lens story progressively. Do NOT mirror the noc.json `collapsed: false` pattern — noc is a single-glance NOC view; this dashboard is a guided walk.

*Header (4 stat panels at y=0):*
- Panel id 1, `redis_up` — type `stat`, datasource `prometheus`, expr `redis_up`, gridPos `{h:4, w:6, x:0, y:0}`. Threshold steps: 0→red, 1→green. Title "Redis (Valkey) up".
- Panel id 2, `pg_up` — type `stat`, datasource `prometheus`, expr `pg_up`, gridPos `{h:4, w:6, x:6, y:0}`. Same threshold scheme. Title "Postgres up".
- Panel id 3, "RabbitMQ up" — type `stat`, datasource `prometheus`, expr `up{job="rabbitmq"}`, gridPos `{h:4, w:6, x:12, y:0}`. Threshold steps: 0→red, 1→green (parity with `redis_up` and `pg_up`). Title "RabbitMQ exporter up".
- Panel id 4, "Active OTel services" — type `stat`, datasource `prometheus`, expr `count(group by (service_name) (target_info{service_name=~"order-.*"}))`, gridPos `{h:4, w:6, x:18, y:0}`. Title mirrors NOC dashboard panel 105 wording.

*Row panel (id 100) "HTTP / AMQP" at y=4, **`collapsed: true`**. When expanded, contains panels 5–7 at y=5..*
- Panel id 5 "RED — request rate by status" — timeseries, prometheus, `sum by (http_response_status_code) (rate(http_server_request_duration_seconds_count[1m]))`, gridPos `{h:8, w:8, x:0, y:5}`.
- Panel id 6 "AMQP queue depth (broker view)" — timeseries, prometheus, `rabbitmq_queue_messages{queue="orders.queue"}`, legend `{{queue}} (broker)`, `{h:8, w:8, x:8, y:5}`.
- Panel id 7 "AMQP queue depth (client estimate)" — timeseries, prometheus, `orders_queue_depth_estimate`, legend `client estimate`, `{h:8, w:8, x:16, y:5}`. Description references the side-by-side comparison.

*Row panel (id 200) "Cache (Valkey)" at y=13, **`collapsed: true`**. Panels 8–11 at y=14..*
- Panel id 8 "Valkey memory used" — timeseries, prometheus, `redis_memory_used_bytes`, unit `bytes`, `{h:8, w:6, x:0, y:14}`.
- Panel id 9 "Hits vs misses (rate/1m)" — timeseries, prometheus, two targets: A=`rate(redis_keyspace_hits_total[1m])` legend "hits", B=`rate(redis_keyspace_misses_total[1m])` legend "misses". `{h:8, w:6, x:6, y:14}`.
- Panel id 10 "Evictions (rate/1m)" — timeseries, prometheus, `rate(redis_evicted_keys_total[1m])`, `{h:8, w:6, x:12, y:14}`.
- Panel id 11 "Recent SET spans (app perspective)" — table, tempo, TraceQL `{resource.service.name="order-producer" && name="SET"}`, `{h:8, w:6, x:18, y:14}`. queryType `traceql`, tableType `traces`, limit 20. (Span name `"SET"` is the literal from `InstrumentedJedisPool.java` line 83.)

*Row panel (id 300) "Database (Postgres + HikariCP)" at y=22, **`collapsed: true`**. Panels 12–15 at y=23.. — the two-lens money row.*
- Panel id 12 "Connections — app view (HikariCP)" — timeseries, prometheus, `sum by (state) (db_client_connection_count{service_name="order-consumer"})`, legend `{{state}}`, `{h:8, w:6, x:0, y:23}`.
- Panel id 13 "Connections — server view (postgres_exporter)" — timeseries, prometheus, `pg_stat_database_numbackends{datname="orders"}`, legend "numbackends", `{h:8, w:6, x:6, y:23}`.
- Panel id 14 "Inserts/sec (server view)" — timeseries, prometheus, `rate(pg_stat_database_tup_inserted_total{datname="orders"}[1m])`, `{h:8, w:6, x:12, y:23}`.
- Panel id 15 "Recent INSERT spans (app view)" — table, tempo, TraceQL `{resource.service.name="order-consumer" && name="INSERT processed_orders"}`, `{h:8, w:6, x:18, y:23}`. (Span name `"INSERT processed_orders"` is the literal resolved from `OrderRepository.java` line 82 — `tracer.spanBuilder("INSERT " + TABLE)` where `TABLE = "processed_orders"`.)

*Row panel (id 400) "Broker (RabbitMQ Prometheus plugin)" at y=31, **`collapsed: true`**. Panels 16–18 at y=32..*
- Panel id 16 "Queue depth per queue" — timeseries, prometheus, `rabbitmq_queue_messages`, legend `{{queue}}`, `{h:8, w:8, x:0, y:32}`.
- Panel id 17 "Consumers per queue" — timeseries, prometheus, `rabbitmq_queue_consumers`, legend `{{queue}}`, `{h:8, w:8, x:8, y:32}`.
- Panel id 18 "Messages publish/deliver (rate/1m)" — timeseries, prometheus, two targets: A=`rate(rabbitmq_global_messages_publish_total[1m])` legend "published", B=`rate(rabbitmq_global_messages_delivered_total[1m])` legend "delivered". `{h:8, w:8, x:16, y:32}`.

**For every panel target:** include its own `datasource: {"type": "prometheus" | "tempo", "uid": "prometheus" | "tempo"}` block. Assign unique `refId` ("A", "B", ...) per target. For Tempo panels (11, 15) set `queryType: "traceql"` and `tableType: "traces"`.

**Row panels** use type `"row"`, `collapsed: true`, and store their child panels in the row's nested `panels: [...]` array. (Grafana 13 stores collapsed-row children inside the row's `panels` field; expanded rows can use either nested or top-level placement. Since all 4 rows in this dashboard are `collapsed: true`, use the nested form for all of them — this is the canonical schema for collapsed rows.)

After authoring, validate JSON: `python3 -m json.tool grafana/dashboards/ose-otel-infra.json > /dev/null`.

DO NOT modify `ose-otel-demo.json`, `ose-otel-noc.json`, or `dashboards.yaml` — Phase 7 invariants.
  </action>
  <verify>
    <automated>
python3 -m json.tool grafana/dashboards/ose-otel-infra.json > /dev/null && \
python3 -c "
import json
d = json.load(open('grafana/dashboards/ose-otel-infra.json'))
assert d['uid'] == 'ose-otel-infra', 'uid mismatch'
assert d['schemaVersion'] == 39, 'schemaVersion must be 39'
assert d.get('refresh') == '10s', 'refresh must be 10s'
assert 'id' not in d and 'version' not in d and 'iteration' not in d, 'forbidden top-level field present'
assert 'infra' in d['tags'] and 'otel' in d['tags'], 'tags missing'
assert any('ose-otel-demo' in (l.get('url','') or '') for l in d.get('links', [])), 'workshop link missing'
assert any('ose-otel-noc' in (l.get('url','') or '') for l in d.get('links', [])), 'noc link missing'
# Every row panel must be collapsed: true (Infra dashboard mandate)
rows = [p for p in d['panels'] if p.get('type') == 'row']
assert len(rows) == 4, f'expected 4 row panels, got {len(rows)}'
for r in rows:
    assert r.get('collapsed') is True, f'row id={r.get(\"id\")} must have collapsed: true'
# Walk all panels (top-level and nested under rows) — every non-row target must carry datasource.uid
def walk(panels):
    for p in panels:
        if p.get('type') == 'row':
            for child in p.get('panels', []) or []:
                yield child
        else:
            yield p
allp = list(walk(d['panels']))
for p in allp:
    for t in p.get('targets', []) or []:
        assert t.get('datasource', {}).get('uid') in ('prometheus', 'tempo'), f'panel {p.get(\"id\")} target missing datasource.uid'
        assert t.get('refId'), f'panel {p.get(\"id\")} target missing refId'
print(f'PANELS OK count={len(allp)} rows=4 collapsed=True')
" && \
! grep -l '\"uid\": \"ose-otel-infra\"' grafana/dashboards/ose-otel-demo.json grafana/dashboards/ose-otel-noc.json 2>/dev/null && \
echo DASHBOARD-AUTHORING OK
    </automated>
  </verify>
  <done>
`grafana/dashboards/ose-otel-infra.json` parses as JSON; uid is `ose-otel-infra`, schemaVersion 39, refresh 10s; no `id`/`version`/`iteration`; tags include `infra` and `otel`; cross-links to workshop + NOC dashboards present; all 4 row panels have `collapsed: true`; every panel target carries `datasource.uid` (`prometheus` or `tempo`) and a unique `refId`; existing two dashboards remain untouched (filename + content unchanged on disk).
  </done>
</task>

<task id="T3" type="auto">
  <name>Task 3: README Step 9 chapter + FAQ entries on app-vs-infra two-lens telemetry</name>
  <files>README.md</files>
  <action>
Insert a new top-level `## Step 9: Dashboards & Infrastructure Telemetry` section between the existing `## Step 8: Database & Cache` (line 346) and `## Concepts & FAQ` (line 415). Follow the established pattern (Steps 1–8): five `###` sub-headings with the EXACT titles below (these are asserted by automated verify):

1. `### What you'll learn`
2. `### Checkpoint`
3. `### Run`
4. `### What to look for`
5. `### Why it matters`

Then append at least two new FAQ entries inside `## Concepts & FAQ`.

**Required content for the Step 9 section:**

`### What you'll learn` — A 3-4 bullet list framing the two-lens lesson (app-emitted OTel telemetry vs dedicated infrastructure exporters; why production observability needs both; that the same physical thing — a connection, a queue, a memory page — looks different through each lens).

`### Checkpoint` — Per D-09: do NOT claim a tag exists. Phrase as "Workshop is at `main` HEAD past the most recent step-NN tag; Step 9 is delivered as quick task `260502-j00`. The orchestrator may apply an annotated tag `step-09-dashboards` at exit gate per WORK-01 / D-21 — if applied, attendees can `git checkout step-09-dashboards`. Otherwise reference the quick task ID in STATE.md."

`### Run` — Copy-pasteable commands that work on a fresh `mise run infra:up`:

```bash
mise run infra:up
# Validate the new exporters are scrapable:
curl -sS http://localhost:15692/metrics | head -5
docker exec ose-otel-lgtm wget -qO- 'http://localhost:9090/api/v1/query?query=up{job=~"rabbitmq|redis_exporter|postgres_exporter"}' | python3 -m json.tool
# Generate traffic so panels populate:
mise run dev      # in another terminal
mise run load     # in a third terminal — drives ~1 req/sec for 60s
# Open the new dashboard:
open http://localhost:3000/d/ose-otel-infra
```

`### What to look for` — A walk through the 4 rows. Highlight panel 12 vs panel 13 (HikariCP gauge vs `pg_stat_database_numbackends`) as the centerpiece — the two-lens money panel. Mention small skew is expected and is itself the lesson.

`### Why it matters` — One paragraph linking back to production observability. Include the explicit production-fidelity callout (RESEARCH §4 Path A):

> **Production callout: do NOT reuse the application's database user for postgres_exporter in production.** This workshop wires `postgres-exporter` with `DATA_SOURCE_NAME=postgresql://orders:orders@postgres:5432/orders` to keep the compose file minimal. In a real deployment, create a dedicated read-only role and grant `pg_monitor`. The boundary lesson — exporter as a separate principal with the minimum privilege required — is what survives translation to production.

**Required FAQ entries** (append two new Q/A blocks to `## Concepts & FAQ`, matching the existing Q/A formatting). Use these EXACT `### ` titles (asserted by automated verify):

1. `### What is the two-lens telemetry model and why does it matter?` — Explain app-emitted (OTel SDK from your code: spans, business metrics) vs infra-emitted (exporters scraped by Prometheus: queue depth, eviction rates, slow queries). Both are necessary; neither alone is sufficient. Give the worked example: producer's `SET` span vs `redis_keyspace_hits_total`.

2. `### Why does the dashboard show two different connection-count series for Postgres?` — Reference panels 12 and 13. `db_client_connection_count{service_name="order-consumer"}` is what HikariCP reports the pool is doing; `pg_stat_database_numbackends{datname="orders"}` is what Postgres itself sees. Skew is normal (different scrape phases, different sampling). Equality within ±1 is healthy; sustained mismatch is a debug signal (e.g., leaked sessions held outside HikariCP).

(The exact phrase `two different connection-count` MUST appear in the second FAQ heading — automated verify uses this regex anchor.)

Optional 3rd FAQ entry if space allows: `### Why does the Step 9 prometheus.yaml override preserve the bundled otlp: block?` — explain Pitfall #1: the bundled lgtm prometheus.yaml has NO `scrape_configs:` section and ingests app-emitted OTLP metrics via the `otlp:` receiver block; dropping that block silently breaks app metrics in Mimir.

Do not modify any existing Step 1–8 sections, the Prerequisites section, or the Workshop Checkpoints table at line 68.
  </action>
  <verify>
    <automated>
# 1. Step 9 top-level section exists
grep -q "^## Step 9: Dashboards & Infrastructure Telemetry" README.md && \
# 2. Step 9 section contains all 5 mandated ### sub-headings (exact titles)
awk '/^## Step 9: Dashboards & Infrastructure Telemetry/,/^## Concepts & FAQ/' README.md \
  | grep -E "^### (What you'\''ll learn|Checkpoint|Run|What to look for|Why it matters)" \
  | wc -l | awk '$1 == 5 {exit 0} {exit 1}' && \
# 3. FAQ section has at least 8 ### entries (current 6 + at least 2 new)
awk '/^## Concepts & FAQ/{flag=1; next} flag' README.md \
  | grep -c "^### " \
  | awk '$1 >= 8 {exit 0} {exit 1}' && \
# 4. Specifically the two-lens connection-count FAQ entry exists by title
grep -qE "^### .*two different connection-count" README.md && \
# 5. The two-lens model FAQ entry exists by title
grep -qE "^### .*two-lens telemetry model" README.md && \
# 6. Production callout (pg_monitor) present
grep -q "pg_monitor" README.md && \
# 7. Cross-link to new dashboard URL present
grep -q "/d/ose-otel-infra" README.md && \
echo README-STEP-9 OK
    </automated>
  </verify>
  <done>
`## Step 9` section exists with the 5 sub-headings exactly named per the Phase 1–8 pattern; FAQ section has grown by ≥2 entries (from 6 to ≥8 `###` entries) including the two named FAQ titles ("two-lens telemetry model" and "two different connection-count"); production-fidelity callout (`pg_monitor`) present; cross-link to `/d/ose-otel-infra` present; existing Step 1–8 content unchanged (verified by reviewing diff before commit).
  </done>
</task>

<task id="T4" type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-verify smoke — full stack up, all 3 exporters scraping, OTLP ingest preserved, new dashboard renders</name>
  <files>(no source edits — verification only)</files>
  <what-built>
Tasks 1–3 are merged in the worktree. The compose file declares 6 services (rabbitmq with plugin enabled + 15692 exposed, valkey, postgres, lgtm with custom prometheus.yaml mount, redis-exporter, postgres-exporter). The new dashboard is provisioned. The README documents Step 9.
  </what-built>
  <how-to-verify>
1. Bring the stack down clean and back up against the new compose:
   ```bash
   docker compose down
   mise run infra:up      # docker compose up -d --wait
   docker compose ps      # expect 6 services in 'running' / 'healthy' state
   ```
2. RabbitMQ plugin endpoint reachable on the host:
   ```bash
   curl -sS http://localhost:15692/metrics | grep -E '^rabbitmq_(build_info|queue_messages|identity_info)' | head -5
   ```
   Expected: at least 3 `rabbitmq_*` metric lines (text/plain, no HTML error page).
3. The bundled Prometheus inside lgtm reports the 3 NEW scrape jobs as up=1:
   ```bash
   docker exec ose-otel-lgtm wget -qO- 'http://localhost:9090/api/v1/query?query=up{job=~"rabbitmq|redis_exporter|postgres_exporter"}' | python3 -m json.tool
   ```
   Expected: jobs `rabbitmq`, `redis_exporter`, `postgres_exporter` each with `value: [..., "1"]` (3 series). NOTE: do NOT expect an `otel-collector` job — the bundled lgtm prometheus.yaml has NO scrape jobs; app metrics arrive via the `otlp:` ingest pathway (verified separately in step 4).
4. **OTLP-ingest regression check** (proves the bundled `otlp:` block survived the override). First start the apps so they emit OTLP metrics:
   ```bash
   mise run dev       # in another terminal — both services start + connect
   ```
   Then, after ~30s, query Prometheus for the resource-info series the OTLP ingest path materializes:
   ```bash
   docker exec ose-otel-lgtm wget -qO- 'http://localhost:9090/api/v1/query?query=target_info{service_name=%22order-producer%22}' | python3 -m json.tool
   ```
   Expected: `result` array contains ≥1 series with `service_name="order-producer"`. If empty → the `otlp:` block was dropped from `grafana/prometheus.yaml` (Pitfall #1). Go fix Task 1.
5. Generate traffic so dashboard panels populate:
   ```bash
   mise run load      # in a third terminal — load.sh runs ~60s
   ```
6. Open `http://localhost:3000/d/ose-otel-infra` in a browser. Confirm:
   - Header row: 4 stat panels green (`redis_up=1`, `pg_up=1`, `up{job="rabbitmq"}=1`, "Active OTel services" ≥ 2).
   - Expand "Database" row: panel 12 (HikariCP gauge `db_client_connection_count`) AND panel 13 (`pg_stat_database_numbackends`) both render at least one data series. Both should be non-empty within ~30s of `mise run load` starting.
   - Expand "Cache" row: panel 9 (hits/misses rate) shows non-zero values during load.
   - Expand "Broker" row: panel 16 (queue depth per queue) renders `orders.queue` series.
   - Cross-links at top of dashboard navigate to `/d/ose-otel-demo` and `/d/ose-otel-noc` — and those two dashboards still render unchanged.
7. Confirm no regressions on the original two dashboards:
   ```bash
   curl -sS http://localhost:3000/api/search | python3 -c "import json,sys; d=json.load(sys.stdin); print([x['uid'] for x in d if x.get('uid','').startswith('ose-otel')])"
   ```
   Expected: list contains `ose-otel-demo`, `ose-otel-noc`, `ose-otel-infra`.

If any check fails, capture the failing output and the dashboard JSON path/panel id, then revise the relevant earlier task and re-run this checkpoint. Do not apply the `step-09-dashboards` git tag — that is the orchestrator's exit-gate responsibility per D-09 / WORK-01.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to commit + verifier handoff, or describe failures (compose service not running, panel empty, dashboard 404, prometheus job down, OTLP regression) so the planner can route a revision.</resume-signal>
  <done>
Operator confirms: 6 services running; rabbitmq:15692 returns Prometheus metrics; bundled Prometheus reports the 3 new jobs `up=1` (rabbitmq, redis_exporter, postgres_exporter); OTLP-ingest regression check passes (`target_info{service_name="order-producer"}` returns ≥1 series after `mise run dev`); new dashboard renders all 4 header panels green and all 4 rows populate during `mise run load`; `ose-otel-demo` and `ose-otel-noc` dashboards still present and functional. NO `step-09-*` git tag applied by the executor.
  </done>
</task>

</tasks>

<verification>
After all tasks land:

- [ ] `docker compose config -q` returns 0
- [ ] `docker compose ps` shows 6 services running (rabbitmq, valkey, postgres, lgtm, redis-exporter, postgres-exporter)
- [ ] `curl -sS http://localhost:15692/metrics | head` returns Prometheus text with `rabbitmq_*` metrics
- [ ] In-container Prometheus reports 3 new scrape jobs `up=1` (rabbitmq, redis_exporter, postgres_exporter)
- [ ] OTLP-ingest regression check: `target_info{service_name="order-producer"}` returns ≥1 series after `mise run dev` — proves the bundled `otlp:` block survived the override
- [ ] `http://localhost:3000/d/ose-otel-infra` renders; all 4 header stats green; Database row shows HikariCP gauge AND postgres_exporter numbackends side-by-side
- [ ] `http://localhost:3000/d/ose-otel-demo` and `/d/ose-otel-noc` continue to render unchanged
- [ ] README has `## Step 9` section + at least 2 new FAQ entries (FAQ `###` count grew from 6 to ≥8); production-fidelity callout present
- [ ] No edits to `grafana/dashboards/ose-otel-demo.json`, `grafana/dashboards/ose-otel-noc.json`, or `grafana/dashboards/dashboards.yaml`
- [ ] No `step-09-*` git tag applied (D-09 / WORK-01 orchestrator-owned)
</verification>

<success_criteria>
1. **Two-lens visibility:** A workshop attendee running `mise run infra:up && mise run dev && mise run load` can open `http://localhost:3000/d/ose-otel-infra` and see Phase 8's HikariCP gauge AND postgres_exporter's `numbackends` rendering simultaneously in the Database row.
2. **Reproducibility:** Every new image is pinned to an exact tag (`oliver006/redis_exporter:v1.83.0`, `prometheuscommunity/postgres-exporter:v0.19.1`); no `:latest`.
3. **Phase 7 invariants preserved:** Existing two dashboards unchanged on disk, both still render after the compose change.
4. **No bundled-job regression:** The bundled `otlp:` block (with `keep_identifying_resource_attributes: true` and the full `promote_resource_attributes` list) survives in the override `grafana/prometheus.yaml`, AND `target_info{service_name="order-producer"}` returns ≥1 series at smoke verify — proving app-emitted OTLP metrics still ingest into Mimir.
5. **Workshop framing:** README Step 9 chapter follows the established 5-subheading pattern, contains copy-pasteable commands and the production-fidelity callout.
</success_criteria>

<output>
After completion, create `.planning/quick/260502-j00-add-infrastructure-exporters-rabbitmq-pr/260502-j00-SUMMARY.md` summarizing:
- Files modified (docker-compose.yml, README.md) and created (grafana/prometheus.yaml, grafana/dashboards/ose-otel-infra.json)
- Verifier hooks: every must_have truth + key_link should be checkable from the live stack
- Any deviations from RESEARCH (e.g., bundled prometheus.yaml content surprised the executor — note actual structure: otlp: ingest, no scrape_configs)
- Reminder that `step-09-dashboards` annotated tag is orchestrator-applied, NOT auto-applied
</output>
</content>
</invoke>